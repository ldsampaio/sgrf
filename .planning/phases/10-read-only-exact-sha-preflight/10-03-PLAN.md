---
phase: 10-read-only-exact-sha-preflight
plan: "03"
type: execute
wave: 3
depends_on: ["10-02"]
files_modified:
  - tools/release-close/release-close.js
  - tools/release-close/gh-client.js
autonomous: true
requirements:
  - REL-01
  - SAFE-01
estimate:
  tokens: 45000
  raw_tokens: 30000
  tasks: 2
  confidence: medium
must_haves:
  truths:
    - verify command wires the real gh-client (not the fake) as default; --fixture flag activates the fake client (D-14)
    - verify reports explicit result for tag, main, CI, Release, Milestone; includes mutations: 0
    - Invalid versions, repositories, tags, abbreviated or mismatched SHAs, credentials, and insufficient endpoint permissions are rejected before any mutation-capable path can run
    - The live v0.1.1 baseline reports tag object 0a68d6f0c55e7be07d13a0bbc4ed36d4af772630, peeled/main/expected commit 10c62ac85fd3ab275b8926c89f5f34ba4116e2cf, green runs 36095855139 and 36095872529, missing Release/Milestone
  artifacts:
    - tools/release-close/release-close.js
    - tools/release-close/gh-client.js
  key_links:
    - release-close.js imports ghClient as default; makeFakeClient activates only with --fixture
    - verify command reads tag, main, CI, Release, Milestone and reports each explicitly
  prohibitions:
    - verify must never write to the remote; mutations: 0 on every output
    - --fixture is the only way fixtures enter production flow; no implicit fallback to fake
---

<objective>
Wire the verify command with the real gh-client as default; --fixture flag activates the fake client (D-14). Verify reports explicit result for tag, main, CI, Release, Milestone; includes mutations: 0. Invalid versions, repositories, tags, abbreviated or mismatched SHAs, credentials, and insufficient endpoint permissions are rejected before any mutation-capable path can run. The live v0.1.1 baseline reports tag object 0a68d6f0c55e7be07d13a0bbc4ed36d4af772630, peeled/main/expected commit 10c62ac85fd3ab275b8926c89f5f34ba4116e2cf, green runs 36095855139 and 36095872529, missing Release/Milestone.

Purpose: Close the verify command with the real client, proving the read-only preflight works against live GitHub.
Output: verify command wired with real gh-client; --fixture flag activates fake; live baseline reports correctly.
</objective>

<execution_context>
@/home/lucas/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/lucas/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-read-only-exact-sha-preflight/10-CONTEXT.md
@tools/release-close/release-close.js
@tools/release-close/gh-client.js
@tools/release-close/fake-client.js
@.planning/phases/09-release-close-contract-fixtures/09-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Wire verify command with real gh-client (D-14)</name>
  <files>tools/release-close/release-close.js, tools/release-close/gh-client.js</files>
  <read_first>tools/release-close/release-close.js (current verify implementation), tools/release-close/gh-client.js (from plan 01), tools/release-close/fake-client.js (makeFakeClient), 10-CONTEXT.md decisions D-10, D-11, D-13, D-14</read_first>
  <action>Wire the verify command in release-close.js to use the real gh-client as default. The verify command reads tag, main, CI, Release, Milestone and reports each explicitly. --fixture flag activates the fake client (D-14); --fixture is the only way fixtures enter production flow; no implicit fallback to fake. The verify command validates inputs (version, sha format) before any read; abbreviated or mismatched SHAs are rejected. The live v0.1.1 baseline reports: tag object 0a68d6f0c55e7be07d13a0bbc4ed36d4af772630, peeled/main/expected commit 10c62ac85fd3ab275b8926c89f5f34ba4116e2cf, green runs 36095855139 and 36095872529, missing Release/Milestone. Output includes mutations: 0 on every verify output.</action>
  <verify>
    <automated>node tools/release-close/release-close.js verify --help; node tools/release-close/release-close.js verify --json 2>&1 | head -30</automated>
    <fails_when>verify command does not use real gh-client as default, or --fixture does not activate the fake client, or output does not include mutations: 0</fails_when>
  </verify>
  <done>verify command wired with real gh-client; --fixture activates fake; live baseline reports correctly; mutations: 0 on every output.</done>
  <acceptance_criteria>Source asserts release-close.js imports ghClient as default and makeFakeClient only with --fixture; behavior asserts verify reports tag, main, CI, Release, Milestone explicitly; output includes mutations: 0.</acceptance_criteria>
</task>

<task type="auto">
  <name>Input validation and rejection paths (SAFE-01)</name>
  <files>tools/release-close/release-close.js</files>
  <read_first>tools/release-close/release-close.js (from task 1), 10-CONTEXT.md decision D-13, SAFE-01 success criteria</read_first>
  <action>Implement input validation in the verify command: invalid versions (not vX.Y.Z), invalid repositories (not owner/repo format), abbreviated SHAs (not 40 hex chars), mismatched SHAs (not 40 hex), and missing credentials are rejected before any read. Per D-13: 404 on any object means absence — no repo probe, no permission disambiguation step. "Absent vs insufficient permission" is out of scope; a real permission failure surfaces as a distinct HTTP error the tool already recuses (transport family). The verify command rejects invalid inputs with PT-BR messages and exits 1.</action>
  <verify>
    <automated>node tools/release-close/release-close.js verify --version invalid; echo "exit=$?"; node tools/release-close/release-close.js verify --sha abc; echo "exit=$?"</automated>
    <fails_when>invalid version or abbreviated SHA is accepted, or the exit code is not 1 for invalid inputs</fails_when>
  </verify>
  <done>Invalid versions, repositories, tags, abbreviated/mismatched SHAs rejected before any read; 404 maps to absence; permission failures recused.</done>
  <acceptance_criteria>Source asserts verify validates version (vX.Y.Z), sha (40 hex), repo (owner/repo) before any read; behavior asserts invalid inputs exit 1 with PT-BR messages; 404 maps to absence, not permission error.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

|| Boundary | Description |
||----------|-------------|
|| operator→verify argv | Untrusted --version/--sha/--repo values cross here |
|| gh-client→gh subprocess | Real GitHub API calls cross here; 404→absence per D-13 |

## STRIDE Threat Register

|| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
||-----------|----------|-----------|----------|-------------|-----------------|
|| T-10-06 | Tampering | verify argv validation | high | mitigate | Version, SHA, repo validated before any read; abbreviated SHAs rejected |
|| T-10-07 | Information disclosure | verify output | medium | mitigate | Output limited to decision fields plus mutations counter; no headers or credentials |
|| T-10-08 | Tampering | --fixture flag | high | mitigate | --fixture is the only way fixtures enter production flow; no implicit fallback to fake |

</threat_model>

<verification>
verify command wired with real gh-client; --fixture activates fake; input validation rejects invalid versions/SHAs; 404 maps to absence; mutations: 0 on every output.
</verification>

<success_criteria>
verify command wired with real gh-client; --fixture activates fake; input validation works; live baseline reports correctly; mutations: 0.
</success_criteria>

<output>
Create `.planning/phases/10-read-only-exact-sha-preflight/10-03-SUMMARY.md` when done
</output>