---
phase: 10-read-only-exact-sha-preflight
plan: "02"
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - tools/release-close/classify.js
  - tools/release-close/fixtures
autonomous: true
requirements:
  - SAFE-03
estimate:
  tokens: 40000
  raw_tokens: 30000
  tasks: 2
  confidence: medium
must_haves:
  truths:
    - Classifier reads failedRunIds from ci.records (D-11b, D-12), not from snapshot.failedRunIds or any top-level key
    - The classifier validates failedRunIds as an array of integers and reports FAILED when any required run is in the failed set
    - The 09 fixture suite pins the fail-open as expected (evidence.test.js MISSING, safe04.test.js) — those tests flip to expect FAILED when the source is wired
    - No stray key exists in the classifier contract after the refactor
  artifacts:
    - tools/release-close/classify.js
    - tools/release-close/fixtures
  key_links:
    - classify.js already validates failedRunIds with precedence above CONCURRENT — it only needs filling from ci.records
    - The classifier reads ci.records, derives failedRunIds, and validates against requiredRunIds
  prohibitions:
    - never add a top-level snapshot.failedRunIds key — the stray key is what the gap was about
    - never change the six-state classifier output shape (eligible, code, reason, ciCode, writeAction)
---

<objective>
Update classify.js to read failedRunIds from ci.records (D-11b, D-12) instead of any top-level key. The classifier already validates and honors failedRunIds — it only needs filling from the CI records. No new declared read (09-06 exact-surface check stays green). The 09 fixture suite pins the fail-open as expected (evidence.test.js MISSING, safe04.test.js) — those tests flip to expect FAILED when the source is wired.

Purpose: Close G-1 by wiring the failedRunIds source inside the CI read, not by adding a new key.
Output: Classifier reading failedRunIds from ci.records, with tests flipping from MISSING to FAILED.
</objective>

<execution_context>
@/home/lucas/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/lucas/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-read-only-exact-sha-preflight/10-CONTEXT.md
@tools/release-close/classify.js
@tools/release-close/gh-client.js
@.planning/phases/09-release-close-contract-fixtures/09-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Refactor classifier: failedRunIds from ci.records (D-11b, D-12)</name>
  <files>tools/release-close/classify.js</files>
  <read_first>tools/release-close/classify.js (current failedRunIds validation), tools/release-close/gh-client.js (from plan 01), 10-CONTEXT.md decisions D-11b, D-12, D-10</read_first>
  <action>Refactor classify.js so failedRunIds is derived from ci.records inside the CI read, not from a top-level snapshot.failedRunIds key. The classifier reads ci.records, derives failedRunIds as the IDs of non-green runs on the target branch+SHA, and validates against requiredRunIds with precedence above CONCURRENT. Remove any stray failedRunIds key from the classifier contract. The six-state classifier output shape stays unchanged (eligible, code, reason, ciCode, writeAction). PT-BR reasons / EN codes preserved.</action>
  <verify>
    <automated>node -e "import('./tools/release-close/classify.js').then(m => { const r = m.classifySnapshot({ target: { version: 'v0.1.1', expectedSha: '10c62ac85fd3ab275b8926c89f5f34ba4116e2cf' }, ci: { event: 'push', targetSha: '10c62ac85fd3ab275b8926c89f5f34ba4116e2cf', requiredRunIds: [36095855139, 36095872529], records: [{ runId: 36095855139, job: 'backend', headSha: '10c62ac85fd3ab275b8926c89f5f34ba4116e2cf', status: 'completed', conclusion: 'success' }, { runId: 36095872529, job: 'frontend', headSha: '10c62ac85fd3ab275b8926c89f5f34ba4116e2cf', status: 'completed', conclusion: 'success' }] }, releases: [], milestones: [], closeMarkers: [] }); console.log(r.code);"")</automated>
    <fails_when>classifySnapshot throws on valid input, or the output shape changes, or failedRunIds is read from a top-level key</fails_when>
  </verify>
  <done>Classifier reads failedRunIds from ci.records; stray key removed; six-state output shape unchanged.</done>
  <acceptance_criteria>Source asserts no top-level failedRunIds key in the classifier contract; behavior asserts FAILED when any required run is in the failed set; tests flip from MISSING to FAILED when the source is wired.</acceptance_criteria>
</task>

<task type="auto">
  <name>Update 09 fixture suite: fail-open expectation flips to FAILED</name>
  <files>tools/release-close/fixtures</files>
  <read_first>tools/release-close/classify.js (from task 1), 09-VERIFICATION.md (G-1 gap description), COVERAGE.md (G-1 gate)</read_first>
  <action>Update the 09 fixture suite so the fail-open expectation flips to FAILED when the source is wired. evidence.test.js and safe04.test.js currently pin the fail-open as expected (MISSING); with failedRunIds derived from ci.records, those tests flip to expect FAILED. The redness is the correct signal — a failed run must produce FAILED, not MISSING. Add a regression test that does not exist today: a snapshot with a failed run in ci.records must classify as FAILED.</action>
  <verify>
    <automated>node --test tools/release-close/ 2>&1 | tail -10</automated>
    <fails_when>non-zero exit, or evidence.test.js / safe04.test.js still expect MISSING when a failed run is present in ci.records</fails_when>
  </verify>
  <done>Fixture suite flips fail-open expectation to FAILED; regression test for failed-run classification passes.</done>
  <acceptance_criteria>Source asserts evidence.test.js and safe04.test.js expect FAILED when a failed run is present; behavior asserts the regression test passes; the suite records the correct signal.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

|| Boundary | Description |
||----------|-------------|
|| classifier→ci.records | CI records cross here; failedRunIds derived, not declared |
|| fixture→classifier | Test data crosses here; fail-open expectation flips to FAILED |

## STRIDE Threat Register

|| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
||-----------|----------|-----------|----------|-------------|-----------------|
|| T-10-04 | Tampering | ci.records input | high | mitigate | Classifier validates requiredRunIds as exactly two integers; failedRunIds derived from records, not declared |
|| T-10-05 | Tampering | fixture suite expectations | medium | mitigate | Fail-open expectation flips to FAILED when the source is wired; the correct signal is redness, not silence |

</threat_model>

<verification>
classify.js reads failedRunIds from ci.records; stray key removed; fixture suite flips fail-open expectation to FAILED; regression test passes.
</verification>

<success_criteria>
Classifier reads failedRunIds from ci.records; stray key removed; fixture suite flips fail-open expectation to FAILED; regression test passes.
</success_criteria>

<output>
Create `.planning/phases/10-read-only-exact-sha-preflight/10-02-SUMMARY.md` when done
</output>