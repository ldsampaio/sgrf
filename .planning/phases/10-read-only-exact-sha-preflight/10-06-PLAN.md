---
phase: 10-read-only-exact-sha-preflight
plan: "06"
type: execute
wave: 6
depends_on: ["10-05"]
files_modified:
  - tools/release-close/integration.test.js
autonomous: true
requirements:
  - REL-01
  - SAFE-01
estimate:
  tokens: 40000
  raw_tokens: 30000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - Integration test with real gh cli (if available) or fake-only suite passes
    - The integration test proves the verify command works end-to-end with the real gh CLI and authenticated token
    - If the real gh CLI is not available, the fake-only suite proves the same contract with the --fixture flag
  artifacts:
    - tools/release-close/integration.test.js
  key_links:
    - integration test proves the verify command works end-to-end with the real gh CLI and authenticated token
    - If the real gh CLI is not available, the fake-only suite proves the same contract with the --fixture flag
  prohibitions:
    - never write to the remote in the integration test; mutations: 0 on every output
---

<objective>
Integration test with real gh cli (if available) or fake-only suite passes. The integration test proves the verify command works end-to-end with the real gh CLI and authenticated token. If the real gh CLI is not available, the fake-only suite proves the same contract with the --fixture flag.

Purpose: Prove the verify command works end-to-end with the real gh CLI and authenticated token.
Output: Integration test passing with the real gh CLI or fake-only suite.
</objective>

<execution_context>
@/home/lucas/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/lucas/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-read-only-exact-sha-preflight/10-CONTEXT.md
@.planning/phases/09-release-close-contract-fixtures/09-VERIFICATION.md
@tools/release-close/release-close.js
@tools/release-close/gh-client.js
@tools/release-close/fake-client.js
@tools/release-close/fixtures/live-baseline.json
</context>

<tasks>

<task type="auto">
  <name>Integration test with real gh CLI or fake-only suite</name>
  <files>tools/release-close/integration.test.js</files>
  <read_first>tools/release-close/release-close.js (verify command from plan 03), tools/release-close/gh-client.js (from plan 01), tools/release-close/fake-client.js, 10-CONTEXT.md decision D-14</read_first>
  <action>Create tools/release-close/integration.test.js importing describe/it from node:test and strict assert from node:assert. If the real gh CLI is available (gh auth status succeeds), run the verify command against the live GitHub API with the authenticated token. If the real gh CLI is not available, run the fake-only suite with the --fixture flag. The integration test proves the verify command works end-to-end with the real gh CLI and authenticated token. The fake-only suite proves the same contract with the --fixture flag. Mutations: 0 on every output. No write calls recorded.</action>
  <verify>
    <automated>node --test tools/release-close/integration.test.js</automated>
    <fails_when>non-zero exit, or any test fails, or mutations line does not print 0</fails_when>
  </verify>
  <done>Integration test passes with the real gh CLI or fake-only suite; mutations: 0 on every output.</done>
  <acceptance_criteria>Source asserts integration.test.js imports from node:test; behavior asserts integration test passes; mutations: 0 on every output; no write calls recorded.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

|| Boundary | Description |
||----------|-------------|
|| integration→gh CLI | Real GitHub API calls cross here; 404→absence per D-13 |
|| integration→fake-client | Test-only data crosses here; --fixture flag activates fake |

## STRIDE Threat Register

|| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
||-----------|----------|-----------|----------|-------------|-----------------|
|| T-10-13 | Tampering | integration test argv | high | mitigate | execFile (not exec) prevents shell injection; --repo/--version/--sha treated as opaque strings |
|| T-10-14 | Information disclosure | integration test output | medium | mitigate | Output limited to decision fields plus mutations counter; no headers or credentials |

</threat_model>

<verification>
Integration test passes with the real gh CLI or fake-only suite; mutations: 0 on every output; no write calls recorded.
</verification>

<success_criteria>
Integration test passes; mutations: 0 on every output; no write calls recorded.
</success_criteria>

<output>
Create `.planning/phases/10-read-only-exact-sha-preflight/10-06-SUMMARY.md` when done
</output>