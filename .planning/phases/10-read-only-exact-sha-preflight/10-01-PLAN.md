---
phase: 10-read-only-exact-sha-preflight
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/release-close/gh-client.js
  - tools/release-close/client.js
autonomous: true
requirements:
  - REL-01
  - SAFE-01
estimate:
  tokens: 50000
  raw_tokens: 35000
  tasks: 2
  confidence: medium
must_haves:
  truths:
    - All five gh reads work against the live GitHub API with the existing `gh` CLI and authenticated token
    - Each read returns the normalized { ok, status, data } envelope and never throws on 404 — 404 maps to { ok: false, status: 404, data: null }
    - CI run list method exists and returns runs filtered by event=push, headSha==expectedSha, branch in {main, version-tag}
    - The exact read-only client surface is provable: assertClientShape passes on the real client and no write-method names exist anywhere in the module
  artifacts:
    - tools/release-close/gh-client.js
    - tools/release-close/client.js
  key_links:
    - gh-client.js imports assertClientShape from client.js and passes the shape check
    - Each method uses `gh api` subprocess only (no direct HTTPS, no GH_TOKEN in env plumbing)
  prohibitions:
    - tool must never create, move, delete, or force-update a git ref or tag
    - tool must never read, log, or transmit operator credentials or auth headers
    - verify must never open a network connection or spawn a subprocess in this phase — wrong: it WILL spawn `gh api` subprocesses; the prohibition is on writes, not reads
---

<objective>
Wire gh-client.js with real `gh` CLI subprocess reads for the five read methods (getTagRef, getTagObject, getBranchHead, getReleaseByTag, listMilestones) plus a CI run list method. Each method returns the normalized { ok, status, data } envelope and never throws on 404 — 404 maps to { ok: false, status: 404, data: null }. CI run list filters by event=push, headSha==expectedSha, branch in {main, version-tag}. The exact read-only client surface is provable: assertClientShape passes on the real client and no write-method names exist anywhere in the module.

Purpose: Replace the Phase 9 stub with a real read-only client that the verify command can use.
Output: Wired gh-client.js passing assertClientShape with live reads returning normalized envelopes.
</objective>

<execution_context>
@/home/lucas/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/lucas/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-read-only-exact-sha-preflight/10-CONTEXT.md
@tools/release-close/gh-client.js
@tools/release-close/client.js
@tools/release-close/fake-client.js
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Wire gh-client.js with real gh api subprocess reads</name>
  <files>tools/release-close/gh-client.js</files>
  <read_first>tools/release-close/gh-client.js (current stub), tools/release-close/client.js (assertClientShape contract), tools/release-close/fake-client.js (snapshot shape for reference), .github/workflows/ci.yml (CI authority: jobs backend/frontend)</read_first>
  <action>Replace each of the five stub methods in gh-client.js with real `gh api` subprocess calls using child_process.execFile (not exec, to avoid shell injection). Each method returns { ok, status, data }: getTagRef(version) → tag ref object; getTagObject(tagSha) → tag object with type+sha; getBranchHead(branch) → branch head sha; getReleaseByTag(version) → release or null (404 → { ok: false, status: 404, data: null }); listMilestones() → array of { number, title, state }. Add a sixth method listCiRuns(expectedSha, branch) that calls `gh api repos/{repo}/actions/runs` with event=push, headSha==expectedSha, branch filter. All 404s map to { ok: false, status: 404, data: null } — never throw. Non-404 errors throw with PT-BR message naming the method. The module exports { ghClient } with all six methods. assertClientShape(ghClient) must pass. No write methods anywhere in the module. No env token reading — the `gh` CLI handles auth via its own config.</action>
  <verify>
    <automated>node -e "import('./tools/release-close/gh-client.js').then(m => { assertClientShape(m.ghClient); console.log('shape OK'); console.log('methods:', Object.keys(m.ghClient).filter(k => typeof m.ghClient[k] === 'function')) })"</automated>
    <fails_when>assertClientShape throws, or any write-method name appears in the module, or any method throws on 404</fails_when>
  </verify>
  <done>All six methods pass assertClientShape, return normalized envelopes, and 404s map to null never throw.</done>
  <acceptance_criteria>Source asserts gh-client.js contains no write method names (no post/patch/put/delete/force-update/create/delete-ref); behavior asserts assertClientShape(ghClient) returns true; each method returns { ok, status, data } envelope.</acceptance_criteria>
</task>

<task type="auto">
  <name>CI run discovery by property (D-10) — listCiRuns implementation</name>
  <files>tools/release-close/gh-client.js</files>
  <read_first>tools/release-close/gh-client.js (from task 1), .github/workflows/ci.yml (jobs backend/frontend, branch protection), 10-CONTEXT.md decisions D-10, D-11, D-11b</read_first>
  <action>Implement listCiRuns(expectedSha, branch) in gh-client.js: calls `gh api repos/{repo}/actions/runs` with per_page=100, filters client-side for event=push, headSha==expectedSha, branch in {main, version-tag}. Returns the matching runs array (empty if none). D-10: discovery by property, not operator input — no requiredRunIds input flag. D-11: "última verde" per branch+SHA — the most recent run on that branch+SHA must be green. Monotonic: a green re-run recovers, a red re-run blocks. D-11b: classifier reads failedRunIds from ci.records, not snapshot.failedRunIds. The method returns { ok, status, data: { runs, failedRunIds } } where failedRunIds is derived from non-green runs on the target branch+SHA.</action>
  <verify>
    <automated>node -e "import('./tools/release-close/gh-client.js').then(m => { const c = m.ghClient; console.log('listCiRuns:', typeof c.listCiRuns); })"</automated>
    <fails_when>listCiRuns is not a function, or the method signature doesn't accept (expectedSha, branch)</fails_when>
  </verify>
  <done>listCiRuns exists, accepts (expectedSha, branch), returns { ok, status, data: { runs, failedRunIds } } with runs filtered by event=push, headSha==expectedSha, branch in {main, version-tag}.</done>
  <acceptance_criteria>Source asserts listCiRuns filters client-side for event=push and headSha==expectedSha; behavior asserts the method returns a normalized envelope with runs array and failedRunIds derived from non-green runs.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

|| Boundary | Description |
||----------|-------------|
|| operator→CLI argv | Untrusted --repo/--version/--sha values cross here |
|| gh-client→gh subprocess | Real GitHub API calls cross here; 404→null is the only absence path |

## STRIDE Threat Register

|| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
||-----------|----------|-----------|----------|-------------|-----------------|
|| T-10-01 | Tampering | gh-client.js argv handling | high | mitigate | execFile (not exec) prevents shell injection; --repo/--version/--sha treated as opaque strings |
|| T-10-02 | Information disclosure | gh API response | medium | mitigate | Output limited to decision fields plus mutations counter; no headers or credentials logged |
|| T-10-03 | Tampering | CI run filtering | high | mitigate | Client-side filter on event=push, headSha==expectedSha, branch in {main, version-tag}; wrong-SHA runs excluded by construction |

</threat_model>

<verification>
assertClientShape(ghClient) passes; all six methods return normalized { ok, status, data } envelopes; 404s map to null never throw; listCiRuns filters by event=push, headSha==expectedSha, branch in {main, version-tag}.
</verification>

<success_criteria>
gh-client.js wired with real gh api reads; all six methods pass assertClientShape; 404s map to null; CI run discovery by property working.
</success_criteria>

<output>
Create `.planning/phases/10-read-only-exact-sha-preflight/10-01-SUMMARY.md` when done
</output>