---
phase: 10-read-only-exact-sha-preflight
plan: "04"
type: execute
wave: 4
depends_on: ["10-03"]
files_modified:
  - tools/release-close/fixtures
  - tools/release-close/gh-client.test.js
autonomous: true
requirements:
  - SAFE-01
  - SAFE-03
estimate:
  tokens: 40000
  raw_tokens: 30000
  tasks: 2
  confidence: medium
must_haves:
  truths:
    - Live baseline fixture encodes the verified live state: tag object 0a68d6f0c55e7be07d13a0bbc4ed36d4af772630, peeled/main/expected commit 10c62ac85fd3ab275b8926c89f5f34ba4116e2cf, green runs 36095855139 and 36095872529, missing Release/Milestone
    - Snapshot tests prove the fixture shape matches the assertClientShape contract and the classifier contract
    - The historical red run on another SHA is recorded as "ignored" by construction — the tool's vocabulary names it, not hides it
  artifacts:
    - tools/release-close/fixtures/live-baseline.json
    - tools/release-close/gh-client.test.js
  key_links:
    - live-baseline.json encodes the canonical live baseline verbatim — tag object sha, peeled commit, remote main head, expectedSha, release absent, milestones empty, frozen timestamp, canned run identifiers
    - gh-client.test.js proves the fixture shape matches the assertClientShape contract and the classifier contract
  prohibitions:
    - never hardcode credentials or auth headers in the fixture
    - never include the historical red run in the green verdict — it is ignored by construction
---

<objective>
Create the live baseline fixture encoding the verified live state: tag object 0a68d6f0c55e7be07d13a0bbc4ed36d4af772630, peeled/main/expected commit 10c62ac85fd3ab275b8926c89f5f34ba4116e2cf, green runs 36095855139 and 36095872529, missing Release/Milestone. Snapshot tests prove the fixture shape matches the assertClientShape contract and the classifier contract. The historical red run on another SHA is recorded as "ignored" by construction — the tool's vocabulary names it, not hides it.

Purpose: Prove the live baseline fixture matches the verified live state and the classifier contract.
Output: Live baseline fixture + snapshot tests proving the fixture shape.
</objective>

<execution_context>
@/home/lucas/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/lucas/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-read-only-exact-sha-preflight/10-CONTEXT.md
@.planning/phases/09-release-close-contract-fixtures/09-VERIFICATION.md
@tools/release-close/fake-client.js
@tools/release-close/classify.js
</context>

<tasks>

<task type="auto">
  <name>Create live baseline fixture (live-baseline.json)</name>
  <files>tools/release-close/fixtures/live-baseline.json</files>
  <read_first>tools/release-close/fixtures/reference.json (Phase 9 reference fixture shape), 10-CONTEXT.md canonical refs (live baseline verified live 2026-09-25), .planning/phases/09-release-close-contract-fixtures/09-VERIFICATION.md</read_first>
  <action>Create tools/release-close/fixtures/live-baseline.json encoding the canonical live baseline verbatim: tag object sha 0a68d6f0c55e7be07d13a0bbc4ed36d4af772630 of type tag, peeled commit 10c62ac85fd3ab275b8926c89f5f34ba4116e2cf, remote main head identical, expectedSha identical, release absent (404), milestones empty, frozen timestamp string, canned run identifiers 36095855139 (main, success) and 36095872529 (v0.1.1 tag, success), historical red run 36095528423 on another SHA recorded as "ignored" by construction. The fixture is frozen — timestamp string, not a live read. No credentials or auth headers in the fixture.</action>
  <verify>
    <automated>node -e "import('./tools/release-close/fixtures/live-baseline.json').then(f => { console.log('tag:', f.tag.object.sha); console.log('peeled:', f.tag.peeledCommit); console.log('runs:', f.ci.records.length); })"</automated>
    <fails_when>fixture missing any of tag object sha, peeled commit, run identifiers, or includes the historical red run in the green verdict</fails_when>
  </verify>
  <done>Live baseline fixture encodes the verified live state verbatim; historical red run recorded as ignored.</done>
  <acceptance_criteria>Source asserts live-baseline.json contains tag object sha 0a68d6f0c55e7be07d13a0bbc4ed36d4af772630, peeled commit 10c62ac85fd3ab275b8926c89f5f34ba4116e2cf, runs 36095855139 and 36095872529 both success; behavior asserts the fixture shape matches the assertClientShape contract and the classifier contract.</acceptance_criteria>
</task>

<task type="auto">
  <name>Snapshot tests for fixture shape and classifier contract</name>
  <files>tools/release-close/gh-client.test.js</files>
  <read_first>tools/release-close/fixtures/live-baseline.json (from task 1), tools/release-close/client.js (assertClientShape contract), tools/release-close/classify.js (six-state classifier contract)</read_first>
  <action>Create tools/release-close/gh-client.test.js importing describe/it from node:test and strict assert from node:assert. Tests: fixture shape matches assertClientShape contract (all six methods present, return { ok, status, data } envelope); 404 maps to { ok: false, status: 404, data: null }; historical red run recorded as "ignored" by construction; classifier contract: requiredRunIds exactly two integers, failedRunIds validation, CI allowlist. The tests use the live-baseline.json fixture, not the reference fixture.</action>
  <verify>
    <automated>node --test tools/release-close/gh-client.test.js</automated>
    <fails_when>non-zero exit, or any test fails</fails_when>
  </verify>
  <done>Snapshot tests prove the fixture shape matches the assertClientShape contract and the classifier contract.</done>
  <acceptance_criteria>Source asserts test file imports from node:test; behavior asserts all snapshot tests pass; fixture shape matches the contract.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

|| Boundary | Description |
||----------|-------------|
|| fixture→classifier | Test data crosses here; live baseline frozen verbatim |
|| historical red run | Ignored by construction — vocabulary names it, not hides it |

## STRIDE Threat Register

|| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
||-----------|----------|-----------|----------|-------------|-----------------|
|| T-10-09 | Tampering | live-baseline.json | medium | mitigate | Canonical SHA values frozen verbatim; full-40-hex validators reject abbreviations |
|| T-10-10 | Information disclosure | historical red run | low | mitigate | Recorded as "ignored" by construction; vocabulary names it, not hides it |

</threat_model>

<verification>
live-baseline.json encodes the verified live state verbatim; snapshot tests prove the fixture shape; historical red run recorded as ignored.
</verification>

<success_criteria>
Live baseline fixture created; snapshot tests pass; historical red run recorded as ignored.
</success_criteria>

<output>
Create `.planning/phases/10-read-only-exact-sha-preflight/10-04-SUMMARY.md` when done
</output>