---
phase: 10-read-only-exact-sha-preflight
plan: "05"
type: execute
wave: 5
depends_on: ["10-04"]
files_modified:
  - tools/release-close/release-close.js
  - tools/release-close/verify.test.js
autonomous: true
requirements:
  - REL-01
  - SAFE-01
  - SAFE-03
estimate:
  tokens: 50000
  raw_tokens: 35000
  tasks: 2
  confidence: medium
must_haves:
  truths:
    - Full verification suite: verify against live-baseline.json reports tag, main, CI, Release, Milestone explicitly with mutations: 0
    - Each verify output carries mutations: 0 and the suite fails if any write call is recorded
    - node --test "tools/release-close/*.test.js" passes on a clean checkout with no network and no database
    - The strict peel: annotated tag whose peeled commit equals remote main and the supplied full SHA; any mismatch reported without a write action
  artifacts:
    - tools/release-close/release-close.js
    - tools/release-close/verify.test.js
  key_links:
    - verify command reads tag, main, CI, Release, Milestone and reports each explicitly
    - mutations: 0 on every verify output; any write call recorded fails the suite
  prohibitions:
    - verify must never write to the remote; mutations: 0 on every output
    - never open a network connection or spawn a subprocess in the test suite — the test suite uses the live-baseline.json fixture
---

<objective>
Full verification suite: verify against live-baseline.json reports tag, main, CI, Release, Milestone explicitly with mutations: 0. Each verify output carries mutations: 0 and the suite fails if any write call is recorded. node --test "tools/release-close/*.test.js" passes on a clean checkout with no network and no database. The strict peel: annotated tag whose peeled commit equals remote main and the supplied full SHA; any mismatch reported without a write action.

Purpose: Prove the full verification suite passes with the live baseline fixture.
Output: Full verification suite passing with mutations: 0 on every verify output.
</objective>

<execution_context>
@/home/lucas/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/lucas/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-read-only-exact-sha-preflight/10-CONTEXT.md
@.planning/phases/09-release-close-contract-fixtures/09-VERIFICATION.md
@tools/release-close/release-close.js
@tools/release-close/gh-client.js
@tools/release-close/fake-client.js
@tools/release-close/fixtures/live-baseline.json
</context>

<tasks>

<task type="auto">
  <name>Full verification suite with live-baseline.json</name>
  <files>tools/release-close/verify.test.js</files>
  <read_first>tools/release-close/release-close.js (verify command from plan 03), tools/release-close/fixtures/live-baseline.json (from plan 04), tools/release-close/gh-client.js (from plan 01), .planning/phases/09-release-close-contract-fixtures/09-VERIFICATION.md (strict peel, exact CI evidence)</read_first>
  <action>Create tools/release-close/verify.test.js importing describe/it from node:test and strict assert from node:assert. Tests: verify against live-baseline.json reports tag, main, CI, Release, Milestone explicitly with mutations: 0; each verify output carries mutations: 0 and the suite fails if any write call is recorded; strict peel: annotated tag whose peeled commit equals remote main and the supplied full SHA; any mismatch reported without a write action; invalid versions, repositories, tags, abbreviated or mismatched SHAs rejected before any read; 404 maps to absence; permission failures recused. The test suite uses the live-baseline.json fixture, not the reference fixture.</action>
  <verify>
    <automated>node --test tools/release-close/verify.test.js</automated>
    <fails_when>non-zero exit, or any test fails, or mutations line does not print 0</fails_when>
  </verify>
  <done>Full verification suite passes with mutations: 0 on every verify output; strict peel proved; invalid inputs rejected.</done>
  <acceptance_criteria>Source asserts verify.test.js imports from node:test; behavior asserts all verification tests pass; mutations: 0 on every verify output; strict peel proved.</acceptance_criteria>
</task>

<task type="auto">
  <name>Regression: verify output shape and --json format</name>
  <files>tools/release-close/release-close.js</files>
  <read_first>tools/release-close/release-close.js (verify command from plan 03), 10-CONTEXT.md decisions D-14, D-10, D-11</read_first>
  <action>Verify the verify command output shape: human-readable PT-BR text by default, JSON with --json flag printing the decision object plus mutations field. The --json flag prints the plan structure before the human text while the gate verdict stays PT-BR text, so an unattended consumer cannot parse its way to a decision. The emitted mutations value is the measured counter, making the zero-mutation claim an implication of the write trap rather than a literal. Verify the output includes: tag state, main state, CI state (runs + failedRunIds), Release state, Milestone state, mutations: 0.</action>
  <verify>
    <automated>node tools/release-close/release-close.js verify --json 2>&1 | grep -c mutations; node tools/release-close/release-close.js verify 2>&1 | grep -c "mutations"</automated>
    <fails_when>--json does not print mutations field, or default verify output does not include mutations line</fails_when>
  </verify>
  <done>Verify output shape correct: human-readable PT-BR by default, JSON with --json; mutations: 0 in both modes.</done>
  <acceptance_criteria>Source asserts verify --json prints decision object plus mutations field; behavior asserts default verify output contains mutations zero line; output includes tag, main, CI, Release, Milestone states.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

|| Boundary | Description |
||----------|-------------|
|| verify→live-baseline.json | Test fixture crosses here; live baseline frozen verbatim |
|| verify→gh-client | Real GitHub API calls cross here; 404→absence per D-13 |

## STRIDE Threat Register

|| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
||-----------|----------|-----------|----------|-------------|-----------------|
|| T-10-11 | Tampering | verify output shape | high | mitigate | --json prints decision object plus mutations field; default output includes mutations zero line |
|| T-10-12 | Information disclosure | verify output | medium | mitigate | Output limited to decision fields plus mutations counter; no headers or credentials |

</threat_model>

<verification>
Full verification suite passes with mutations: 0 on every verify output; strict peel proved; invalid inputs rejected; --json format correct.
</verification>

<success_criteria>
Full verification suite passes; mutations: 0 on every verify output; strict peel proved; --json format correct.
</success_criteria>

<output>
Create `.planning/phases/10-read-only-exact-sha-preflight/10-05-SUMMARY.md` when done
</output>