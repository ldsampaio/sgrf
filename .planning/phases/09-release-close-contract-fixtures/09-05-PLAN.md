---
phase: 09-release-close-contract-fixtures
plan: "05"
type: execute
wave: 4
depends_on:
  - 09-02
coupling_justified:
  - "09-04: both plans run the shared tools/release-close/ suite while writing disjoint files; 09-05 rewrites the frozen fixtures that 09-04 only reads, and 09-05 keeps verify and plan green on the frozen baseline, so either order yields the same suite result."
  - "09-06: same shared suite and disjoint implementation files; the one shared input is the fixture set, which this plan owns and 09-06 only reads through makeFakeClient, and the scripted-failure vocabulary 09-06 preserves is untouched by this plan."
files_modified:
  - tools/release-close/classify.js
  - tools/release-close/classify.test.js
  - tools/release-close/release-close.js
  - tools/release-close/safe04.test.js
  - tools/release-close/fixtures/reference.json
  - tools/release-close/fixtures/missing.json
  - tools/release-close/fixtures/partial.json
  - tools/release-close/fixtures/duplicate.json
  - tools/release-close/fixtures/conflicting.json
  - tools/release-close/fixtures/failed.json
  - tools/release-close/fixtures/concurrent.json
  - tools/release-close/fixtures/complete.json
  - tools/release-close/fixtures/unrelated.json
autonomous: true
gap_closure: true
requirements:
  - OPS-02
  - SAFE-02
estimate:
  tokens: 44000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - Every snapshot the classifier accepts carries the five contract keys — a target block, a canonical ci block, a releases list, a milestones list, and a closeMarkers list — and a missing or malformed one of them is a TypeError, never a lenient default
    - The fixture set declares two shapes and never mixes a meaning inside one fixture — the six state fixtures plus `complete.json` and `unrelated.json` are flat classifier evidence, while `reference.json` and the `reviewed.json` that plan 09-08 owns are client-shaped snapshots that keep their `release` and `milestones` envelopes and gain only the `target` and `ci` blocks; `milestones` is a list in the first shape and an envelope in the second, and no fixture is ever both. Exactly one rule tells the shapes apart everywhere in this phase, and it is the same literal in every helper, `Array.isArray(snapshot.milestones)`, where true means flat classifier evidence and false means client-shaped
    - Only an explicit allowlist of canonical backend and frontend success records, at the exact full target SHA, on both required push-run identities, leaves CI unblocking; every other outcome blocks
    - Missing, malformed, pending, cancelled, timed-out, action-required, neutral, unknown, wrong-run, wrong-SHA, and contradictory CI evidence each classify as FAILED with the family named in a stable EN field and a PT-BR reason
    - No code path derives a green CI state from bare run identifiers, and no classifier input may omit CI evidence
    - Release and Milestone candidates are partitioned by requested version identity alone before duplicate or conflict is decided, and target-SHA equality is a comparison inside that partition rather than a membership test, so unrelated repository history cannot block or unblock a target and a same-version record with a divergent SHA stays in the decision instead of being filtered out of it
    - Expected-SHA validation is a separate decision input and never a prefilter, for the decision reports whether the partition's target SHAs validate against `expectedSha`, that fact names the divergence in the PT-BR reason, and it withholds the completed no-op without changing the state code or suppressing a conflict
    - All partitioned Release records and all partitioned Milestone records are retained with their stable identifiers; the classifier never collapses an array to its first element
    - A published Release plus a closed Milestone for the requested target reports state code MISSING together with the separate field outcome COMPLETE_NOOP, a PT-BR reason stating the target is already closed, and writeAction null; MISSING stays a non-blocking member of the six locked codes, no seventh code is introduced, and PARTIAL keeps its meaning of work still remaining
    - Repeated classification of the same frozen evidence, including the concurrent case, is byte-identical and never proposes a write
  artifacts:
    - tools/release-close/classify.js
    - tools/release-close/classify.test.js
    - tools/release-close/fixtures/reference.json
    - tools/release-close/fixtures/complete.json
    - tools/release-close/fixtures/unrelated.json
  key_links:
    - tools/release-close/fixtures/reference.json is the single repository source of the canonical ci block — task 1 creates it with four frozen records (backend and frontend on each of the two required run identities, all at the exact target SHA), and plans 09-07 and 09-08 copy that block through unchanged instead of inventing CI evidence, because none of the five client reads returns CI. It stays client-shaped for its whole life, because `fake-client.js` serves `snapshot.milestones` as the `listMilestones` envelope, `release-close.js` reads `snapshot.milestones.ok`, and the SAFE-04 helper reads `snapshot.release.ok`, so only the `target` and `ci` blocks are ever added to it and its two envelopes are never rewritten as lists
    - the two shapes are told apart by exactly one rule used by every helper in this phase, and the rule is the same literal everywhere, `Array.isArray(snapshot.milestones)`, where true means flat classifier evidence and false means a client-shaped snapshot whose `milestones` is an envelope object carrying `ok`. `evidenciaDe` in safe04.test.js and every later production builder discriminate on that single literal, because keying the discriminator on `target`/`ci` would misroute the client-shaped `reference.json` into the flat path the moment this task adds those two blocks to it
    - the target scoping runs partition, then compare, then validate, in that order and never in another — partition on `tagName` identity alone, compare `targetSha` values inside the partition, and only then validate against `expectedSha`. Ordering it this way is what keeps `fixtures/conflicting.json` at CONFLICTING, because its two records both name the requested version and their two distinct target SHAs are exactly the comparison signal a prefilter would have erased
    - the minimal snapshot pass-through this plan adds in release-close.js emits the target block, the ci block, and the full release and milestone lists so the CLI keeps classifying on the frozen baseline; plan 09-07 task 2 replaces that helper with the exported five-read evidence builder, so nothing here is thrown away
    - the six existing state fixtures plus reference.json carry the canonical green ci block, so the frozen six-state table from 09-02 keeps its exact one-to-one mapping after the CI allowlist lands
    - the completed no-op is spelled exactly as state code MISSING plus outcome COMPLETE_NOOP in this plan, and the same literal pair is asserted in 09-07 task 3, 09-08 task 3, and 09-09 task 3, so no consumer has to guess which surface carries the completed condition
    - classify.js keeps zero imports of eligibility.js, so the target filter and the CI allowlist are implemented locally rather than by reusing the eligibility vocabulary
  prohibitions:
    - no live GitHub, network, or subprocess call from any non-test source under tools/release-close/
    - no git ref or tag write path at porcelain, ref, or object level
    - no credential or authorization-header value read, passed as an argument, or emitted
    - no fabricated CI success synthesized from bare run identifiers, and no implicit default-green when CI evidence is absent
    - no ignored, swallowed, or redirected output path around the human-visible reviewed plan
    - no remote write of any kind before Phase 13; every domain mismatch keeps writeAction null
---

<objective>
Close VERIFICATION gap 2's pure half and gap 3's pure half inside the classifier: replace failure-only CI detection with an exact allowlist over canonical backend/frontend check evidence, define the snapshot contract the classifier requires, and make release/milestone classification target-scoped with full evidence retention.

Purpose: The classifier currently treats anything that is not literally `failure` as non-red, it compares every release in the snapshot regardless of tag, and it accepts a snapshot that carries no target and no CI evidence at all. The CLI then supplies a fabricated green state from bare run IDs. A fail-closed contract cannot rest on absence of red, and a later plan cannot feed a contract whose required fields have no defined source in the repository.
Output: A fail-closed snapshot contract (required `target` and `ci` blocks plus three evidence lists) with an exact CI allowlist over frozen backend/frontend records, eleven blocking CI families (ten conclusion or status families plus CI-CONTRADICTORY), two new versioned fixtures, the migrated canonical contract in the six existing state fixtures and in `reference.json` in the shape each one declares, a minimal production pass-through so the CLI keeps working on the frozen baseline, and an exact-code test matrix.

Task order is chosen so every task is a green increment on its own: task 1 makes the snapshot contract canonical in production and in every existing fixture in its declared shape, task 2 consumes the `target` block that task 1 froze, and task 3 only adds assertions. No task leaves a caller, a fixture, or a test unmigrated behind it.

Planner assumptions (deterministic edge probe, resolved here):
- OPS-02 concurrency: repeated classification of the concurrent fixture evidence is deterministic across identical runs and never proposes a write. Real cross-process arbitration remains Phase 11 REC-05 and is NOT claimed here. Resolved by task 3.
- SAFE-02 unclassified edge: an unclassified or malformed CI outcome is normalized to its own family code and is never accepted as eligibility. Resolved by task 1 and asserted in task 3.
</objective>

<execution_context>
@/home/lucas/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/lucas/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-release-close-contract-fixtures/09-CONTEXT.md
@.planning/phases/09-release-close-contract-fixtures/09-VERIFICATION.md
@.github/workflows/ci.yml
@tools/release-close/classify.js
@tools/release-close/classify.test.js
@tools/release-close/safe04.test.js
@tools/release-close/fixtures/reference.json
@tools/release-close/fixtures/missing.json
@tools/release-close/fixtures/failed.json
@tools/release-close/release-close.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Canonical snapshot contract (required target, ci, and evidence lists) plus the exact backend/frontend CI success allowlist, migrated in production and in every existing fixture in the shape that fixture declares</name>
  <files>tools/release-close/classify.js, tools/release-close/release-close.js, tools/release-close/safe04.test.js, tools/release-close/fixtures/reference.json, tools/release-close/fixtures/missing.json, tools/release-close/fixtures/partial.json, tools/release-close/fixtures/duplicate.json, tools/release-close/fixtures/conflicting.json, tools/release-close/fixtures/failed.json, tools/release-close/fixtures/concurrent.json, tools/release-close/classify.test.js</files>
  <read_first>tools/release-close/classify.js lines 41-50 and 68-78 (the failure-only CI check and the precedence chain it feeds); .github/workflows/ci.yml jobs backend and frontend (the two canonical job names and the required status-check context names the allowlist must match); tools/release-close/fixtures/reference.json (the only fixture the CLI loads, which today carries just the bare runs array, is client-shaped — its `release` and `milestones` values are envelopes that `fake-client.js` serves verbatim and that `release-close.js` and the SAFE-04 helper read as envelopes — and therefore may gain only the `target` and `ci` blocks from this task); tools/release-close/fake-client.js lines 103-128 (the five read implementations, which serve `snapshot.release` and `snapshot.milestones` unchanged, so those two keys can never hold a flat list in a client-shaped fixture); tools/release-close/release-close.js lines 151-171 and 181-228 (the two call sites that build classification evidence, the fabricated green CI object that must go, and the plan builder that calls the same helper); tools/release-close/safe04.test.js lines 84-129 (the local evidence helper, its shape discriminator, and the client-snapshot builder whose output must satisfy the new contract after this task) and lines 175-187 (the all-fixture assertion, whose `reference` row is the one direct-classifier case that has to move); tools/release-close/classify.test.js lines 95-100 (the write-action loop that currently feeds the client-shaped `reference.json` straight into the classifier and therefore cannot survive the five-key contract); the six state fixtures under tools/release-close/fixtures/ (each currently carries a hand-written checks object of state plus two bare run numbers, which is exactly the shape that must stop being authoritative, and each is flat evidence, not a client snapshot); .planning/phases/09-release-close-contract-fixtures/09-VERIFICATION.md gap 2 (cancelled, pending, timed-out, and missing evidence all reaching MISSING and therefore an apply-ready plan)</read_first>
  <behavior>
    - case: canonical evidence with one completed successful backend and one completed successful frontend record at the exact target SHA on both required run identities -> CI unblocking, classification continues to the state rules
    - case: no CI block present in the snapshot -> TypeError on entry, so a caller that drops the block can never reach a decision
    - case: no target block present in the snapshot -> TypeError on entry with a PT-BR message
    - case: evidence array empty -> FAILED with family CI-MISSING
    - case: a required job record absent for one of the two required runs -> FAILED with family CI-MISSING
    - case: a record whose status is not completed -> FAILED with family CI-PENDING
    - case: a record whose conclusion is cancelled -> FAILED with family CI-CANCELLED
    - case: a record whose conclusion is timed_out -> FAILED with family CI-TIMED-OUT
    - case: a record whose conclusion is action_required -> FAILED with family CI-ACTION-REQUIRED
    - case: a record whose conclusion is neutral or skipped -> FAILED with family CI-NEUTRAL
    - case: a record whose conclusion or job name is an unrecognized string -> FAILED with family CI-UNKNOWN
    - case: a record whose headSha is not a full 40-hex, or differs from the target SHA, or abbreviates it -> FAILED with family CI-WRONG-SHA
    - case: a record carrying a run identifier outside the two required identities -> FAILED with family CI-WRONG-RUN
    - case: two records for the same run and job with different conclusions -> FAILED with family CI-CONTRADICTORY
    - case: a record that is not a plain record, or a missing required field, or a non-numeric run identifier -> FAILED with family CI-MALFORMED
    - case: every one of the above blocking cases -> FAILED, eligible false, writeAction null, PT-BR reason naming the family, and that family in the EN field ciCode
    - case: the client-shaped reference.json passed straight into the classifier -> TypeError, because its `milestones` value is an envelope and not a list; the client shape is never a classifier input
    - case: the reference baseline normalized through the production evidence path instead -> code MISSING, eligible false, writeAction null, a non-empty PT-BR reason, and the plan still reporting applyLiberado true because MISSING is not one of the blocking codes
    - case: the CLI running verify and plan over the frozen reference fixture -> both still exit zero, the reference ci block is copied through, and the plan still reports applyLiberado true with bloqueio null
  </behavior>
  <action>
Define the canonical snapshot contract in `classify.js`, freeze it in every existing fixture, and give production a minimal pass-through that supplies it, in one atomic green increment. This task ends with the whole tool suite green and the CLI still exiting zero on the frozen baseline, so the next task starts from a passing suite.

The snapshot contract becomes five required keys. `target` is an object carrying `version` and `expectedSha`, where `expectedSha` must match the full 40-hex lowercase pattern. `ci` is an object carrying `targetSha` as a full 40-hex lowercase string, `event` equal to `push`, `requiredRunIds` as an array of exactly two numeric run identifiers, and `records` as an array of check records where each record has `runId`, `job`, `headSha`, `status`, and `conclusion`. `releases`, `milestones`, and `closeMarkers` are arrays. A missing or malformed `target` or `ci` throws a TypeError with a PT-BR message, because an absent CI block is a contract violation and must never be read as a green run; a non-array value for any of the three lists also throws. `throw` stays reserved for invalid caller input exactly as D-11 requires, so every domain mismatch is still returned as data. The two allowed job names are exactly `backend` and `frontend`, taken from the canonical job names in `.github/workflows/ci.yml`.

Replace the failure-only `checksFailed` helper with an allowlist evaluator. CI is unblocking only when, for every required run identifier, there is exactly one `backend` record and exactly one `frontend` record whose `headSha` is byte-identical to `ci.targetSha`, whose `status` is `completed`, and whose `conclusion` is `success`. Anything else blocks. When it blocks, return state code `FAILED` with `writeAction` null, a PT-BR reason naming the concrete family, and a new stable EN field `ciCode` carrying one of CI-MISSING, CI-PENDING, CI-CANCELLED, CI-TIMED-OUT, CI-ACTION-REQUIRED, CI-NEUTRAL, CI-UNKNOWN, CI-WRONG-SHA, CI-WRONG-RUN, CI-CONTRADICTORY, CI-MALFORMED. Evaluation order within a snapshot is deterministic: malformed first, then missing, then wrong-run, then wrong-SHA, then per-family conclusion/status, then contradictory, so the same evidence always yields the same family.

In `reference.json`, add the canonical `ci` block as the repository's single source of CI evidence, and add the `target` block, and change nothing else in that file. `targetSha` equals the `expectedSha` the file already carries, `event` equals `push`, `requiredRunIds` equals the two numeric run identifiers already in its `runs` array, and there are exactly four records — one `backend` and one `frontend` record for each required run, every record carrying that same full SHA, `status` equal to `completed`, and `conclusion` equal to `success`. Derive every value from a field the file already carries: do not invent a run identifier, a SHA, or a job name. `target` mirrors the file's existing `version` and `expectedSha`. This is the only file in the repository a later plan may read CI evidence from, and none of the five client reads returns CI, so plan 09-07 must copy this block through rather than reconstruct one. Critically, `reference.json` is a client-shaped snapshot and stays one for its whole life: its `release` and `milestones` values are the envelopes `makeFakeClient` serves verbatim to `getReleaseByTag` and `listMilestones` and that `release-close.js` and the SAFE-04 helper read through `.ok`. Do not add `releases` or `closeMarkers` to it, do not rename `release`, and do not turn `milestones` into a list. The same key cannot be an envelope in this file and a list in the flat fixtures, and a fixture that tries to be both is a fixture no helper in this repository can classify.

In the six state fixtures `missing.json`, `partial.json`, `duplicate.json`, `conflicting.json`, `failed.json`, and `concurrent.json`, these six are flat classifier evidence, so replace the hand-written `checks` object with the same canonical `ci` block shape, add the `target` object, and normalize each fixture to the full five-key contract: move the existing singular `release` and `milestone` values into the `releases` and `milestones` lists without changing their contents, keep every existing `closeMarkers` and `eligibility` value, and keep the existing `version`, `expectedSha`, `tagSha`, `commitSha`, `frozenAt`, and `runs` fields. Use the frozen run identities and the frozen commit SHA that already appear in those files, so the baseline stays single-sourced (D-08). In `failed.json` keep the `ci` block unblocking and represent the red signal explicitly: add a `failedRunIds` list naming the run that is red, taken from the `failingRuns` value already in that file, and have the state evaluator read that list as the explicit FAILED trigger. These six fixture files form one deliberately serialized fixture group: they are the frozen six-state contract and must change in the same increment as the classifier, otherwise the suite goes red mid-plan.

In `release-close.js`, make the existing snapshot evidence helper satisfy the new contract with the smallest change that keeps the CLI honest. Give it the resolved target as a parameter, emit `target` from that parameter, emit `ci` by copying `snapshot.ci` through unchanged, and stop discarding evidence by emitting `releases` as a list and `milestones` as the full list — note that the list it emits is a different value from the envelope it reads, and the helper is the one place in the module where that translation is allowed to happen. Delete the fabricated `checks: { state: 'success' }` object, because that literal is the spoofing vector this gap is about. Pass the resolved target from both call sites, in the decision function and in the plan builder, so a `--version` or `--sha` override flows into the contract instead of being ignored; note that an override which no longer matches the frozen `ci.targetSha` then classifies as FAILED with family CI-WRONG-SHA, which is the correct fail-closed answer. Export this helper as well, so plan 09-07 task 2 can replace the exported symbol with the five-read builder in place, but do not import `release-close.js` from any test in this task: until plan 09-07 task 1 makes the entrypoint import-safe, importing the module still executes `main` and calls `process.exit`, which would truncate the test run. Keep the helper in place: plan 09-07 task 2 replaces it with the exported five-read evidence builder, and until then it is the only thing standing between the CLI and a red suite.

In `safe04.test.js`, update the two local helpers so the suite keeps passing. The local evidence helper must emit `target`, the copied `ci` block, and the full lists from the client-shaped snapshot, and its shape discriminator must change with the contract on exactly one key: it currently decides client-shaped versus flat by testing for a `milestones` field and the absence of `checks`, and both of those keys move in this migration — `checks` disappears everywhere and `reference.json` keeps its `milestones` envelope while the state fixtures turn the same key into a list — so discriminate on `Array.isArray(snapshot.milestones)` alone, returning the object unchanged when that holds and normalizing through the client-shaped path when it does not. Never key the discriminator on `target` or `ci`: this task adds both blocks to `reference.json`, so a discriminator that treats their presence as the flat marker would misroute the client baseline into the flat path and hand the classifier an object with no `releases` and no `closeMarkers`. The client-snapshot helper must read the migrated `releases` and `milestones` arrays instead of the removed singular `release` and `milestone` fields, so each state fixture still yields a client-shaped snapshot the fake can serve. In the same file, upgrade the reference-baseline row of the all-fixture loop to assert the exact expected code `MISSING` with `eligible` false, `writeAction` null, and a non-empty PT-BR reason, and leave the other six rows asserting only that a code is a string: those six flow through a local helper that still collapses duplicate and conflict arrays, and asserting exact codes for them here would fail until plan 09-08 task 3 deletes that helper for the production builder. Both local helpers are temporary: 09-08 task 3 deletes them, and this task must not try to rewrite the rest of that suite.

In `classify.test.js`, extend the existing six-state table with the negative CI cases in the behavior block, built by mutating a deep clone of the canonical block taken from `fixtures/missing.json`, and assert the exact `ciCode` for each. Add the two TypeError cases for a missing `target` and a missing `ci` block. Keep the existing six-state rows, the module-independence test, the scripted-409 ordering test, the five-read call-log test, the invalid-input TypeError test, and the concurrent determinism test exactly as they are; they must keep passing unchanged, which is what proves the migration preserved the frozen table. The write-action loop is the one existing case that cannot stay unchanged, and migrate it deliberately: it currently iterates the six state fixtures plus `reference`, which can no longer be right, because `reference.json` is client-shaped and is therefore not a classifier input. Narrow the loop to the six flat state fixtures and rename the test to say so. In its place add one case asserting that `classifySnapshot(fixture('reference'))` throws a TypeError, which is the executable statement that the client shape is never a classifier input and that keeps the reference baseline covered from this file. The reference baseline's exact decision — code `MISSING`, `eligible` false, `writeAction` null, non-empty PT-BR reason — belongs to `safe04.test.js`, where the fixture already reaches the classifier through the normalized path, and it is added there in this same task. Do not import `release-close.js` into this test to route that row through the production builder, because the module is not import-safe until plan 09-07 task 1; plan 09-07 task 3 and 09-08 task 3 are where the reference baseline is driven through the exported production builder, and by then neither of them asserts anything about a local copy.

Do not keep the legacy `checks` object as a fallback input, and do not default an absent or unrecognized CI block to green. Do not add an import of `eligibility.js` to reach a shared status vocabulary: the existing independence test forbids it, so implement the CI families locally. Do not change the six state code strings or add a seventh state code; FAILED is the state, the family lives in `ciCode`. Do not add the three evidence lists to `reference.json`, do not convert its envelopes into lists, and do not give any client-shaped fixture a flat `releases` or `closeMarkers` key; the two shapes are declared here and every acceptance instruction below is written against that declaration.

The fixture group and its two shared consumers are explicitly serialized here as a justified exception to the five-file task limit: the six fixture files, the CLI evidence helper, the SAFE-04 helper, and `classify.js` are one atomic green unit, and splitting them across tasks would leave the tool suite red between tasks, which is exactly the failure mode this revision removes. Tasks 2 and 3 each stay inside the limit.
  </action>
  <verify>
    <automated>node --test tools/release-close/classify.test.js && node --test "tools/release-close/*.test.js" && node tools/release-close/release-close.js verify --json && node tools/release-close/release-close.js plan --json</automated>
    <fails_when>any command exits non-zero, or the plan JSON stops reporting applyLiberado true with bloqueio null on the frozen baseline, or any legacy `checks` object is still authoritative, or a non-green or absent CI block leaves the classifier unblocking, or any of the six state fixtures stops mapping to its own state code, or reference.json lacks the canonical ci block with one backend and one frontend record per required run at the exact target SHA, or reference.json loses either of its two envelopes or gains a releases or closeMarkers key, or a client-shaped snapshot is ever accepted by the classifier, or a flat state fixture is ever routed through the client-shaped normalization path</fails_when>
  </verify>
  <done>The classifier requires the five-key snapshot contract, only a complete exact-target both-job both-run success allowlist leaves CI unblocking, every other outcome blocks with a named family, every existing fixture carries the frozen canonical contract in the shape it declares — the six state fixtures as flat evidence with the three lists, reference.json as a client-shaped baseline with its release and milestones envelopes intact — and the CLI still exits zero on the frozen baseline.</done>
  <reversibility rating="costly">The canonical `ci` evidence shape and the five-key snapshot contract are what plan 09-07 copies into the production evidence builder and what Phase 10 fills from real runs and Phase 12 documents in the runbook; reshaping them later invalidates the migrated fixtures, this plan's suite, and both downstream phases.</reversibility>
  <acceptance_criteria>Source asserts classify.js has no fallback that treats an absent or unrecognized CI block as unblocking, asserts a missing target or ci block throws a TypeError, asserts a client-shaped snapshot is rejected because milestones is not a list, asserts the job allowlist is exactly backend and frontend, and asserts each blocking family maps to one EN ciCode. Behavior asserts each case above returns FAILED with the expected ciCode and writeAction null, that the canonical green block leaves CI unblocking, that the two missing-block cases throw, and that the reference baseline normalizes to code MISSING with writeAction null while the plan still reports applyLiberado true. Fixture asserts the six state fixtures carry the target block, the canonical ci block, and the three evidence lists while still returning their own state code, and asserts reference.json keeps both envelopes, gains no releases or closeMarkers key, and holds four records covering both jobs on both required runs at the exact target SHA. CLI asserts verify and plan still exit zero on the frozen baseline with applyLiberado true. Test asserts the SAFE-04 local helpers discriminate the two shapes on Array.isArray of milestones alone and read the migrated releases and milestones arrays, that the reference-baseline row asserts the exact code MISSING, and that classify.test.js asserts the client shape throws instead of being fed to the classifier.</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Target-scoped Release and Milestone evidence with full retention and an explicit completed no-op, with the new scenarios and their assertions in this same task</name>
  <files>tools/release-close/classify.js, tools/release-close/classify.test.js, tools/release-close/fixtures/complete.json, tools/release-close/fixtures/unrelated.json</files>
  <read_first>tools/release-close/classify.js as rewritten by task 1 (the five-key contract validation and the CI allowlist, whose precedence this task keeps in front of the target rules); tools/release-close/fixtures/duplicate.json and conflicting.json (two records each naming the requested version `v0.1.1` — identical target SHAs in the first, two materially different target SHAs in the second — the exact cases partition-then-compare must preserve, now in their migrated flat shape); tools/release-close/classify.test.js (the six-state table and the CI matrix task 1 left in place, whose existing call sites must keep passing while the new surfaces are added); .planning/phases/09-release-close-contract-fixtures/09-REVIEW.md WR-06 and WR-07 (a completed target reported as PARTIAL, and two unrelated tags reported as CONFLICTING while the reason text says "da mesma tag")</read_first>
  <behavior>
    - case: two identical releases whose tagName equals the requested version and whose targetSha equals the expected target -> DUPLICATE, both stable ids retained in the decision
    - case: two releases for the requested version with different targetSha values -> CONFLICTING, both stable ids retained
    - case: the frozen fixtures/conflicting.json through the target version partition -> CONFLICTING, because both records name the requested version and their two distinct target SHAs are the comparison signal, so the divergent record is reported rather than filtered away
    - case: one release for the requested version whose targetSha diverges from expectedSha -> the record still enters the partition, the decision reports targetShaValidates false and names the divergence in the PT-BR reason, and the completed no-op is not earned
    - case: two releases for two other versions, none matching the requested version -> they are excluded from the target decision entirely and never appear in the reason
    - case: one release for another version plus one matching release for the requested version -> classification is decided only by the matching record
    - case: two Milestone records both naming the requested version -> both retained, decision reflects the in-progress pair
    - case: one Milestone record naming another version -> excluded from the target decision
    - case: a published release for the requested version plus a closed Milestone for the requested version with no open issues -> code MISSING with outcome COMPLETE_NOOP, a non-blocking decision, writeAction null
    - case: a draft release for the requested version -> PARTIAL, with outcome absent
    - case: an open Milestone with at least one open issue for the requested version -> PARTIAL, with outcome absent
    - case: the CLI running verify and plan over the frozen reference fixture after this task -> both still exit zero, because the target block and the full lists the classifier now reads are already supplied by task 1
  </behavior>
  <action>
Make classification target-scoped and evidence-complete in `classify.js`, and land the new versioned scenarios together with the assertions that consume them, so this task is green on its own.

Read the `target` block that task 1 froze and validate it on entry with the same full 40-hex strictness used for a target SHA, throwing a TypeError with a PT-BR message only for a malformed caller input. Then do exactly three things, always in this order — partition, then compare, then validate — and never in another order. The order is the contract: the one thing this change is fixing is that a target-SHA equality test used to act as a membership test, so a same-version record with a divergent SHA was filtered out of the decision instead of being reported as a conflict.

Partition, on version identity alone. A Release record enters the target version partition exactly when its `tagName` is byte-identical to `target.version`. That is the only membership rule, and it never consults `expectedSha` and never consults `targetSha`. Every other record, and every non-record value, goes to the retained-unrelated field. Partitioning first is what keeps a same-version record with a divergent target SHA in the decision, and it is exactly the shape `fixtures/conflicting.json` holds.

Compare, inside the partition. Only when the partition holds at least two records: collect the distinct `targetSha` values, counting a record that omits `targetSha` as its own distinct value rather than as a wildcard. Exactly one distinct value is DUPLICATE; two or more materially different values is CONFLICTING. `fixtures/duplicate.json` reaches DUPLICATE because both records name `v0.1.1` and both carry the expected SHA; `fixtures/conflicting.json` reaches CONFLICTING because both records name `v0.1.1` and their two target SHAs are materially different. When the partition holds no record at all, the retained-unrelated records must not reach the reason text, so an operator is never told two unrelated tags conflict.

Validate, as a separate decision input. The decision also carries `targetShaValidates`, true when every record in the partition carries a `targetSha` byte-identical to `target.expectedSha`. A false value never removes a record from the partition, never changes the state code the compare step reached, and never suppresses a conflict; it names the divergence in the PT-BR reason and it withholds the completed no-op. This is the only place `expectedSha` is consulted, and it is consulted after the decision is reached rather than before the records are chosen.

Retain every partitioned record's stable identifier (`id`) and its tag and target fields in the returned decision, sorted by identifier so the output is deterministic, so a downstream consumer in Phase 11 can address each record without re-reading — including the record whose target SHA diverges, which is precisely the one a prefilter would have dropped. Never let a divergent record leave the decision as a mere absence; an operator has to be able to see both SHAs side by side.

Apply the same version partition to Milestone evidence: keep the full `milestones` array, partition by a title or label that names the requested version, retain every partitioned Milestone's `number` and `state`, and use the count of partitioned in-progress markers for the existing concurrency rule.

Represent the completed no-op with one exact, unambiguous surface, and write that surface down here because three later plans assert it. When the target version partition holds exactly one release, that release is not a draft, `targetShaValidates` is true, and exactly one partitioned Milestone is present, is closed, and carries zero open issues, the decision carries state code `MISSING` — a member of the six locked codes that this plan never renames and never adds to — plus a separate field `outcome` with the exact literal `COMPLETE_NOOP`, plus `writeAction` null, plus a PT-BR reason stating the target is already closed and that this run is a no-op. `MISSING` is the taxonomy's terminal non-blocking state and the reason is the human signal; `PARTIAL` keeps its own meaning of work still remaining and is never returned for a completed target. No seventh state code exists: `COMPLETE_NOOP` lives in `outcome`, never in `code`. A single partitioned release whose `targetSha` diverges from `expectedSha` does not earn the no-op: it is PARTIAL with a reason naming the divergence, because the target the operator asked to close is not the one the evidence describes, and that is the validate step's only power over the state code. State the blocking answer explicitly for every consumer: `MISSING` is not in the blocking list, so the completed no-op is non-blocking, and the plan's eight steps for that target are marked adopt-or-read rather than create, so no consumer has to infer non-blockingness from an absence.

Add `fixtures/complete.json`, a frozen state fixture carrying the same five-key contract as the six existing ones, with the canonical unblocking `ci` block, a published non-draft Release for the requested version at the expected target, and one closed Milestone for the requested version with zero open issues. Add `fixtures/unrelated.json`, a frozen state fixture with the same contract and unblocking `ci` block, two Releases belonging to two other versions, and one Milestone belonging to another version, so target scoping has a versioned scenario rather than only inline literals. Derive both from the frozen version, commit SHA, and run identities the existing fixtures already carry (D-08); do not invent a SHA, a run identifier, or a job name.

In `classify.test.js`, add the eleven behavior cases above in this same task, driving the two new fixtures and the frozen `duplicate.json` and `conflicting.json`, so no call site is left unmigrated behind the change: exact `code` plus the exact `outcome` for every row, the exact `targetShaValidates` value for every row, the retained identifier lists present, the unrelated records present in the retained-unrelated field and absent from any conflict reason, and `writeAction` null everywhere. The two rows that pin this task's contract are `conflicting.json` reaching CONFLICTING with both ids retained and the single divergent-SHA row reporting `targetShaValidates` false; assert both by exact value, not by substring. Keep every row task 1 added, and keep the existing six-state table unchanged, so the frozen mapping from 09-02 is still provable from this suite.

Do not filter evidence destructively: the retained unrelated set must still be returned in the decision under a clearly named field so the operator can see what was ignored, and a record excluded by target-SHA divergence must not be quietly moved into that field either, because a divergence is a reported comparison result rather than an unrelated version. Do not let the `expectedSha` validation run before the partition is built, and do not let it remove a record, change the state code, or suppress a conflict. Do not resolve an ambiguity by choosing a record; ambiguity is reported, never adopted. Do not let a completed target return a blocking state, and do not let a partial target return the `COMPLETE_NOOP` outcome.
  </action>
  <verify>
    <automated>node --test tools/release-close/classify.test.js && node --test "tools/release-close/*.test.js" && node tools/release-close/release-close.js plan --json</automated>
    <fails_when>any command exits non-zero, or an unrelated-version record appears in a duplicate or conflict reason, or any partitioned record identifier is dropped from the decision, or fixtures/conflicting.json reports PARTIAL or MISSING instead of CONFLICTING, or a record whose targetSha diverges from expectedSha is absent from the partition instead of being reported through targetShaValidates false, or a published release plus a closed Milestone returns PARTIAL or omits outcome COMPLETE_NOOP, or a draft or open Milestone returns the completed no-op, or the plan JSON stops reporting applyLiberado true on the frozen baseline</fails_when>
  </verify>
  <done>Duplicate, conflict, partial, and completed decisions are scoped by the requested version partition, with target-SHA equality read as the duplicate-or-conflict signal inside that partition and `expectedSha` validation reported separately as `targetShaValidates` rather than applied as a filter; every partitioned Release and Milestone identifier is retained, unrelated history is excluded from the target decision but still reported, and a finished target reports MISSING with outcome COMPLETE_NOOP as an explicit non-blocking no-op.</done>
  <reversibility rating="costly">The target-scoped evidence shape, the retained identifier lists, and the MISSING plus COMPLETE_NOOP pair become the reconciliation input contract in Phase 11 and are asserted literally by plans 09-07, 09-08, and 09-09; changing them later invalidates both new fixtures and every downstream assertion.</reversibility>
  <acceptance_criteria>Source asserts classify.js reads the target block, builds the release and milestone partitions from `tagName` identity alone with no `expectedSha` term in the membership test, compares `targetSha` values inside the partition, reports `targetShaValidates` as a separate field consulted after the decision, never selects the first array element, and never returns a seventh state code. Behavior asserts the eleven cases above by exact value, including that the frozen conflicting.json returns code CONFLICTING with both ids retained, that a single divergent-SHA record stays in the partition with targetShaValidates false and the divergence named in the reason, that the completed case returns code MISSING with the exact outcome literal COMPLETE_NOOP and writeAction null, and that a draft or open Milestone returns PARTIAL with outcome absent. Fixture asserts complete.json and unrelated.json are valid JSON carrying the five-key contract, the canonical ci block, and versioned records, and asserts the plan JSON still reports applyLiberado true on the frozen baseline.</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Full exact-code matrix over every fixture, every CI family, and the deterministic repeat guarantee</name>
  <files>tools/release-close/classify.test.js</files>
  <read_first>tools/release-close/classify.test.js as it stands (the six-state table, the module-independence test that forbids importing eligibility.js, the scripted call-log ordering test, and the concurrency determinism test that must keep passing); tools/release-close/classify.js as rewritten by tasks 1 and 2 (the exact EN family codes and outcome fields the matrix must assert); tools/release-close/fixtures/complete.json and unrelated.json (the two new versioned scenarios)</read_first>
  <action>
Complete the classifier proof in `classify.test.js` only — this task changes no production source and no fixture.

Replace the loose "the code is a string" style assertion anywhere it appears in this file with an exact expected value. Build one table mapping every flat evidence fixture file to its exact expected decision surface: the six original state fixtures to their own state code; `complete.json` to code `MISSING` with the exact `outcome` literal `COMPLETE_NOOP`; `unrelated.json` to code `MISSING` with `outcome` absent, the unrelated records present in the retained-unrelated field, and those records absent from any conflict reason. The client-shaped `reference.json` is not a row in that table and must not be added to it: its coverage from this file is the TypeError case task 1 added, because the client shape is not classifier input. Assert for every row that writeAction is null and that the reason is a non-empty PT-BR string, and assert that `complete.json` and `unrelated.json` are distinguished only by the `outcome` field, so a consumer can never confuse the finished target with the target that has nothing yet.

Add the CI matrix: derive a canonical green CI block once from the frozen `ci` object in `fixtures/missing.json`, then build each negative family by mutating a deep clone of that block, and assert the exact `ciCode` for each of the eleven blocking families — CI-MISSING, CI-PENDING, CI-CANCELLED, CI-TIMED-OUT, CI-ACTION-REQUIRED, CI-NEUTRAL, CI-UNKNOWN, CI-WRONG-SHA, CI-WRONG-RUN, CI-CONTRADICTORY, CI-MALFORMED — plus the empty-records case. The absent-block case is already covered by the TypeError assertion task 1 added, so do not re-assert it as a family here. Assert that at least one negative CI case forces the blocking state and that the blocking state is one of the six state codes the phase already defines, so no new state vocabulary sneaks in through the CI path.

Add the OPS-02 concurrency proof: classify `concurrent.json` twice, the second time from a deep clone, and assert the two decisions are deeply equal and that neither proposes a write. Add a short PT-BR comment on that test recording the boundary: determinism inside one process is proven here and cross-process arbitration is Phase 11 REC-05, so this suite must never be cited as a locking guarantee. Add a mutual-exclusion test asserting that two decisions produced from two different evidence objects in the same process share no mutable state, by classifying one, then asserting the other still returns its own expected code.

Avoid: adding a runner, assertion library, or any `dependencies` entry to `tools/release-close/package.json` (D-02, D-06). Avoid retyping the frozen SHAs and run identifiers as literals in the test; derive them from the fixtures so the versioned fixtures stay the single source of truth (D-08). Avoid weakening the existing module-independence test or the call-log ordering test to make a new assertion pass.
  </action>
  <verify>
    <automated>node --test tools/release-close/classify.test.js && node --test tools/release-close/</automated>
    <fails_when>either run exits non-zero, or any flat evidence fixture row asserts only a string type instead of the exact expected code, or the client-shaped reference fixture is ever added to that table, or any CI family lacks an exact ciCode assertion, or the two concurrent classifications differ, or the whole tool suite regresses</fails_when>
  </verify>
  <done>Every flat evidence fixture maps to an exact expected decision, the client-shaped baseline is proven to be rejected by the classifier rather than misread by it, every CI blocking family has an exact code assertion, concurrent classification is provably deterministic and write-free, and the full tool suite is green.</done>
  <acceptance_criteria>Test asserts eight flat evidence fixture rows with exact codes, with complete.json asserted as code MISSING plus the exact outcome literal COMPLETE_NOOP and unrelated.json asserted as code MISSING with outcome absent, eleven CI family rows with exact ciCode values, a row asserting the client-shaped reference fixture throws a TypeError instead of being classified, deeply-equal repeat classification of the concurrent fixture, and no shared mutable state between two decisions. Source asserts tools/release-close/package.json still declares no dependencies key and classify.js still imports no eligibility module.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI check evidence to the classifier | Untrusted, possibly absent or contradictory CI records cross here and must never be read as green |
| repository-wide Release and Milestone history to the classifier | Unrelated records cross here and must not decide a target that is not theirs |
| frozen fixture to the executable classifier | Fixture data is the only evidence source in this phase; it must be canonical rather than convenient |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-05-01 | Tampering | classify.js CI allowlist | high | mitigate | Unblocking requires a complete allowlist: both canonical jobs, both required run identities, exact full target SHA, completed status, success conclusion, no contradictions; each of the eleven families — missing, malformed, pending, cancelled, timed-out, action-required, neutral, unknown, wrong-run, wrong-SHA, contradictory — blocks with its own named code, and a missing target or ci block is a TypeError rather than a default |
| T-09-05-02 | Spoofing | synthesized green CI state | high | mitigate | The legacy state-plus-run-identifier object is deleted from every fixture and from the production evidence helper in task 1, no fallback input exists, an absent CI block is a thrown contract violation, and plan 09-07 deletes the whole helper in favour of the exported five-read builder that copies the same frozen block |
| T-09-05-03 | Tampering | target scoping of duplicate and conflict | high | mitigate | Membership is decided by requested version identity alone, so a same-version record with a divergent target SHA cannot be filtered out of the decision and the frozen `conflicting.json` still reports CONFLICTING with both identifiers visible; target-SHA divergence is the comparison signal that produces that conflict, `expectedSha` is validated afterwards as the reported `targetShaValidates` field and never as a prefilter, and unrelated records never reach a target reason while every partitioned identifier is retained so nothing is silently dropped |
| T-09-05-04 | Repudiation | completed no-op outcome | medium | mitigate | A published release plus a closed Milestone with zero open issues returns the exact non-blocking pair code MISSING plus outcome COMPLETE_NOOP with writeAction null and a PT-BR reason, instead of the ambiguous PARTIAL that a recovery tool would have treated as work to do; the pair is asserted literally by three later plans so no consumer has to infer it |
| T-09-05-05 | Information disclosure | retained unrelated records | low | mitigate | Retained unrelated records carry only tag names, target SHAs, and stable identifiers already public in the repository; no credential or environment value can enter a decision object |
| T-09-05-SC | Tampering | npm installs | high | mitigate | No install step exists in any task of this plan; the zero-dependency manifest and the node:test runner are unchanged |

</threat_model>

<verification>
`node --test tools/release-close/classify.test.js` green with the eight-row flat evidence table, the client-shape rejection row, and the eleven-row CI family table; `node --test "tools/release-close/*.test.js"` green with no regression in the eligibility, gate, no-write, or SAFE-04 suites; `node tools/release-close/release-close.js verify --json` and `plan --json` both still exit zero on the frozen baseline with `applyLiberado: true` and `bloqueio: null`; all nine fixture files parse as JSON and carry the canonical contract in the shape each one declares — the eight flat evidence files (`missing`, `partial`, `duplicate`, `conflicting`, `failed`, `concurrent`, `complete`, `unrelated`) with the target block, the canonical `ci` block and the three evidence lists, and the client-shaped `reference.json` with its `release` and `milestones` envelopes unchanged plus the same `target` and `ci` blocks; `git status` shows changes confined to `tools/release-close/classify.js`, `tools/release-close/classify.test.js`, `tools/release-close/release-close.js`, `tools/release-close/safe04.test.js`, and `tools/release-close/fixtures/`, with `.github/workflows/ci.yml` itself unmodified.
</verification>

<success_criteria>
The pure classifier is fail-closed and target-scoped: a snapshot without the five contract keys is a thrown violation rather than a guess, only exact, complete, both-job, both-run, exact-target success evidence leaves CI unblocking, every other outcome blocks with a named family, unrelated history cannot decide a target because membership is the requested version partition, a same-version record whose target SHA diverges is reported as a conflict instead of being filtered away, `expectedSha` is validated after the decision as a reported field rather than applied as a prefilter, all partitioned evidence identifiers are retained, a finished target reports the exact non-blocking pair MISSING plus COMPLETE_NOOP, the frozen canonical CI evidence has exactly one owner in the repository, every fixture declares its shape so no key ever carries two meanings and the one `Array.isArray(snapshot.milestones)` discriminator is the same literal everywhere, the CLI keeps working on the frozen baseline, and repeated classification is deterministic and write-free.
</success_criteria>

<output>
Create `.planning/phases/09-release-close-contract-fixtures/09-05-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Symbol | File | Consumed by |
|--------|------|-------------|
| `classifySnapshot` with the five-key snapshot contract, the canonical CI allowlist, the eleven blocking ciCode families, target scoping, and the MISSING plus COMPLETE_NOOP completed no-op | tools/release-close/classify.js (rewritten) | 09-07 production decision path, Phase 11 reconciliation, Phase 12 runbook |
| Frozen completed-no-op scenario reported as code MISSING plus outcome COMPLETE_NOOP | tools/release-close/fixtures/complete.json | 09-07 and 09-08 adapter suites, Phase 11 idempotent adoption, 09 re-verification |
| Frozen unrelated-history scenario | tools/release-close/fixtures/unrelated.json | 09-07 adapter suite, 09 re-verification, Phase 11 target scoping |
| Canonical CI evidence block and target block, the repository's single source for the production path, kept client-shaped with both envelopes untouched | tools/release-close/fixtures/reference.json (extended) | 09-07 production evidence builder, 09-08 reviewed-content fixture, Phase 10 preflight |
| Declared two-shape fixture contract: flat classifier evidence versus client-shaped snapshot, discriminated on the array shape of `milestones` alone | tools/release-close/fixtures/{missing,partial,duplicate,conflicting,failed,concurrent}.json plus the shape rule every helper applies | 09-05 and 09-07 suites, 09-07 production evidence builder, 09-08 reviewed.json |
| Canonical CI evidence block in the six state fixtures | tools/release-close/fixtures/{missing,partial,duplicate,conflicting,failed,concurrent}.json | 09-05 and 09-07 suites |
| Minimal snapshot pass-through that keeps the CLI honest until the five-read builder lands, exported so 09-07 can replace the symbol in place | tools/release-close/release-close.js (partially extended, superseded by 09-07) | 09-07 task 2 |
| Exact-code fixture and CI-family matrix plus concurrency determinism proof | tools/release-close/classify.test.js (rewritten) | 09-VERIFICATION re-run, OPS-02 traceability |
