---
phase: 09-release-close-contract-fixtures
plan: "06"
type: execute
wave: 4
depends_on:
  - 09-01
  - 09-02
coupling_justified:
  - "09-04: both plans run the shared tools/release-close/ suite while writing disjoint files; 09-06 reads the frozen fixtures through makeFakeClient and never rewrites them, and 09-04 changes only the eligibility predicate, so neither plan can observe the other's in-flight output."
  - "09-05: same shared suite and disjoint implementation files; the one shared input is the fixture set, which 09-05 rewrites and 09-06 only reads, and 09-05 keeps verify and plan green on the frozen baseline that this plan's own verify exercises, so either order is correct."
files_modified:
  - tools/release-close/client.js
  - tools/release-close/fake-client.js
  - tools/release-close/canary.test.js
autonomous: true
gap_closure: true
requirements:
  - SAFE-04
  - SAFE-02
estimate:
  tokens: 30000
  raw_tokens: 20000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - The read client contract is an exact capability surface: the five declared reads are present and any additional callable member is rejected before any decision runs
    - The programmable fake reports a measured mutation count produced by its own side-effect trap, never by a literal, and the count cannot be edited from outside
    - The legacy `writes` member survives only as a read-only, array-shaped accessor whose `length` always equals the closure tally, so the four existing `fake.writes.length` assertions in suites this plan does not own stay green without this plan taking ownership of their files
    - A single shared invariant refuses any decision whose measured mutation count is not exactly zero, naming the offending capability in PT-BR
    - A deliberately injected forbidden-write canary makes a scoped test run exit non-zero, so an escape cannot leave the suite green
    - The five-read ordering, the scripted failure vocabulary, and the per-instance call log keep working unchanged
    - The fail-closed gh seam still passes the exact-surface check and still throws naming Phase 10
  artifacts:
    - tools/release-close/client.js
    - tools/release-close/fake-client.js
    - tools/release-close/canary.test.js
  key_links:
    - release-close.js decide builds its client through makeFakeClient, so the measured counter it must report is produced at this boundary rather than in the renderer
    - canary.test.js exercises client.js and fake-client.js directly, so a weakened surface check shows up as a failing canary child run, not only as a text scan
    - gh-client.js is checked against the same exact surface, so Phase 10's real client inherits the constraint rather than inventing a new one
    - the fixture set this plan reads is owned by 09-05 task 1 (canonical ci and target blocks, with the six state fixtures flat and reference.json client-shaped); this plan rewrites no fixture, so the two same-wave edits cannot collide and the scripted-failure contract 09-09 depends on stays stable
    - the frozen `writes` accessor is the compatibility seam that keeps `tools/release-close/eligibility.test.js` and `tools/release-close/safe04.test.js` untouched by this plan: both read `fake.writes.length` after a run, and neither file is in this plan's declared scope, so the accessor must reproduce the array semantics those call sites already rely on rather than this plan silently breaking two suites it does not own
  prohibitions:
    - no live GitHub, network, or subprocess call from any non-test source under tools/release-close/
    - no git ref or tag write path at porcelain, ref, or object level
    - no credential or authorization-header value read, passed as an argument, or emitted
    - no fabricated CI success synthesized from bare run identifiers
    - no ignored, swallowed, or redirected output path around the human-visible reviewed plan
    - no remote write of any kind before Phase 13; the trap exists to measure an escape, never to perform one
---

<objective>
Close VERIFICATION gap 4 (zero mutation is a literal convention, not an enforced boundary) and the overlapping advisory CR-04: make the read-only capability surface exact, make the mutation counter a real measurement taken at the side-effect boundary, and prove the guard with a deliberately injected forbidden-write canary whose escape fails a scoped test run.

Purpose: `assertClientShape` accepts extra callable members, the fake's `writes` array has no producer, and every renderer emits a hard-coded zero. A probe added and invoked an extra write method on the fake and the reported mutation count stayed zero while the write executed. The CLI must report a measured number, and the number must be capable of being non-zero.
Output: An exact capability surface with a shared zero-mutation invariant, a programmable fake that measures and cannot be edited from outside, and a canary suite that proves an escape turns a test run red.
</objective>

<execution_context>
@/home/lucas/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/lucas/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-release-close-contract-fixtures/09-CONTEXT.md
@.planning/phases/09-release-close-contract-fixtures/09-VERIFICATION.md
@tools/release-close/client.js
@tools/release-close/fake-client.js
@tools/release-close/gh-client.js
@tools/release-close/nowrite.test.js
@tools/release-close/safe04.test.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Exact read-only capability surface plus a shared zero-mutation invariant</name>
  <files>tools/release-close/client.js</files>
  <read_first>tools/release-close/client.js lines 22-40 (the five declared reads and the required-methods-only check that silently tolerates anything else); tools/release-close/fake-client.js lines 100-127 (the client object with its call log, writes array, and mutations getter that must survive the stricter check); tools/release-close/gh-client.js lines 42-49 (the fail-closed stub that must keep passing); .planning/phases/09-release-close-contract-fixtures/09-REVIEW.md CR-04 (the reproduced escape: an extra write method ran successfully while the reported count stayed zero)</read_first>
  <behavior>
    - case: object with exactly the five declared reads and non-callable bookkeeping -> shape check accepts
    - case: object missing any of the five declared reads -> shape check rejects with a PT-BR reason naming the missing read
    - case: object with the five reads plus one extra callable member -> shape check rejects with a PT-BR reason naming the extra capability
    - case: object with a read whose value is not a function -> shape check rejects
    - case: non-object input -> shape check rejects
    - case: measured count of zero -> invariant accepts and returns zero
    - case: measured count of one with a named capability -> invariant refuses with a PT-BR reason naming that capability and the count
    - case: measured value that is not a non-negative integer -> invariant refuses
    - case: the fail-closed gh stub -> shape check accepts, unchanged behavior
  </behavior>
  <action>
Turn the client contract from a required-methods check into an exact capability surface in `client.js`.

Keep `READ_METHODS` as the single declared surface, unchanged in content and order. Change `assertClientShape` so that after confirming the five reads are present and callable it also enumerates the object's own enumerable members and refuses any member that is a function and is not one of the five declared reads, throwing a TypeError whose PT-BR message names the offending capability and states that the release-close client is read-only. Non-callable own members must remain permitted so the programmable fake can expose its call log and its counter, and inherited or non-enumerable members must not be treated as capabilities, so the check stays about what a caller can actually invoke on the returned object. A read whose value is not a function, and a non-object input, keep refusing exactly as they do today.

Add a shared invariant in the same module: a function that takes a measured record exposing a non-negative integer mutation count plus an optional list of observed side effects, returns the count when it is exactly zero, and otherwise throws a TypeError whose PT-BR message states that a remote side effect escaped the read-only contract, names the observed capability when one is available, and states the count. Refuse a measured value that is not a non-negative integer, so a missing or corrupt measurement is treated as a failure of the contract rather than as a pass. Do not give this invariant any way to reset, forgive, or subtract: a count of anything other than zero must refuse.

Do not add a sixth read method, and do not let the extra-member rejection inspect nested objects. Do not add a `capabilities` flag argument that lets a caller opt out of the exact check; the whole point is that the surface cannot be widened by a caller. Do not move the check into the fake, and do not make the fake responsible for policing itself.

Avoid: a text-based denylist of method names. The existing static scan stays as a complementary check, but this module enforces the surface structurally so a differently named or array-shaped write capability cannot slip through.
  </action>
  <verify>
    <automated>node --test tools/release-close/</automated>
    <fails_when>the run exits non-zero, or an object carrying an extra callable member passes the shape check, or the shared invariant accepts a count other than zero, or the gh stub stops satisfying the shape check</fails_when>
  </verify>
  <done>A client with any callable member beyond the five declared reads is rejected with a PT-BR reason naming the capability, and the shared invariant refuses any measured count other than zero.</done>
  <reversibility rating="costly">This is the exact client surface the Phase 10 live client and the Phase 11 reconciliation writes must satisfy; widening or loosening it later would re-open the escape the canary exists to catch.</reversibility>
  <acceptance_criteria>Source asserts assertClientShape enumerates own members and rejects extra callables by name, asserts the shared invariant compares against exactly zero with no forgiveness path, and asserts READ_METHODS still lists exactly five names in the original order. Behavior asserts the nine cases above, including that the gh stub is still accepted.</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Programmable fake with a real side-effect trap and an uneditable measured counter</name>
  <files>tools/release-close/fake-client.js</files>
  <read_first>tools/release-close/fake-client.js lines 43-128 (the failure-plan normalizer, the per-instance call log, the writes array with no producer, the mutations getter, the five read implementations, and the trailing shape check); tools/release-close/client.js as rewritten by task 1 (the exact surface rule and the shared invariant this module must feed); tools/release-close/classify.test.js lines 44-72 (the two existing tests that depend on scripted outcomes and on exact call-log ordering, which must keep passing); tools/release-close/eligibility.test.js lines 29 and 135 and tools/release-close/safe04.test.js lines 181 and 228 (the four `fake.writes.length` assertions that live in files outside this plan's declared scope and that the compatibility accessor defined below has to keep green untouched)</read_first>
  <action>
Make the fake's mutation count a real measurement in `fake-client.js`.

Keep the scripted failure vocabulary, the per-instance call log, the call ordering, and the five read implementations exactly as they are: six scripted outcomes, numeric sequence numbers starting at one, and a fallback to the snapshot once a method's queue is drained. This task changes what the counter means, not what the reads do.

Give the fake a real side-effect trap. Expose a single recording function that any future side effect must pass through, which appends the capability name and the call arguments to the internal record and increments an internal tally. The measured count is that tally, not the length of a public array. Close the client surface after construction by freezing the returned object, so attaching an untracked capability directly to the instance fails in strict mode, which ESM modules always are; the freeze must happen after the five reads, the call log, the counter accessor, and the trap accessor exist, so nothing that exists today stops working.

Make the counter uneditable from outside. The tally and the record live in a closure that the module never exposes; the client exposes only a read-only accessor for the count and a read-only accessor for a frozen copy of the observed side effects, so neither the count nor the record can be rewritten to look clean after the fact. The shape check from task 1 still runs on the frozen client, and the non-callable bookkeeping members it permits are exactly the accessors this task defines.

Preserve one named compatibility accessor, because this change invalidates call sites in two suites this plan does not own. The public mutable `writes` array is replaced by a read-only accessor on the frozen client that returns a frozen array of the observed side-effect records. That keeps the array-like observable contract the current public array already has, so `length` reads, index reads, iteration, and a deep comparison against an empty array all behave exactly as they do today. Four call sites depend on precisely that contract and none of their files is in this plan's `files_modified`: `tools/release-close/eligibility.test.js:29` and `tools/release-close/eligibility.test.js:135` and `tools/release-close/safe04.test.js:181` and `tools/release-close/safe04.test.js:228`, each asserting `fake.writes.length === 0` after a run and expecting `0`. The observable contract those four sites rely on is therefore frozen and named: the accessor is named `writes`, it returns an array, its `length` equals the closure tally, and with no side effect observed it is an empty frozen array, so all four assertions stay literally true with no edit to either suite. Do not rename `writes`, do not return an object, a number, or a set, do not make the accessor writable, and do not drop the member entirely. If a later plan replaces the accessor, it must migrate those four call sites explicitly and add the two test files to its own declared scope, never silently break them.

Add a test-visible way to drive the trap deliberately, so the canary in task 3 can record a real side effect: export a factory that returns a client whose trap is armed and whose measured count is observed, and keep it clearly named as a test seam rather than as a production capability. Do not give the fake any method that performs a remote side effect; the trap records, it never acts.

Avoid: keeping a public mutable writes array as the source of the count, which is now the closure tally. Avoid a count that any caller can set. Avoid freezing so aggressively that the existing `assertClientShape` call at the end of construction throws; assert the shape before freezing. Avoid changing the failure-plan contract, since plan 09-09 depends on it to drive the reconciliation seam. Avoid touching `eligibility.test.js` or `safe04.test.js` in this task: the compatibility accessor exists precisely so neither file has to change, and editing them would put two files outside this plan's declared scope into its diff.
  </action>
  <verify>
    <automated>node --test tools/release-close/</automated>
    <fails_when>the run exits non-zero, or the measured count can be changed from outside, or attaching a member to the returned client succeeds, or the scripted-failure or call-ordering tests regress, or the `writes` accessor stops being a read-only array whose length equals the measured count, which is what would turn the four untouched assertions in eligibility.test.js and safe04.test.js red</fails_when>
  </verify>
  <done>The fake measures side effects through its own trap, exposes an uneditable count, refuses direct capability attachment by being frozen, keeps the legacy `writes` member alive as a read-only array-shaped accessor so the two suites this plan does not own stay green, and keeps every existing read, scripted outcome, and call-log assertion green.</done>
  <acceptance_criteria>Source asserts the tally lives in a closure, asserts the client is frozen after construction, asserts the shape check runs before the freeze, asserts the `writes` member is a read-only accessor returning a frozen array whose length equals the closure tally, asserts neither eligibility.test.js nor safe04.test.js is modified by this task, and asserts the five read methods and the six scripted outcomes are unchanged. Behavior asserts an armed trap increments the measured count, asserts the `writes` length matches the measured count with zero observed side effects, asserts a caller cannot assign the count, asserts direct member attachment throws, and asserts the four existing `fake.writes.length` call sites still read zero.</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Forbidden-write canary whose escape turns a scoped test run red</name>
  <files>tools/release-close/canary.test.js</files>
  <read_first>tools/release-close/client.js as rewritten by tasks 1 and 2 (the exact surface check and the shared zero-mutation invariant the canary must defeat on purpose); tools/release-close/fake-client.js as rewritten by task 2 (the frozen client, the trap, and the test seam that arms it); tools/release-close/nowrite.test.js lines 86-114 (the existing static text scan, which stays as a complementary check and is not replaced); tools/release-close/safe04.test.js lines 175-240 (the existing zero-mutation assertions that keep reading `fake.writes.length` unchanged, and that plan 09-07 task 2 later extends to consume the measured count in the rendered output)</read_first>
  <action>
Add `tools/release-close/canary.test.js` proving the zero-mutation boundary is real rather than asserted, and add nothing else to production source.

Write two groups of tests. The green group, which must pass: an object carrying the five declared reads plus one extra callable member is rejected by the shape check with a reason naming the extra capability; an object with exactly the five reads plus non-callable bookkeeping is accepted; the shared invariant accepts a measured count of zero and refuses a measured count of one with a PT-BR reason naming the capability and the count; the shared invariant refuses a measured value that is not a non-negative integer; arming the fake's trap and recording a side effect makes the measured count non-zero and makes the invariant refuse; and the fail-closed gh stub is still accepted by the exact surface check and still throws naming Phase 10.

The canary group, which is the point of the file: spawn a child Node process running a narrowly scoped test file written into the operating system's temporary directory, never into the tool directory, that deliberately performs the escape the boundary exists to prevent. The child builds a client by copying the fake's own properties into a fresh plain object and adding a forbidden write capability taken from the vocabulary the static scan already enumerates, so the frozen instance is bypassed and the extra member is invisible to any structural check that only inspects the instance. The child then invokes the decision path with a measured record claiming zero while the trapped side effect is outstanding, and asserts the clean result. Because the escape is real, the shared invariant must refuse, and the child process must terminate with a non-zero status. The parent test asserts the child's status is non-zero, asserts the child's output names the detected side effect, and asserts the child never wrote outside its temporary directory. Remove the temporary file in a finally path so a failing canary leaves nothing behind.

Write the child spec's text inside this test file, never in a production source, and keep the forbidden capability name out of every production file. Keep the existing static scan in `nowrite.test.js` untouched: the scan and the canary are complementary, and neither replaces the other. Keep the canary a child process rather than an in-process failure, because an in-process failure would make the whole suite red on every run and hide a real regression behind a permanently expected failure.

Avoid: a canary that merely asserts a static text pattern; that is the gap being closed. Avoid writing the temporary spec inside `tools/release-close/`, which the secret-hygiene scan and the source scan would then pick up. Avoid adding a runner, an assertion library, or any `dependencies` entry to `tools/release-close/package.json` (D-02, D-06). Avoid shelling out to anything other than the local Node binary.
  </action>
  <verify>
    <automated>node --test tools/release-close/canary.test.js && node --test tools/release-close/</automated>
    <fails_when>either run exits non-zero, or the canary child exits zero, or the canary child's output does not name the detected side effect, or any production file under tools/release-close/ ends up naming the forbidden capability, or the whole tool suite regresses</fails_when>
  </verify>
  <done>An injected forbidden write is structurally rejected, a trapped side effect is measured and refused by the shared invariant, and a deliberately escaped write makes a scoped child test run exit non-zero with a message naming the detected side effect.</done>
  <acceptance_criteria>Test asserts the extra-callable rejection names the capability, asserts the invariant refuses a count of one and a non-integer measurement, asserts the trap makes the measured count non-zero, asserts the canary child exits non-zero with the side effect named, and asserts the temporary spec is removed afterwards. Source asserts no production file under tools/release-close/ names the forbidden capability and that the temporary path is outside the tool directory.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller-supplied client object to the decision path | A client is a capability surface, not just a data holder; anything callable on it is reachable by the contract |
| fake client internals to reported evidence | The measured count crosses into CLI output, so it must be a closure-owned measurement, not a value a caller can edit |
| test process to temporary filesystem | The canary writes a temporary spec; it must stay outside the tool directory and be cleaned up |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-06-01 | Elevation | assertClientShape extra-callable tolerance | high | mitigate | The surface check now enumerates own callable members and rejects any name outside the five declared reads, with a PT-BR reason naming the offending capability |
| T-09-06-02 | Tampering | measured mutation count | high | mitigate | The tally lives in a closure, the instance is frozen after construction, both the count and the `writes` record are exposed only as non-writable accessors, and the shared invariant refuses any count other than zero with no reset or forgive path, so a post-hoc clean-up is impossible |
| T-09-06-03 | Repudiation | hard-coded zero in renderers | high | mitigate | This plan supplies the measurement; plan 09-07 removes the literal and asserts the reported value is the measured one, so a tautological green cannot survive |
| T-09-06-04 | Information disclosure | temporary canary spec | low | mitigate | The canary spec lives in the operating system temporary directory, is removed in a finally path, and carries no credential-shaped value; it never touches the tool directory that the secret-hygiene scan walks |
| T-09-06-05 | Denial of service | canary leaving a child process pending | low | mitigate | The canary child is spawned with a bounded timeout and a failure to terminate fails the parent test rather than hanging the suite |
| T-09-06-SC | Tampering | npm installs | high | mitigate | No install step exists in any task of this plan; the zero-dependency manifest and the node:test runner are unchanged |

</threat_model>

<verification>
`node --test tools/release-close/canary.test.js` green; `node --test "tools/release-close/*.test.js"` green with no regression in the eligibility, classifier, gate, or SAFE-04 suites, which is what proves the four out-of-scope `fake.writes.length` assertions still read zero through the compatibility accessor; a manual probe that attaches an extra callable to the frozen client throws; a manual probe that spreads the client to bypass the freeze is still rejected by the exact surface check; `git status` shows changes confined to `tools/release-close/client.js`, `tools/release-close/fake-client.js`, and `tools/release-close/canary.test.js`, with `tools/release-close/eligibility.test.js` and `tools/release-close/safe04.test.js` untouched.
</verification>

<success_criteria>
The zero-mutation claim is now a measurement rather than a convention: the client surface is exact, the counter is closure-owned and uneditable, the shared invariant refuses any non-zero count, and a deliberately injected forbidden write provably turns a scoped test run red instead of leaving the suite green.
</success_criteria>

<output>
Create `.planning/phases/09-release-close-contract-fixtures/09-06-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Symbol | File | Consumed by |
|--------|------|-------------|
| Exact read-only capability surface and `assertNoMutation` invariant | tools/release-close/client.js (tightened) | 09-07 counter propagation, Phase 10 live client, Phase 11 reconciliation |
| Programmable fake with a frozen surface, a closure-owned tally, a trap test seam, and the legacy `writes` member preserved as a read-only array-shaped accessor | tools/release-close/fake-client.js (tightened) | 09-07 decision path, 09-09 reconciliation seam, eligibility.test.js and safe04.test.js read sites, all suites |
| Forbidden-write canary suite with a child-process escape proof | tools/release-close/canary.test.js | 09-VERIFICATION re-run, SAFE-04 traceability |
