---
phase: 09-release-close-contract-fixtures
plan: "03"
type: execute
wave: 3
depends_on:
  - 09-02
files_modified:
  - tools/release-close/apply-gate.js
  - tools/release-close/release-close.js
  - tools/release-close/safe04.test.js
  - .planning/phases/09-release-close-contract-fixtures/COVERAGE.md
autonomous: true
requirements:
  - SAFE-04
  - OPS-01
estimate:
  tokens: 40000
  raw_tokens: 28000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - Operator running verify or plan sees mutations: 0 and the suite fails if any write call escaped either mode
    - apply prints the full ordered plan (Release draft, readback, publish, readback, Milestone open, readback, close, final readback) before asking anything
    - apply proceeds only with both the yes flag and a live TTY sim confirmation; piped non-TTY with the flag still refuses
    - The suite proves output never carries credentials or auth headers
  artifacts:
    - tools/release-close/apply-gate.js
    - tools/release-close/safe04.test.js
    - .planning/phases/09-release-close-contract-fixtures/COVERAGE.md
  key_links:
    - release-close.js apply path calls the gate in apply-gate.js only after rendering the plan
    - safe04.test.js asserts mutations zero for verify and plan plus refusal for all three missing-lock combinations
  prohibitions:
    - apply must never execute without a human-visible ordered plan
    - a lone yes flag in a script must never substitute for interactive confirmation
    - output must never include secrets, tokens, or authorization material
---

<objective>
SAFE-04 close-out on top of the 09-01 tracer and 09-02 contract (per D-13, D-14, D-15, D-16): double-locked apply gate, ordered plan display previewing the Phase 11 recovery order, text-default plus json output, mutations-zero proof for verify/plan, secret-hygiene static test, and the API-coverage declaration.

Purpose: Make the no-mutation guarantee and the two-lock apply refusal executable proof, completing all four Phase 9 requirements.
Output: apply gate module, finished CLI, SAFE-04 suite, and COVERAGE.md declaration.
</objective>

<execution_context>
@/home/lucas/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/lucas/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/src/config/logger.js
@.planning/phases/09-release-close-contract-fixtures/09-01-SUMMARY.md
@.planning/phases/09-release-close-contract-fixtures/09-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Double-locked apply gate plus ordered plan display</name>
  <files>tools/release-close/apply-gate.js, tools/release-close/release-close.js</files>
  <read_first>tools/release-close/release-close.js from 09-01 (verify path and refusal stubs to replace on the apply path only); backend/src/config/logger.js lines 1-8 (redaction precedent: never handle secret-shaped values); 09-RESEARCH.md Code Examples apply double-lock gate and Pattern 4 (plan-always-visible-before-prompt); CONTEXT D-14/D-15 (both-locks rule, Phase 11 order preview)</read_first>
  <action>Create tools/release-close/apply-gate.js exporting confirmApply with inputs yesFlag, isTTY, planText, and askAnswer connection, implementing the refusal order: missing yesFlag refuses with PT-BR reason, non-TTY refuses even with the flag, answer other than sim refuses, only all three together confirm, per D-14. Extend tools/release-close/release-close.js: replace the apply refusal stub with plan rendering that lists the eight ordered steps Release draft, readback, publish, readback, Milestone open, readback, close, final readback with what-will-be-created versus adopted markers against frozen SHAs and IDs per D-15, print the plan before any prompt, then drive confirmApply over node:readline with the isTTY property of process stdin; keep verify and plan read-only and non-prompting. Add json-flag support to plan mirroring verify per D-13. Keep all Phase 9 apply outcomes refusal-or-fail-closed against fakes only; no live mutation path is added and ci.yml stays untouched per the resolved researcher open question.</action>
  <verify>
    <automated>node tools/release-close/release-close.js apply --yes < /dev/null; echo "piped_exit=$?"</automated>
    <fails_when>exit code is zero, or output lacks a refusal mentioning interactive terminal or the yes flag</fails_when>
  </verify>
  <done>apply renders the eight-step plan then refuses without both locks; piped non-TTY with flag refuses fail-closed.</done>
  <acceptance_criteria>Source asserts apply-gate.js checks the flag before TTY before answer in that order; behavior asserts plan text precedes any prompt in output; CLI asserts piped apply exits non-zero with PT-BR refusal.</acceptance_criteria>
</task>

<task type="auto">
  <name>SAFE-04 proof suite plus secret-hygiene static test</name>
  <files>tools/release-close/safe04.test.js</files>
  <read_first>tools/release-close/apply-gate.js and tools/release-close/release-close.js from task 1 (gate order, plan text, json shape); tools/release-close/fake-client.js from 09-02 (writes trap and call log for zero-write asserts); backend/src/config/logger.js (redact-key precedent for the banned-output token set)</read_first>
  <action>Create tools/release-close/safe04.test.js in node:test with PT-BR it names asserting: verify and plan runs over every fixture record zero writes and emit mutations 0 in both text and json forms per D-16; apply refuses when the flag is absent; apply refuses when TTY is false despite the flag; apply refuses on answer other than sim; apply confirms only with flag plus TTY true plus sim answer with plan text rendered before confirmation; json output of verify and plan parses as JSON and carries the mutations field. Extend the file with a static test reading CLI and gate sources plus captured sample outputs, asserting none contain credential-shaped material (the token variable names, bearer header shape, or verbose-trace flag documented in RESEARCH Pitfall 7 — spell the tokens only inside this test file, never in implementation sources). Flagged assumption E (SAFE-04 idempotency probe, unresolved): verify/plan re-runs are safe because mutations zero makes repetition side-effect-free — codify with a test running verify then plan on the same fake asserting identical decisions and empty writes. Flagged assumption F (SAFE-04 concurrency probe, unresolved): no cross-process apply lock exists in Phase 9; the double-lock plus single-process fake is the whole guarantee and workstation locking lands in Phase 11 REC-05 — codify with a test asserting two sequential apply-gate evaluations share no state.</action>
  <verify>
    <automated>node --test tools/release-close/</automated>
    <fails_when>non-zero exit, or the summary line shows 0 passed</fails_when>
  </verify>
  <done>Full tool suite green: mutations-zero on verify/plan, all three refusal locks, confirm-only-with-all-locks, secret-free output, and both flagged SAFE-04 assumptions executable.</done>
  <acceptance_criteria>Source asserts safe04.test.js is the only implementation-adjacent file naming credential-shaped tokens; behavior asserts all refusal and confirmation cases; test asserts verify-then-plan identical output with empty writes and stateless sequential gate evaluations.</acceptance_criteria>
</task>

<task type="auto">
  <name>Coverage declaration plus final gate sweep</name>
  <files>.planning/phases/09-release-close-contract-fixtures/COVERAGE.md</files>
  <read_first>tools/release-close/gh-client.js from 09-02 (throwing stub as evidence of no live integration); .planning/ROADMAP.md Phase 9 section (scope bounds for the declaration)</read_first>
  <action>Write .planning/phases/09-release-close-contract-fixtures/COVERAGE.md with a reasoned declaration that no external API integration exists in Phase 9: the pure contract runs against an in-memory fake and a throwing gh-client stub, no live gh api call is issued by any source or test, and live gh api calls land in Phase 10. Then run the final gate sweep: full scoped suite green, backend vitest collateral run proving no collateral damage, and confirm git status shows no changes outside tools/release-close/ and the phase directory with ci.yml and backend and frontend untouched per milestone constraints.</action>
  <verify>
    <automated>node --test "tools/release-close/*.test.js" && cd backend && npx vitest run 2>&1 | tail -4</automated>
    <fails_when>tool suite non-zero exit, or backend suite shows failures caused by this phase (backend must stay green; it was green before Phase 9)</fails_when>
  </verify>
  <done>COVERAGE.md declares fake-plus-stub only; tool suite and backend suite both green; change surface confined to the tool dir and phase dir.</done>
  <acceptance_criteria>Source asserts COVERAGE.md names the fake and stub with Phase 10 as the live-call milestone; behavior asserts both suites green; CLI asserts git status lists no backend, frontend, ci.yml, or Dockerfile paths.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator TTY→apply gate | Typed sim answer crosses here; spoofed pipes must refuse |
| tool output→logs/terminal | Rendered plan and JSON cross here; secrets must never cross |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-07 | Elevation | apply-gate.js single-lock regression | high | mitigate | Tests pin all three missing-lock refusals plus the confirm-only-with-all-locks case; any future weakening breaks the suite (blocks Phase 13 safety case from silently eroding) |
| T-09-08 | Information disclosure | verify/plan --json output | high | mitigate | Secret-hygiene static test scans sources and sample outputs for credential-shaped material; tool never reads env token variables or passes trace flags (blocks credential leak into runbook evidence) |
| T-09-09 | Tampering | plan display diverging from real Phase 11 order | medium | mitigate | Eight steps fixed in the order Release draft, readback, publish, readback, Milestone open, readback, close, final readback per D-15; test asserts the order verbatim |
| T-09-SC | Tampering | npm installs | high | mitigate | Zero-dependency lock holds end to end: no install step in any task of any plan |

</threat_model>

<verification>
node --test "tools/release-close/*.test.js" green; piped apply refuses non-zero; backend vitest green; git status confined to tools/release-close/ plus phase dir; COVERAGE.md present.
</verification>

<success_criteria>
SAFE-04 executable: verify/plan mutations-zero proven, apply double-lock with visible eight-step plan proven, output secret-free proven, coverage declared, and all four Phase 9 requirements (OPS-01, OPS-02, SAFE-02, SAFE-04) covered across the three plans.
</success_criteria>

<output>
Create `.planning/phases/09-release-close-contract-fixtures/09-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Symbol | File | Consumed by |
|--------|------|-------------|
| confirmApply (double-lock gate) | tools/release-close/apply-gate.js | Phase 13 live apply procedure |
| finished CLI (verify/plan/apply + --json + plan display) | tools/release-close/release-close.js (extended) | Phase 10 preflight verbs, Phase 12 runbook |
| SAFE-04 suite + secret-hygiene test | tools/release-close/safe04.test.js | Phase 12 evidence-shape work |
| no-live-integration declaration | .planning/phases/09-release-close-contract-fixtures/COVERAGE.md | Phase 10 live-client planning |
