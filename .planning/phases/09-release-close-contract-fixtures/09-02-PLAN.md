---
phase: 09-release-close-contract-fixtures
plan: "02"
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - tools/release-close/classify.js
  - tools/release-close/fake-client.js
  - tools/release-close/gh-client.js
  - tools/release-close/fixtures/missing.json
  - tools/release-close/fixtures/partial.json
  - tools/release-close/fixtures/duplicate.json
  - tools/release-close/fixtures/conflicting.json
  - tools/release-close/fixtures/failed.json
  - tools/release-close/fixtures/concurrent.json
  - tools/release-close/classify.test.js
  - tools/release-close/nowrite.test.js
autonomous: true
requirements:
  - OPS-02
  - SAFE-02
estimate:
  tokens: 45000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - Operator can classify a frozen snapshot into exactly one of MISSING, PARTIAL, DUPLICATE, CONFLICTING, FAILED, CONCURRENT with a PT-BR reason and writeAction null
    - Fixture-backed runs reproduce scripted timeout, lost-response, 409, 422, 429, and 5xx sequences with observable re-read ordering in the call log
    - The suite proves no ref-write or destructive tag path exists anywhere in non-test sources
    - The gh seam exists only as a throwing stub naming Phase 10, and its throw is covered by a test
  artifacts:
    - tools/release-close/classify.js
    - tools/release-close/fixtures/missing.json
    - tools/release-close/fixtures/partial.json
    - tools/release-close/fixtures/duplicate.json
    - tools/release-close/fixtures/conflicting.json
    - tools/release-close/fixtures/failed.json
    - tools/release-close/fixtures/concurrent.json
    - tools/release-close/gh-client.js
    - tools/release-close/classify.test.js
    - tools/release-close/nowrite.test.js
  key_links:
    - classify.js imports nothing from eligibility.js and is tested standalone per D-09
    - fake-client.js scripted sequences drive classify tests without network per D-07
  prohibitions:
    - classification must never overwrite or delete a conflicting remote object to resolve ambiguity
    - fixture data must never contain credentials, tokens, or authorization headers
    - the gh seam must never perform a live call in this phase
---

<objective>
Expansion from the 09-01 tracer (per D-05, D-07, D-08, D-09, D-10): pure six-state classifier, six versioned JSON fixtures, scripted fake-client sequences with call-log ordering, throwing gh-client stub, and the no-ref-write static proof for SAFE-02.

Purpose: Give OPS-02 its deterministic mocked-API scenario coverage and SAFE-02 its executable no-ref-write guarantee.
Output: classify module, fixture suite, hardened fake, stub, and two test files — all green under node --test.
</objective>

<execution_context>
@/home/lucas/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/lucas/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/src/services/votingService.js
@.planning/phases/09-release-close-contract-fixtures/09-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Pure six-state classifier plus six fixtures</name>
  <files>tools/release-close/classify.js, tools/release-close/fixtures/missing.json, tools/release-close/fixtures/partial.json, tools/release-close/fixtures/duplicate.json, tools/release-close/fixtures/conflicting.json, tools/release-close/fixtures/failed.json, tools/release-close/fixtures/concurrent.json</files>
  <read_first>backend/src/services/votingService.js lines 29-49 (tally closed-set outcome vocabulary precedent for D-10); tools/release-close/eligibility.js from 09-01 (return-shape family to match, module independence to preserve); tools/release-close/fixtures/reference.json from 09-01 (frozen-value style to extend); 09-RESEARCH.md Pattern 2 (return-data-not-throw)</read_first>
  <action>Create tools/release-close/classify.js exporting classifySnapshot(snapshot) as a pure function of a plain evidence object with release and milestone and checks and eligibility fields, returning one of the six EN codes MISSING, PARTIAL, DUPLICATE, CONFLICTING, FAILED, CONCURRENT inside the eligible/code/reason/writeAction family with PT-BR reason and writeAction null per D-10/D-11/D-12; throw TypeError with PT-BR message only on invalid snapshot shape. Keep the module import-free of eligibility.js per D-09. Author six fixtures under tools/release-close/fixtures/ with frozen canonical values per D-05/D-08: missing (absent release and milestone over eligible tag baseline), partial (owned draft release present), duplicate (two same-tag releases), conflicting (materially different duplicate), failed (red checks evidence), concurrent (two in-flight close markers). Reuse the canonical SHAs and run identifiers from reference.json verbatim; freeze one timestamp string shared across all six.</action>
  <verify>
    <automated>node --test tools/release-close/classify.test.js 2>&1 | tail -4 || node -e "import('./tools/release-close/classify.js').then(m => console.log(typeof m.classifySnapshot))"</automated>
    <fails_when>both probes fail: no test file yet AND the import does not print function</fails_when>
  </verify>
  <done>classifySnapshot maps each of the six fixtures to its code with PT-BR reason and null write action.</done>
  <acceptance_criteria>Source asserts classify.js has no import of eligibility.js; behavior asserts one-to-one mapping of six fixtures to six codes; CLI asserts nothing in this task.</acceptance_criteria>
</task>

<task type="auto">
  <name>Scripted fake sequences plus throwing gh stub</name>
  <files>tools/release-close/fake-client.js, tools/release-close/gh-client.js</files>
  <read_first>tools/release-close/fake-client.js and tools/release-close/client.js from 09-01 (method surface to extend, never to narrow); 09-RESEARCH.md Pattern 1 and Pitfall 4 (scripted failure sequences, no write-method names, no write HTTP-method strings); 09-RESEARCH.md Open Question 2 resolution (stub that throws with Phase 10 marker)</read_first>
  <action>Extend tools/release-close/fake-client.js so makeFakeClient accepts an optional scripted-failure plan keyed by method name, where each entry is an ordered list of outcomes drawn from timeout, lost-response, status-409, status-422, status-429, status-5xx, consumed in order across successive calls and recorded in the existing call log with sequence numbers, defaulting to snapshot data when the plan is exhausted, preserving the writes-array trap and the five read-method surface per D-07. Create tools/release-close/gh-client.js exporting the same five method names, each throwing an Error naming Phase 10 as the wiring milestone, with a module header comment stating the stub exists so the seam compiles and fails closed. Keep every write-verb and every write HTTP-method token out of both files by referring to the forbidden surface only as refused-future-capability prose in comments, so the 09-03 static test has a clean target.</action>
  <verify>
    <automated>node --test "tools/release-close/*.test.js" 2>&1 | tail -5</automated>
    <fails_when>non-zero exit, or the summary line shows 0 passed</fails_when>
  </verify>
  <done>Scripted timeout/409/422/429/5xx/lost sequences observable via call log; gh stub throws on every method; tracer suite still green.</done>
  <acceptance_criteria>Source asserts fake-client.js exposes script consumption plus call log plus writes trap; behavior asserts a 429-then-data script returns failure first and data second with two ordered log entries; test asserts every gh-client method throws naming Phase 10.</acceptance_criteria>
</task>

<task type="auto">
  <name>Six-state proof suite plus no-ref-write static test</name>
  <files>tools/release-close/classify.test.js, tools/release-close/nowrite.test.js</files>
  <read_first>backend/tests/voting.test.js lines 1-24 (tally PT-BR test naming to mirror); tools/release-close/classify.js and the six fixtures from task 1 plus fake-client.js and gh-client.js from task 2 (systems under test); 09-RESEARCH.md Pitfalls 4 and 6 (forbidden surface, SHA transcription drift)</read_first>
  <action>Create tools/release-close/classify.test.js in node:test with PT-BR it names asserting: each fixture classifies to its code with writeAction null; scripted 409 then 429 then data sequence is observable through the fake call log in ref, tag-object, main, release, milestones order; failed fixture carries FAILED with PT-BR reason and no write action; invalid snapshot shape throws TypeError. Create tools/release-close/nowrite.test.js reading every non-test .js file under tools/release-close/ and asserting absence of the forbidden surface tokens that would indicate a ref-write or destructive tag path, plus asserting no test snapshot contains credential-shaped strings, per SAFE-02 success criterion 3 and RESEARCH Pitfall 4. Flagged assumption C (OPS-02 concurrency probe, unresolved): concurrent-state classification in Phase 9 is single-process fake evidence only; real workstation locking is Phase 11 REC-05 — codify with a test classifying the concurrent fixture deterministically twice with identical codes. Flagged assumption D (SAFE-02 unclassified probe, unresolved): no ref-write path exists by construction (interface has reads only) — codify with the static-absence test plus an assertion that no decision object ever carries a non-null write action across all seven fixtures.</action>
  <verify>
    <automated>node --test tools/release-close/</automated>
    <fails_when>non-zero exit, or the summary line shows 0 passed</fails_when>
  </verify>
  <done>Six states proven from fixtures, scripted failures observable, static test proves no ref-write path, flagged OPS-02/SAFE-02 assumptions executable.</done>
  <acceptance_criteria>Source asserts nowrite.test.js scans non-test sources only; behavior asserts six fixture-to-code mappings and deterministic concurrent classification; test asserts zero forbidden-surface matches and all write actions null.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture files→classify | Versioned JSON crosses here; a malicious edit could flip a decision |
| operator→gh stub | Any accidental call crosses here and must fail closed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-04 | Tampering | fixtures/*.json | high | mitigate | Reference values reviewed against ROADMAP baseline in PR; full-SHA equality means edits fail closed; nowrite test pins the forbidden surface so poisoned fixtures cannot smuggle write paths |
| T-09-05 | Elevation | gh-client.js stub mistaken for working code | medium | mitigate | Every method throws naming Phase 10; dedicated test asserts the throw; header comment states stub status |
| T-09-06 | Repudiation | scripted fake sequences hide real ordering bugs | low | accept | Call log asserts the read order ref, tag-object, main, release, milestones; Phase 10 re-verifies against live reads |
| T-09-SC | Tampering | npm installs | high | mitigate | Zero-dependency lock holds: no install step, no new registry surface |

</threat_model>

<verification>
node --test "tools/release-close/*.test.js" green with classify plus nowrite suites; git status shows only tools/release-close/ paths; ci.yml untouched.
</verification>

<success_criteria>
Six-state contract proven from versioned fixtures, scripted failure sequences observable, gh seam stubbed fail-closed, and the suite executable-proves no ref-write or destructive tag path.
</success_criteria>

<output>
Create `.planning/phases/09-release-close-contract-fixtures/09-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Symbol | File | Consumed by |
|--------|------|-------------|
| classifySnapshot | tools/release-close/classify.js | 09-03 plan display, Phase 11 adoption logic |
| six state fixtures | tools/release-close/fixtures/missing.json, partial.json, duplicate.json, conflicting.json, failed.json, concurrent.json | Phase 11 rehearsal inputs |
| scripted fake sequences + call log | tools/release-close/fake-client.js (extended) | 09-03 write-trap asserts, Phase 11 retry tests |
| throwing gh seam | tools/release-close/gh-client.js | Phase 10 real client implementation |
