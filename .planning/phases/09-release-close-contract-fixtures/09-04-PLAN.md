---
phase: 09-release-close-contract-fixtures
plan: "04"
type: execute
wave: 4
depends_on:
  - 09-01
coupling_justified:
  - "09-05: both plans run the shared tools/release-close/ suite, but the written file sets are disjoint (09-04 writes only the eligibility predicate and its suite; 09-05 writes the classifier, the frozen fixtures, and the two shared test/CLI files). 09-04 only reads fixtures/reference.json and never rewrites it, and 09-05 keeps verify and plan green on the frozen baseline, so either order yields the same suite result."
  - "09-06: same shared suite and disjoint writes; 09-04 builds no client or fake, so 09-06 tightening the capability surface cannot invalidate an 09-04 assertion and neither plan reads the other's in-flight output."
files_modified:
  - tools/release-close/eligibility.js
  - tools/release-close/eligibility.test.js
autonomous: true
gap_closure: true
requirements:
  - SAFE-02
  - OPS-01
estimate:
  tokens: 27000
  raw_tokens: 18000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - An annotated tag is eligible only when the first hop is the exact ref refs/tags/<requested version>, the ref object SHA is a full 40-hex tag-object identity, the returned tag object's own SHA equals that identity, and the second hop object type is commit
    - A tree, blob, or tag second hop, a ref naming another version, a tag-object identity that does not match the ref, and an abbreviated SHA at either hop are all refused with writeAction null and a stable EN code
    - An absent read, a denied read, a throttled or server-error read, a malformed envelope, and a transport failure each normalize to their own stable EN code and are never reported as eligible
    - Repeating verify over the same frozen input produces byte-identical output and zero measured mutations
    - Two independently constructed fake clients keep separate call logs and separate mutation counters
    - No eligibility decision ever proposes a remote write
    - Human-facing reasons are PT-BR and machine codes are EN
  artifacts:
    - tools/release-close/eligibility.js
    - tools/release-close/eligibility.test.js
  key_links:
    - release-close.js decide calls checkTagEligibility with the same signature, so the stricter predicate is internal to eligibility.js and no caller changes shape
    - eligibility.test.js drives the frozen reference fixture through makeFakeClient and asserts exact codes, never merely that a code is a string
    - the CLI verify path emits the code the pure predicate returned, so a tightened predicate is visible without a caller rewrite
    - the reference fixture this plan reads is owned by 09-05 task 1, which adds the canonical ci and target blocks to it; this plan asserts nothing about those blocks, so the two same-wave edits to that one file cannot collide (09-04 never writes a fixture)
  prohibitions:
    - no live GitHub, network, or subprocess call from any non-test source under tools/release-close/
    - no git ref or tag write path at porcelain, ref, or object level
    - no credential or authorization-header value read, passed as an argument, or emitted
    - no fabricated CI success synthesized from bare run identifiers
    - no ignored, swallowed, or redirected output path around the human-visible reviewed plan
    - no remote write of any kind before Phase 13; every domain mismatch keeps writeAction null
---

<objective>
Close VERIFICATION gap 1 (strict annotated-tag peel) and the overlapping advisory WR-01 (permission/transport outcomes collapsed into absence) in the pure eligibility predicate, so the SAFE-02 contract actually proves "annotated tag whose second hop is a commit" instead of "two equal strings".

Purpose: Today `eligibility.js` reads only the second-hop SHA. A tree peel, a ref for another tag, a mismatched tag-object identity, and an abbreviated tag SHA all return ELIGIBLE. Phase 10 will wire this predicate to real reads; a permissive predicate must be repaired now, not later.
Output: A strict two-hop identity predicate with a stable EN code vocabulary and PT-BR reasons, plus an exact-code negative matrix that drives it end to end.

Planner assumptions (deterministic edge probe, resolved here):
- SAFE-02 unclassified edge: an absent, denied, throttled, server-error, malformed, or transport-failed read must normalize to its own code and must never be accepted as eligibility. Resolved by task 2, asserted in task 3.
- OPS-01 idempotency: repeated verify/plan over the same frozen input is byte-stable and mutation-free, and an interruption cannot produce a remote write in Phase 9. Resolved by task 3.
- OPS-01 concurrency: independent fake-client decision runs share no call log and no mutation counter. Real workstation locking is Phase 11 REC-05 and is explicitly NOT claimed here; this plan only proves single-process isolation.
</objective>

<execution_context>
@/home/lucas/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/lucas/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-release-close-contract-fixtures/09-CONTEXT.md
@.planning/phases/09-release-close-contract-fixtures/09-VERIFICATION.md
@tools/release-close/eligibility.js
@tools/release-close/fake-client.js
@tools/release-close/fixtures/reference.json
@tools/release-close/release-close.js
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Strict two-hop annotated-tag identity, end to end from the frozen fixture to the CLI decision</name>
  <files>tools/release-close/eligibility.js, tools/release-close/eligibility.test.js</files>
  <read_first>tools/release-close/eligibility.js (the first-hop type check at 47-58, the second-hop SHA read at 61-68, and the two equality branches at 76-88); tools/release-close/fixtures/reference.json (the exact ref name, tag-object identity, and peeled commit the reference path must accept); tools/release-close/release-close.js lines 274-301 (how decide calls checkTagEligibility and how the code reaches CLI output); .planning/phases/09-release-close-contract-fixtures/09-VERIFICATION.md gap 1 (the four accepted-as-eligible probe cases that must now be refused)</read_first>
  <behavior>
    - case: reference fixture as frozen -> code ELIGIBLE, eligible true, writeAction null
    - case: second hop object type tree -> code TAG-IDENTITY, eligible false
    - case: second hop object type blob -> code TAG-IDENTITY, eligible false
    - case: second hop object type tag (tag pointing at tag) -> code TAG-IDENTITY, eligible false
    - case: first hop ref name refs/tags/v0.1.0 while v0.1.1 was requested -> code TAG-IDENTITY, eligible false
    - case: tag object data.sha different from the ref object SHA -> code TAG-IDENTITY, eligible false
    - case: first hop object SHA abbreviated to 7 hex -> code TAG-IDENTITY, eligible false
    - case: second hop commit SHA abbreviated -> code TAG-IDENTITY, eligible false
    - case: peeled commit differs from remote main -> code SAFE-02, eligible false
    - case: peeled commit differs from the supplied expected SHA -> code SAFE-02, eligible false
    - case: first hop type commit (lightweight) -> code LIGHTWEIGHT, eligible false
  </behavior>
  <action>
Wire ONE path end to end with the strict predicate, then keep the caller shape unchanged. In `eligibility.js`, after the existing input validation, resolve the requested ref identity and require all of the following before any equality is evaluated: the first-hop envelope carries a `data.ref` string exactly equal to `refs/tags/` concatenated with the requested version; the first-hop `data.object.sha` matches the full 40-hex lowercase pattern; the second-hop envelope carries a `data.sha` string that is byte-identical to that same first-hop object SHA; and the second-hop `data.object.type` is exactly `commit`. Reject every other shape with the stable EN code `TAG-IDENTITY` and a PT-BR reason that names which of the four checks failed (ref name, tag-object SHA shape, tag-object identity match, or second-hop type). Keep the existing `MISSING` code exclusively for a genuinely absent object, keep `LIGHTWEIGHT` for a single-hop tag, and keep `SAFE-02` for a proven commit that diverges from remote `main` or from the expected SHA. Do not relax the strict string comparison into case-insensitive or prefix comparison: exact 40-hex equality only. Do not reorder the reads; the fixed order getTagRef, then getTagObject with the first-hop SHA, then getBranchHead for `main` is asserted by an existing ordering test and must keep passing.

In `eligibility.test.js`, drive the new negative cases through `makeFakeClient` over deep clones of the frozen reference fixture, and assert the exact `code` string for each case listed in the behavior block, plus `writeAction === null` on every refusal. Build the negative fixtures by mutating a deep clone, never by hand-typing a second copy of the reference SHAs, so the frozen baseline stays the single source of truth (D-08). Add one end-to-end assertion that the CLI path agrees: `node tools/release-close/release-close.js verify --json` exits zero and emits code `ELIGIBLE` for the reference baseline, and `--sha` with a divergent full SHA exits non-zero with code `SAFE-02`.

Avoid: a second code for the same family (keep one code per family so the runbook has stable names); accepting a peeled commit whose type is unknown; comparing SHAs case-insensitively; and widening `checkTagEligibility` into a multi-purpose validator that also inspects Release or Milestone evidence (D-09 keeps eligibility and classification in separate modules).
  </action>
  <verify>
    <automated>node --test tools/release-close/eligibility.test.js</automated>
    <fails_when>the run exits non-zero, or any negative case returns eligible true, or any refusal returns a code other than the exact expected EN code, or any refusal carries a non-null writeAction</fails_when>
  </verify>
  <done>A tree, blob, or tag peel, a foreign ref name, a mismatched tag-object identity, and an abbreviated SHA at either hop are all refused with code TAG-IDENTITY and writeAction null, while the frozen reference fixture still reaches ELIGIBLE end to end through the CLI.</done>
  <reversibility rating="costly">The EN code vocabulary and the four-part identity check become the predicate Phase 10 wires to live reads; renaming a code later churns the runbook and the Phase 10 tests.</reversibility>
  <acceptance_criteria>Source asserts eligibility.js compares data.ref against the requested refs/tags path, matches the second-hop data.sha against the first-hop object SHA, and requires second-hop type commit before any equality branch, and asserts every refusal literal uses writeAction null. Behavior asserts the eleven behavior cases above return the exact codes. CLI asserts verify --json exits zero with ELIGIBLE on the frozen baseline and non-zero with SAFE-02 on a divergent --sha.</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Status, permission, malformed, and transport normalization so absence is never inferred from a failure</name>
  <files>tools/release-close/eligibility.js, tools/release-close/eligibility.test.js</files>
  <read_first>tools/release-close/eligibility.js lines 42-75 (every place an unsuccessful envelope collapses into the absence code and where the status field is currently ignored); tools/release-close/fake-client.js lines 84-101 (the scripted outcomes that already produce thrown errors and non-2xx envelopes, and the exact status values 409, 422, 429, 500); tools/release-close/classify.js lines 41-50 (the sibling module's failure-only detection, to keep the two vocabularies recognizably different rather than copy-pasted); .planning/phases/09-release-close-contract-fixtures/09-VERIFICATION.md gap 1 and the SAFE-02 unclassified-edge assumption recorded in the objective</read_first>
  <behavior>
    - case: first hop envelope ok false with status 404 -> code MISSING
    - case: first hop envelope ok false with status 401 -> code PERMISSION
    - case: first hop envelope ok false with status 403 -> code PERMISSION
    - case: first hop envelope ok false with status 409 -> code UNAVAILABLE
    - case: first hop envelope ok false with status 422 -> code UNAVAILABLE
    - case: first hop envelope ok false with status 429 -> code UNAVAILABLE
    - case: first hop envelope ok false with status 500 -> code UNAVAILABLE
    - case: envelope ok true with a missing or wrongly typed data.object -> code MALFORMED
    - case: getTagRef throws a timeout error -> code TRANSPORT
    - case: getBranchHead throws a lost-response error -> code TRANSPORT
    - case: any of the above -> eligible false and writeAction null
  </behavior>
  <action>
In `eligibility.js`, branch on the exact numeric `status` of each unsuccessful envelope instead of collapsing every non-ok envelope into the absence code. Map only status 404 to `MISSING`. Map 401 and 403 to a new stable EN code `PERMISSION` with a PT-BR reason stating the operator's token lacks permission on the repository resource being read. Map 409, 422, 429, and any 5xx to `UNAVAILABLE` with a PT-BR reason naming the status number and stating the remote state is indeterminate. Map an envelope that reports ok true but whose `data` shape is not a plain record with the required fields, or whose status is not a finite number, to `MALFORMED`. Wrap each of the three client reads so that a thrown error from the read is caught and returned as `TRANSPORT` with a PT-BR reason stating the read did not complete and the remote state is unknown, instead of escaping to the caller as an unhandled rejection. Keep D-11 intact: every one of these outcomes returns data, never throws; `throw` stays reserved for invalid caller input such as a malformed version or a non-full expected SHA.

Do not introduce retry, backoff, or sleep here: the retry and natural-identity re-read contract belongs to the reconciliation seam, which plan 09-09 owns. Do not treat any non-404 failure as a retryable success. Do not merge `PERMISSION` into `UNAVAILABLE`; Phase 10 needs to tell a credential/permission problem apart from a transport or availability problem.

In `eligibility.test.js`, add one test per family from the behavior block, using deep clones of the frozen reference fixture with the envelope status or the read behavior changed, and assert the exact code plus `eligible === false` and `writeAction === null`. Add one test asserting the predicate still returns a plain object (never throws) for every scripted failure family, so a transport failure cannot escape as a rejected promise.
  </action>
  <verify>
    <automated>node --test tools/release-close/eligibility.test.js</automated>
    <fails_when>the run exits non-zero, or any status family other than 404 returns the absence code, or any family returns eligible true, or a thrown read escapes as a rejected promise instead of a normalized decision</fails_when>
  </verify>
  <done>Only status 404 reports absence; permission denial, indeterminate availability, malformed shape, and transport failure each report their own stable code with a PT-BR reason, and none is ever eligible.</done>
  <reversibility rating="costly">PERMISSION and UNAVAILABLE become the codes the Phase 10 live client and the Phase 12 runbook branch on; collapsing them later would erase the operator's ability to tell a credential problem from an availability problem.</reversibility>
  <acceptance_criteria>Source asserts eligibility.js has one branch per status family and no branch maps a non-404 status to the absence code. Behavior asserts the eleven normalization cases return their exact codes with writeAction null. Test asserts every scripted failure family returns a decision object rather than a rejected promise.</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Exact-code negative matrix, byte-stable repeat runs, and isolated per-client counters</name>
  <files>tools/release-close/eligibility.test.js</files>
  <read_first>tools/release-close/eligibility.test.js as rewritten by tasks 1 and 2 (the existing ordering, metacharacter, and zero-write tests that must keep passing); tools/release-close/fake-client.js lines 70-128 (the per-instance call log, writes array, and mutations getter that back the isolation assertions); tools/release-close/package.json (type module, so assertions are written for node:test with node:assert/strict and no dependency may be added)</read_first>
  <action>
Consolidate the eligibility proof into an exact-code matrix and close the two recorded OPS-01 assumptions, in `eligibility.test.js` only — this task changes no production source.

Build a table of named cases to exact expected codes (reference ELIGIBLE, lightweight LIGHTWEIGHT, wrong ref TAG-IDENTITY, abbreviated first-hop SHA TAG-IDENTITY, abbreviated peeled SHA TAG-IDENTITY, mismatched tag-object identity TAG-IDENTITY, tree peel TAG-IDENTITY, blob peel TAG-IDENTITY, tag-of-tag peel TAG-IDENTITY, missing tag MISSING, missing tag object MISSING, main divergence SAFE-02, expected-SHA divergence SAFE-02, 401 PERMISSION, 403 PERMISSION, 429 UNAVAILABLE, 500 UNAVAILABLE, malformed envelope MALFORMED, thrown read TRANSPORT) and assert the exact code for every row, never only that a code is a string. Assert in every row that writeAction is null and that the reason is a non-empty PT-BR string.

For the OPS-01 idempotency assumption: run the CLI verify path twice over the frozen reference fixture and assert the two JSON payloads are byte-identical strings and that both carry the zero measured mutation field, so repetition is provably free of drift. For the OPS-01 concurrency assumption: build two independent fake clients over the same frozen fixture, run the decision on the first, and assert the second still has an empty call log and a zero mutation counter, proving no state crosses instances. Add a short PT-BR comment on that test naming the boundary explicitly: single-process isolation is proven here and cross-process workstation locking is Phase 11 REC-05, so this suite must never be cited as a locking guarantee.

Avoid: adding a test runner, assertion library, or any entry to `dependencies` in `tools/release-close/package.json` — the suite stays zero-dependency `node:test` (D-02, D-06). Avoid asserting on a substring of a PT-BR reason where the exact code is the real contract. Avoid mutating the shared fixture module in place; clone before mutating so test order cannot leak state between cases.
  </action>
  <verify>
    <automated>node --test tools/release-close/eligibility.test.js && node --test tools/release-close/</automated>
    <fails_when>either run exits non-zero, or the two repeated verify payloads differ, or a second client inherits a call log or a non-zero counter from the first, or the whole tool suite regresses below its current passing count</fails_when>
  </verify>
  <done>Every eligibility family has an exact-code assertion, repeated verify is byte-stable and mutation-free, and independent clients are provably isolated, with the full tool suite still green.</done>
  <acceptance_criteria>Test asserts nineteen exact codes, asserts writeAction null and a non-empty PT-BR reason on every row, asserts byte-identical repeat CLI payloads, and asserts the untouched second client has an empty call log and a zero counter. Source asserts tools/release-close/package.json still declares no dependencies key.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| remote ref/tag object data to eligibility predicate | Untrusted remote-shaped data crosses here; a tree peel or a foreign ref must not be read as an annotated tag |
| client transport to eligibility predicate | Timeouts, permission denials, and server errors cross here and must not be read as absence |
| frozen fixture to production predicate | Fixture data crosses into the executable path; fixtures must never be a substitute for a real check |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-04-01 | Tampering | eligibility.js two-hop identity check | high | mitigate | Second-hop type must be commit, tag-object SHA must equal the ref object SHA, ref name must equal the requested refs/tags path, and both SHAs must be full 40-hex; tree, blob, tag-of-tag, foreign-ref, mismatched-identity, and abbreviated-SHA negatives are asserted by exact code |
| T-09-04-02 | Repudiation | eligibility.js status collapse | high | mitigate | Only status 404 maps to absence; 401/403, 409/422/429/5xx, malformed shape, and thrown transport each map to their own stable code with a PT-BR reason, so an operator can tell why a run refused |
| T-09-04-03 | Information disclosure | PT-BR reason strings | low | mitigate | Reasons name the SHA, the status, and the failing check only; they never include environment values, header material, or trace output |
| T-09-04-04 | Elevation | eligibility predicate consumed by apply | high | mitigate | Every refusal keeps writeAction null and eligible false, so a permissive peel cannot reach a release; the full tool suite must stay green for the caller to trust the code |
| T-09-04-SC | Tampering | npm installs | high | mitigate | No install step exists in any task of this plan; the zero-dependency manifest and the node:test runner are unchanged |

</threat_model>

<verification>
`node --test tools/release-close/eligibility.test.js` green with the exact-code matrix; `node --test "tools/release-close/*.test.js"` green with no regression in the classifier, gate, no-write, or SAFE-04 suites; `node tools/release-close/release-close.js verify --json` still exits zero with ELIGIBLE on the frozen baseline; `git status` shows changes confined to `tools/release-close/eligibility.js` and `tools/release-close/eligibility.test.js` with `.github/workflows/ci.yml`, `backend/`, and `frontend/` untouched.
</verification>

<success_criteria>
The strict peel gap is closed executably: a non-commit peel, a foreign ref, a mismatched tag-object identity, and an abbreviated SHA are all refused with stable codes and writeAction null; every failure family is normalized distinctly and never read as eligibility; repeat runs are byte-stable; independent clients are isolated; and no caller or contract outside `eligibility.js` had to change shape.
</success_criteria>

<output>
Create `.planning/phases/09-release-close-contract-fixtures/09-04-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Symbol | File | Consumed by |
|--------|------|-------------|
| Strict two-hop annotated-tag predicate `checkTagEligibility` with the EN vocabulary MISSING, PERMISSION, UNAVAILABLE, MALFORMED, TRANSPORT, LIGHTWEIGHT, TAG-IDENTITY, SAFE-02, ELIGIBLE | tools/release-close/eligibility.js (tightened) | 09-07 production decision path, Phase 10 live preflight, Phase 12 runbook |
| Exact-code eligibility matrix plus repeat-run and instance-isolation proofs | tools/release-close/eligibility.test.js (extended) | 09-VERIFICATION re-run, SAFE-02 traceability |
