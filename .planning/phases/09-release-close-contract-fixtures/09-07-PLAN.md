---
phase: 09-release-close-contract-fixtures
plan: "07"
type: execute
wave: 5
depends_on:
  - 09-04
  - 09-05
  - 09-06
files_modified:
  - tools/release-close/release-close.js
  - tools/release-close/evidence.test.js
  - .planning/phases/09-release-close-contract-fixtures/COVERAGE.md
autonomous: true
gap_closure: true
requirements:
  - OPS-01
  - OPS-02
  - SAFE-02
  - SAFE-04
estimate:
  tokens: 36000
  raw_tokens: 24000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - The production decision path executes all five declared reads through the injected client in the fixed order, so no read is bypassed and the fake-to-real swap in Phase 10 needs no rewrite
    - All matching Release records and all matching Milestone records reach the classifier with their stable identifiers intact; no array is collapsed to its first element
    - The classifier receives the real CI evidence carried by the snapshot; no green CI state is synthesized from bare run identifiers and no absent evidence defaults to green
    - The mutation count reported by verify and plan is the client's measured count, never a literal, and a non-zero measurement is refused by the shared invariant before anything is rendered
    - Importing the CLI module runs no verb, prints no usage, and does not terminate the importing process
    - Two independent decision runs on the same frozen input produce byte-identical output, and independent clients share no call log and no counter
    - No plan output ever unlocks apply while the classifier is blocked
  artifacts:
    - tools/release-close/release-close.js
    - tools/release-close/evidence.test.js
    - .planning/phases/09-release-close-contract-fixtures/COVERAGE.md
  key_links:
    - release-close.js decide is the single production seam, so every one of the five reads, the CI evidence, and the measured counter enter the decision from that one place
    - the evidence builder is exported so tests exercise the production normalization instead of re-implementing it, which is what let the duplicate and conflict states collapse unnoticed
    - the canonical `ci` block the builder copies through is owned by 09-05 task 1 in tools/release-close/fixtures/reference.json, and none of the five declared reads returns CI, so the builder copies that frozen block verbatim and never constructs, defaults, or infers one; `plan --json` stays green on the frozen baseline only because that block exists and is unblocking. The builder reads `reference.json` as the client-shaped snapshot it is, taking `releases` from the `getReleaseByTag` payload and `milestones` from the `listMilestones` payload, so no helper here ever reads a flat `milestones` list out of that file
    - the plan-level `applyLiberado` field has exactly one meaning across the phase — eligible tag with no blocking state — and the reconciliation seam that plan 09-09 adds reports a differently named `writeProposed` field, so a completed no-op reporting `applyLiberado` true here and `writeProposed` false there is a consistent pair rather than a contradiction
    - COVERAGE.md restates the no-live-integration boundary against the widened production path, keeping the Phase 10 milestone statement honest
  prohibitions:
    - no live GitHub, network, or subprocess call from any non-test source under tools/release-close/
    - no git ref or tag write path at porcelain, ref, or object level
    - no credential or authorization-header value read, passed as an argument, or emitted
    - no fabricated CI success synthesized from bare run identifiers
    - no ignored, swallowed, or redirected output path around the human-visible reviewed plan
    - no remote write of any kind before Phase 13; every domain mismatch keeps writeAction null
---

<objective>
Close the production half of VERIFICATION gaps 2 and 3 and the propagation half of gap 4: route all five client reads through the production decision path, carry the real evidence into the decision, report a measured mutation count instead of a literal, and make the module importable.

Purpose: `decide` calls only the three eligibility reads, the release and milestone evidence is read straight from the raw snapshot with the array discarded and only the first milestone kept, and the CI state is hard-coded green from bare run identifiers. The pure contract from plans 09-04 and 09-05 is correct, but nothing feeds it.
Output: A production decision seam that exercises all five reads, preserves and scopes the full evidence, passes real CI evidence through, and reports a measured mutation count, plus a production-adapter test suite and a refreshed coverage declaration.

Planner assumptions (deterministic edge probe, resolved here):
- OPS-01 idempotency: repeated verify and plan over the same frozen input are byte-stable and mutation-free, and no interruption in this phase can produce a remote write. Resolved by task 3.
- OPS-01 concurrency: independent decision runs share no call log and no mutation counter. Cross-process workstation locking remains Phase 11 REC-05 and is NOT claimed here.
</objective>

<execution_context>
@/home/lucas/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/lucas/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-release-close-contract-fixtures/09-CONTEXT.md
@.planning/phases/09-release-close-contract-fixtures/09-VERIFICATION.md
@tools/release-close/release-close.js
@tools/release-close/classify.js
@tools/release-close/eligibility.js
@tools/release-close/fake-client.js
@tools/release-close/fixtures/reference.json
@tools/release-close/fixtures/duplicate.json
@.planning/phases/09-release-close-contract-fixtures/COVERAGE.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Import-safe CLI entrypoint with injected output streams and an exit code instead of process termination</name>
  <files>tools/release-close/release-close.js</files>
  <read_first>tools/release-close/release-close.js lines 15-20, 230-269, and 394-419 (the top-level await on main plus process.exit that terminates any importer, and the three renderers that write straight to process.stdout); .planning/phases/09-release-close-contract-fixtures/09-REVIEW.md WR-04 (importing the module printed usage and exited zero before the importing statement could continue, making the exported plan helpers untestable); tools/release-close/safe04.test.js lines 149-163 (the existing helper that spawns the CLI as a child process, which must keep working after the entrypoint changes)</read_first>
  <behavior>
    - case: importing the module in another process -> no usage text, no exit, the importing process continues
    - case: invoking the module as the entry script with no verb -> usage on the output stream, exit code zero
    - case: invoking with an unknown verb -> usage on the error stream, exit code two
    - case: invoking verify on the frozen baseline -> exit code zero, nothing printed to the error stream
    - case: importing the module and calling the exported entry with an injected output sink -> output lands in the sink, the real process streams stay untouched
  </behavior>
  <action>
Wire ONE path end to end: a module that can be imported for its exported helpers without running the CLI.

In `release-close.js`, stop executing `main` at import time. Detect the entry-script case by comparing the module's own URL against the resolved entry path, and only in that case call the exported entry and assign its return value to `process.exitCode` instead of terminating the process. Export the entry function so a test or a downstream module can drive a verb directly. Keep the existing behavior of running as a script byte-for-byte from an operator's point of view: the same verbs, the same usage text, the same exit codes, the same stdout and stderr destinations.

Thread the output streams through instead of reaching for the process globals. Give the entry function and every renderer an output and an error stream taken from an argument, defaulting to the real process streams when the entry runs as a script, so a caller that passes its own sinks sees everything in the sinks and the real streams stay empty. Do not change the wording of the usage text, the refusal reasons, or the plan text, and do not change which verb exits with which code, because the existing SAFE-04 suite and the operator runbook depend on both.

Do not print anything at import time, including a deprecation or banner line. Do not replace the exit code with a thrown error for ordinary refusals: inelegible, blocked, and non-interactive paths must keep returning their codes. Do not add a new verb, and do not make importing the module a supported way to run a verb silently.

Avoid: `process.exit` anywhere in the module, because it would still truncate buffered output on the piped paths the SAFE-04 suite exercises. Avoid reading `process.argv` at import time.
  </action>
  <verify>
    <automated>node --test "tools/release-close/*.test.js" && node --input-type=module -e "const m = await import('./tools/release-close/release-close.js'); if (typeof m.buildClosePlan !== 'function') { throw new Error('helper ausente'); } console.log('import-safe');"</automated>
    <fails_when>either command exits non-zero, or the import probe prints usage text, or any existing CLI test regresses, or the imported module terminates the probing process before the final console output</fails_when>
  </verify>
  <done>Importing the CLI module produces no output and no termination, the exported helpers are reachable, and running the module as a script keeps the same verbs, text, and exit codes as before.</done>
  <reversibility rating="reversible">Entrypoint shape is local to one module and fully covered by the existing suite; reverting is a single-file change with no downstream consumer yet.</reversibility>
  <acceptance_criteria>Source asserts release-close.js has no top-level main invocation, no process.exit call, and no renderer that writes to the process stream globals directly. Behavior asserts the import probe reaches its final output, the no-verb invocation still returns zero with usage on the output stream, and the unknown-verb invocation still returns two with usage on the error stream.</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Five-read production decision path with scoped evidence, real CI evidence, and a measured mutation count</name>
  <files>tools/release-close/release-close.js, .planning/phases/09-release-close-contract-fixtures/COVERAGE.md</files>
  <read_first>tools/release-close/release-close.js lines 151-171, 181-228, and 272-284 (the raw-snapshot evidence helper that 09-05 task 1 gave the five-key contract, the plan builder that still hard-codes the mutation literal, and the decide function that calls only the three eligibility reads); tools/release-close/classify.js as rewritten by 09-05 (the required target and ci keys, the eleven blocking ciCode families, and the MISSING plus COMPLETE_NOOP completed no-op the decision must surface); tools/release-close/client.js as rewritten by 09-06 (the shared zero-mutation invariant this path must call before rendering); tools/release-close/fixtures/reference.json (the one snapshot the CLI loads, which 09-05 task 1 gave the canonical ci block and the target block and which therefore owns every value this builder emits on the frozen baseline); tools/release-close/fixtures/duplicate.json and conflicting.json (two records each for the same tag, the multi-record release payload this task must normalize without collapsing); tools/release-close/fake-client.js (the five read identities and the payload each one serves from the snapshot)</read_first>
  <behavior>
    - case: production decision on the frozen reference snapshot -> the client call log shows all five reads in the fixed order
    - case: the duplicate state fixture through the production path -> DUPLICATE, both release identifiers retained
    - case: the conflicting state fixture through the production path -> CONFLICTING, both release identifiers retained
    - case: a snapshot carrying releases for other versions alongside the target -> the decision reason names only the target records and the retained-unrelated field lists the others
    - case: a snapshot carrying two Milestone records naming the target -> both numbers retained
    - case: the frozen reference snapshot with its canonical ci block -> the classifier is not blocked by CI and the decision carries that block unchanged
    - case: a deep clone of that block with one conclusion set to cancelled -> FAILED with ciCode CI-CANCELLED and applyLiberado false
    - case: a snapshot with no ci block at all -> the classifier's contract violation surfaces as the existing invalid-input refusal, never as a green run
    - case: the completed no-op fixture through the production path -> code MISSING with the exact outcome literal COMPLETE_NOOP and applyLiberado true
    - case: any decision -> the reported mutation count equals the client's measured count, never a literal
    - case: a client whose measured count is non-zero -> the shared invariant refuses and nothing is rendered
  </action>
  <action>
Make the production seam feed the pure contract its real evidence, in `release-close.js`, and update the coverage declaration alongside it. Name the source of every value the builder emits, because the classifier now rejects any evidence it cannot source, and because none of the five declared reads returns CI.

Replace the raw-snapshot evidence helper with an exported production evidence builder that takes the responses actually returned by the five client reads plus the requested version, the expected SHA, and the snapshot's frozen `ci` block, and emits exactly the five-key snapshot contract the classifier now requires. The source of each value is fixed and named here. `target.version` and `target.expectedSha` come from the resolved version and SHA the CLI already computes, which default to the `version` and `expectedSha` fields the frozen reference fixture carries. `ci` is a byte-for-byte copy of the snapshot's frozen `ci` block, which plan 09-05 task 1 owns in `fixtures/reference.json` and which is the only CI source in the repository; the builder never builds, defaults, or infers one. `releases` is the `data` payload of the `getReleaseByTag` response, normalized to a list where a single record becomes a one-element list and an array is retained whole, so a duplicate or conflict payload survives intact. `milestones` is the `data` array of the `listMilestones` response, retained in full. `closeMarkers` is the empty list the phase has always used, because the marker vocabulary belongs to the Phase 11 reconciliation. Never select the first array element, never synthesize a value the five reads did not return, and never drop a record that failed the target filter: records that do not match the target belong in a retained-unrelated field so the operator can see what was ignored.

Change the decision function so it builds its client once, runs all five declared reads through it in the fixed order, and feeds those responses to the exported evidence builder before classification. Pass the requested version and expected SHA into the classification call so target scoping actually happens in production rather than only in unit tests. Keep the three eligibility reads first and in their existing order, because the ordering assertion in the eligibility suite depends on it.

Remove the synthesized CI state entirely: no object whose state is a hard-coded green word may exist in this module, and no path may fall back to a default `ci` block when the snapshot carries none. The CI-blocked outcome is state `FAILED`, which the blocking list already contains, so the apply-release formula is false while CI is unproven without any new blocking code. The release formula keeps its existing shape and keeps its existing name: `applyLiberado` means an eligible tag with no blocking state, and MISSING is not one of the blocking codes, so a finished target reports `applyLiberado: true` there. That meaning is final and is not re-derived anywhere else; the reconciliation seam added by plan 09-09 owns a separate field named `writeProposed`, and the two must never be derived from one another. The classification's `outcome` field is carried through into the plan object unchanged, so a finished target is reported to the operator as the exact pair `MISSING` plus `COMPLETE_NOOP` rather than as a seventh state.

Replace the hard-coded mutation literal with the client's measured count. Report the measured value in the plan object, in the plan text, and in the verify output, and call the shared zero-mutation invariant from 09-06 before rendering anything, so a non-zero measurement refuses the run instead of being printed. Do not keep a fallback that renders zero when the measurement is missing; a missing measurement is a contract failure and must refuse.

In `COVERAGE.md`, restate the no-live-integration boundary against the widened path: all five reads are still served by the programmable fake and the throwing gh stub, no production source or test issues a live GitHub call, the CLI now executes the full five-read seam, the CI evidence still enters as a frozen fixture block rather than a live CI query, and the fail-closed stub remains the wiring point for Phase 10. Keep the declared surface table and the milestone statement; do not add an API matrix, because this phase still has no external API integration.

Avoid: reading Release or Milestone data straight from the raw snapshot again, which is the exact shape that hid the dropped duplicates. Avoid reading CI from anywhere but the snapshot's frozen block, and avoid keeping a green CI default in any form, including as a default parameter. Avoid changing the eight ordered plan steps, the PT-BR reason wording of the refusals, or the exit codes.
  </action>
  <verify>
    <automated>node --test "tools/release-close/*.test.js" && node tools/release-close/release-close.js plan --json</automated>
    <fails_when>either command exits non-zero, or the plan JSON lacks the target block, the copied ci block, or the retained identifier lists, or the emitted mutation value does not equal a measured count, or any hard-coded green CI state survives in the source, or the whole tool suite regresses</fails_when>
  </verify>
  <done>The production path performs all five reads, hands the classifier scoped and complete evidence with stable identifiers and the frozen ci block copied through unchanged, reports a measured mutation count that a non-zero measurement refuses, surfaces the completed no-op as the exact pair MISSING plus COMPLETE_NOOP, and the coverage declaration matches the widened path.</done>
  <reversibility rating="costly">The exported evidence builder, the target block, and the measured-count plumbing become the contract Phase 10 fills from live reads; reshaping them later would rewrite the Phase 10 client and the Phase 11 reconciliation input.</reversibility>
  <acceptance_criteria>Source asserts release-close.js contains no hard-coded green CI state and no literal mutation value, asserts the evidence builder is exported and returns a target block plus retained identifier lists, and asserts the shared invariant is called before any render. Behavior asserts the nine cases above including the cancelled-evidence block and the non-zero-measurement refusal. CLI asserts plan --json carries the target, the retained evidence, the CI block, and the measured count.</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Production-adapter suite proving the five reads, target scoping, CI blocking, and measured idempotency</name>
  <files>tools/release-close/evidence.test.js</files>
  <read_first>tools/release-close/release-close.js as rewritten by tasks 1 and 2 (the exported entry, the exported evidence builder, the client factory seam, and the exact fields the plan object now carries); all files under tools/release-close/fixtures/ (the frozen scenarios this suite drives, including the two new ones from 09-05); tools/release-close/fake-client.js as rewritten by 09-06 (the frozen client, the per-instance call log, and the uneditable measured count); tools/release-close/classify.test.js as rewritten by 09-05 (the exact-code and exact-family assertion style this suite must match)</read_first>
  <action>
Add `tools/release-close/evidence.test.js`, a production-adapter suite that drives the real seam instead of a hand-assembled call log, and change no production source in this task.

Drive the exported decision seam directly for every frozen scenario, so the test exercises the same code path the CLI runs. For each scenario assert: the client's call log lists all five reads in the fixed order with sequence numbers one through five; the eligibility decision and the classification decision are the exact expected values rather than merely present; every matching Release identifier and Milestone number survives into the decision; and writeAction is null on both decisions. Cover the duplicate, conflicting, partial, missing, completed no-op, unrelated-history, concurrent, and failed fixtures, and for the completed no-op assert the exact pair `code === 'MISSING'` with `outcome === 'COMPLETE_NOOP'`, the same literal plan 09-05 task 2 fixed, so the two plans cannot drift apart.

Build each scenario's client-shaped snapshot from the frozen reference baseline by replacing its `release` and `milestones` payloads with the records the state fixture already carries, including the two-record payloads of the duplicate and conflicting fixtures; never hand-type a SHA, a run identifier, or a note string. Add the target-scoping cases the same way, appending records for other versions to the frozen baseline, and assert that the decision reason names only the target records while the retained-unrelated field lists the others. Add the CI cases by deriving the canonical block from the frozen reference fixture once and mutating a deep clone per family, asserting that each blocked family forces the apply-release flag false and names its family. Add one case that removes the `ci` block entirely and asserts the run refuses with the invalid-input refusal rather than defaulting to green.

Add the measured-counter cases: assert that the mutation value in the plan equals the client's measured count for every scenario, and add a case that arms the fake's trap so the measured count is non-zero and assert the run refuses with the shared invariant's PT-BR reason instead of rendering output. Add the idempotency case: run the exported plan builder twice over the same frozen snapshot and assert the two rendered payloads are byte-identical strings and the measured count is zero both times. Add the instance-isolation case: build two clients, run the decision on the first, and assert the second still has an empty call log and a zero count, with a PT-BR comment recording that cross-process workstation locking is Phase 11 REC-05 and must never be cited from this suite.

Add one import-safety case that imports the CLI module and asserts the exported helpers are reachable and that nothing was printed during import.

Avoid: re-implementing the evidence normalization inside the test, which is what allowed the duplicate and conflict states to collapse while the suite stayed green; always call the exported builder or the exported entry. Avoid constructing a `ci` block in the test instead of cloning the frozen one, because that would let the suite pass against evidence no plan owns. Avoid adding a runner, an assertion library, or any `dependencies` entry to `tools/release-close/package.json` (D-02, D-06). Avoid hand-typing frozen SHAs and run identifiers; derive them from the fixtures (D-08).
  </action>
  <verify>
    <automated>node --test tools/release-close/evidence.test.js && node --test tools/release-close/</automated>
    <fails_when>either run exits non-zero, or any scenario asserts only the presence of a field instead of its exact expected value, or the completed no-op is not reported as MISSING plus COMPLETE_NOOP, or the call log ever shows fewer than five reads, or a blocked CI family leaves the apply-release flag true, or the whole tool suite regresses</fails_when>
  </verify>
  <done>Every frozen scenario is proven through the production seam with exact values, all five reads are observed in order, target scoping and CI blocking are proven with the frozen evidence the repository owns, and the reported mutation count is the measured one with a refusal when it is not zero.</done>
  <acceptance_criteria>Test asserts eight fixture scenarios with exact eligibility and classification values, including the completed no-op as the exact pair MISSING plus COMPLETE_NOOP, asserts five ordered reads per scenario, asserts retained identifiers and the retained-unrelated field, asserts the CI blocking outcomes with the apply-release flag false, asserts a missing ci block refuses instead of defaulting green, asserts the measured count equals the client count and that an armed trap refuses, asserts byte-identical repeat rendering, and asserts instance isolation and import safety. Source asserts no production file is modified by this task.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client read responses to the production decision | All five remote-shaped responses cross here and must be normalized identically to the tested path |
| measured counter to rendered operator output | The reported number crosses into what an operator reads, so it must be a measurement, not a constant |
| module import to host process | Importing the module crosses into a host that did not ask for a CLI run; it must have no side effect |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-07-01 | Tampering | synthesized green CI state in the production path | high | mitigate | The hard-coded green state is deleted and no default ci block exists; the classifier receives the frozen ci block copied verbatim from the snapshot, a missing block surfaces the contract violation as an invalid-input refusal, and every blocked family is asserted to force the apply-release flag false |
| T-09-07-02 | Repudiation | dropped duplicate and conflict evidence | high | mitigate | The production evidence builder retains every record's stable identifier and the full Milestone list, and the suite asserts both duplicate and conflicting fixtures reach their exact states through the real seam |
| T-09-07-03 | Repudiation | literal mutation count in output | high | mitigate | The literal is removed, the value reported is the client's measured count, the shared invariant refuses a non-zero or malformed measurement before rendering, and an armed-trap case asserts the refusal |
| T-09-07-04 | Denial of service | module import terminating the host | medium | mitigate | The entrypoint runs only in the entry-script case, assigns an exit code instead of terminating, and an import-safety case asserts the importing process survives and sees no output |
| T-09-07-05 | Information disclosure | widened production output | low | mitigate | Output remains limited to decision fields, the ordered plan, and the measured count; the secret-hygiene scan in the SAFE-04 suite walks every captured output and must stay green |
| T-09-07-SC | Tampering | npm installs | high | mitigate | No install step exists in any task of this plan; the zero-dependency manifest and the node:test runner are unchanged |

</threat_model>

<verification>
`node --test tools/release-close/evidence.test.js` green; `node --test "tools/release-close/*.test.js"` green with no regression in the eligibility, classifier, gate, no-write, or canary suites; `node tools/release-close/release-close.js verify --json` and `plan --json` both exit zero on the frozen baseline, the plan JSON carries the target block, the frozen ci block, the retained identifier lists, and a measured count, and the completed no-op appears as the exact pair MISSING plus COMPLETE_NOOP; `cd backend && npx vitest run` and `cd frontend && npm run build` both stay green as collateral; `git status` shows changes confined to `tools/release-close/release-close.js`, `tools/release-close/evidence.test.js`, and the phase directory.
</verification>

<success_criteria>
The production seam no longer bypasses the contract: all five reads run through the injected client, every matching Release and Milestone identifier reaches the classifier with the target scoping applied, every value the builder emits has a named source in the repository and CI evidence is the frozen block the snapshot already carries rather than anything synthesized, a non-green or absent block prevents any apply release, the completed no-op surfaces as the exact pair MISSING plus COMPLETE_NOOP, the reported mutation count is measured and a non-zero measurement refuses the run, and the module is importable without side effects.
</success_criteria>

<output>
Create `.planning/phases/09-release-close-contract-fixtures/09-07-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Symbol | File | Consumed by |
|--------|------|-------------|
| Exported production evidence builder, five-read decision seam, measured-counter plumbing, import-safe entrypoint, and the completed no-op surfaced as MISSING plus COMPLETE_NOOP | tools/release-close/release-close.js (rewritten) | 09-08 apply gate wiring, 09-09 reconciliation seam, Phase 10 preflight, Phase 12 runbook |
| Production-adapter suite with exact-value, ordering, CI-blocking, no-op-literal, and idempotency proofs | tools/release-close/evidence.test.js | 09-VERIFICATION re-run, OPS-01 and OPS-02 traceability |
| Refreshed no-live-integration declaration for the widened path | .planning/phases/09-release-close-contract-fixtures/COVERAGE.md | Phase 10 live-client planning |
