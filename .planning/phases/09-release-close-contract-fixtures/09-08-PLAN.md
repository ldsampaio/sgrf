---
phase: 09-release-close-contract-fixtures
plan: "08"
type: execute
wave: 6
depends_on:
  - 09-07
files_modified:
  - tools/release-close/apply-gate.js
  - tools/release-close/release-close.js
  - tools/release-close/safe04.test.js
  - tools/release-close/fixtures/reviewed.json
autonomous: true
gap_closure: true
requirements:
  - SAFE-04
  - OPS-01
estimate:
  tokens: 33000
  raw_tokens: 22000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - Apply refuses when the controlling output terminal is absent, even with a live input terminal, the flag present, and the correct word typed
    - The two terminal families are attributed to the lock that actually fires: a fully piped invocation has a non-terminal input stream, so it refuses on `tty` at lock three and never reaches `output` at lock four, while the `output` lock is reachable exactly when the input terminal is live and the output stream is not; a refusal is never attributed to `output` when `tty` is what failed
    - Nothing is written until every lock from `plan` through `content` has passed: the render of the ordered plan and then of the reviewed content is the first act after the content lock and always precedes the prompt, so a refusal on `plan`, `flag`, `tty`, `output`, or `content`, and a sink that is missing or not a function, leave the control terminal completely untouched with zero sink calls and never open the prompt
    - The `sink` zero-character refusal is the single refusal family whose write-observable is sink calls rather than zero calls: it is observed with the render calls made, a reported character total of zero, and no prompt, and it is asserted as its own row so it can never be merged with a pre-render refusal
    - Apply refuses when the reviewed Release notes or Milestone completion content is absent, empty, or not the reviewed object the operator saw
    - The ordered plan is rendered first and the reviewed content second, both through the sink and both before the prompt, and the confirmation is bound to the same canonical digest that was rendered
    - The output sink must be a real sink: a missing, non-function, or zero-character sink refuses instead of silently accepting
    - End-of-input and stream errors refuse promptly without hanging and without a second prompt
    - Rendering the plan and preflighting the confirmation never mutates anything and never drifts the reviewed-content digest
    - The reviewed Release notes and Milestone completion record the digest covers come from named fields of a frozen fixture this plan owns; that fixture is client-shaped like `reference.json`, with both envelopes intact and no flat-list keys, so it is never mistaken for classifier input; no task invents operator-facing text, and a snapshot without those fields yields no reviewed object and therefore refuses
    - The lock order is exported as exactly eight entries with the original five untouched, and the flag, pipe, and no-plan refusals still report the lock they report today
    - The eight ordered recovery steps, the PT-BR refusal reasons, and the phase's fail-closed outcome are unchanged
  artifacts:
    - tools/release-close/apply-gate.js
    - tools/release-close/release-close.js
    - tools/release-close/safe04.test.js
    - tools/release-close/fixtures/reviewed.json
  key_links:
    - release-close.js buildClosePlan is the only producer of the reviewed content, so the digest the gate binds to is computed once from the same immutable object the operator reads
    - apply-gate.js exports the single canonicalReviewedDigest function, so the plan builder in task 2 and the gate's prompt-time recomputation in task 1 can never compute two different digests over the same content
    - the reviewed Release notes and Milestone completion record the digest covers come from named fields of tools/release-close/fixtures/reviewed.json, which this plan owns; no task invents operator-facing text, and a snapshot without those fields produces no reviewed object and therefore refuses on the content lock
    - apply-gate.js renders the ordered plan and then the reviewed content, in that fixed order, only after every visibility and content lock has passed, which is what makes "reviewed" an observable precondition rather than a claim
    - safe04.test.js consumes the production evidence builder exported by 09-07, so the SAFE-04 proof exercises the real seam instead of a local copy that collapsed states
    - the three new locks are placed after `tty` and before `prompt` in the fixed order, so the existing `flag`, `tty`, and no-plan refusals keep returning the same lock they return today without any new input, while the four `confirmApply` cases that used to reach the answer lock must now supply an `outputIsTTY` boolean, a byte-reporting sink, and reviewed content, because the render moved to after the content lock
    - because `tty` is lock three and `output` is lock four, a spawned child process with piped stdin and stdout can never reach the `output` lock — its first failing lock is `tty` — so the suite proves the `output` lock through an injected gate call with a live input terminal and a non-TTY output stream, and the `terminal interativo` PT-BR phrase stays where it always lived, in the `tty` reason the shipped pipe assertions match on
    - every SAFE-04 case whose observable expectation changes with that render position is enumerated by name in task 1, so no call site is left asserting a pre-migration expectation behind this change
  prohibitions:
    - no live GitHub, network, or subprocess call from any non-test source under tools/release-close/
    - no git ref or tag write path at porcelain, ref, or object level
    - no credential or authorization-header value read, passed as an argument, or emitted
    - no fabricated CI success synthesized from bare run identifiers
    - no ignored, swallowed, or redirected output path around the human-visible reviewed plan
    - no remote write of any kind before Phase 13; the reviewed digest is preparation for Phase 13, never a write path
---

<objective>
Close VERIFICATION gap 5 and the overlapping advisories CR-01, WR-05, WR-08, and WR-03: require a controlling output terminal, bind the confirmation to immutable reviewed Release and Milestone content through a canonical digest, render that content with the ordered plan before the prompt, and make end-of-input refuse instead of hanging.

Purpose: With a live input terminal and stdout redirected to a file, the operator answered the confirmation word without ever seeing the plan, and the gate reported that confirmation happened "after the plan". The plan carries no Release notes, no Milestone completion record, and no digest, so the gate can be reused by Phase 11 without binding an operator's approval to any content.
Output: A gate exporting the fixed eight-lock order, an immutable reviewed-content object with a canonical digest computed by one shared function, a prompt that settles on end-of-input, a frozen fixture that owns the reviewed text, and a SAFE-04 suite that uses the production evidence builder and asserts exact codes.
</objective>

<execution_context>
@/home/lucas/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/lucas/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-release-close-contract-fixtures/09-CONTEXT.md
@.planning/phases/09-release-close-contract-fixtures/09-VERIFICATION.md
@tools/release-close/apply-gate.js
@tools/release-close/release-close.js
@tools/release-close/safe04.test.js
@tools/release-close/fixtures/complete.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Gate with an output-terminal lock, a real-sink lock, a reviewed-content lock bound to a canonical digest, the render moved to after the content lock, and every SAFE-04 case whose expectation that move changes migrated in the same task</name>
  <files>tools/release-close/apply-gate.js, tools/release-close/safe04.test.js, tools/release-close/fixtures/reviewed.json</files>
  <read_first>tools/release-close/apply-gate.js lines 22-84 (the exported APPLY_LOCKS list of five entries, the refusal helper, the no-op default write sink, the current lock order, and the return shape that must stay compatible with its caller) — note line 49, where the current gate writes the plan before the flag lock, which is exactly the position this task moves; tools/release-close/safe04.test.js lines 242-396 (the eleven existing confirmApply cases) and lines 434-442 (the D-15 visibility case that currently requires the eight steps to reach piped stdout) and lines 482-496 (the captured gate results the credential scan walks); .planning/phases/09-release-close-contract-fixtures/09-REVIEW.md CR-01 (a live input terminal with redirected output still reached the confirmation branch) and WR-08 (the gate can confirm with no reviewed content ever supplied); tools/release-close/fixtures/reference.json (the baseline snapshot this plan's new fixture must be shaped like: a client-shaped snapshot whose `release` and `milestones` values are envelopes, plus the canonical `ci` block and the `target` block plan 09-05 added — keep both envelopes and add nothing else); tools/release-close/release-close.js lines 181-228 (the plan object the reviewed content and digest will be attached to in task 2)</read_first>
  <behavior>
    - case: live input terminal, absent output terminal, flag present, correct word, reviewed content present -> refusal naming the output lock, with nothing written and nothing asked
    - case: live input terminal and live output terminal, but the sink absent or not a function -> refusal naming the sink, with nothing written and nothing asked
    - case: a sink that reports zero characters written across the render -> refusal naming the sink, after the render and before the prompt
    - case: reviewed content missing, blank, or carrying no Release notes and no Milestone completion record -> refusal naming the content lock, with nothing written and nothing asked
    - case: reviewed content whose digest does not match the digest recomputed at prompt time -> refusal naming the content lock, with nothing written and nothing asked
    - case: any refusal on `plan`, `flag`, `tty`, `output`, or `content`, and a `write` that is missing or not a function -> the recording sink observed zero calls and the prompt was never opened
    - case: a `write` that is a function but reports zero characters across the render -> the recording sink observed both render calls, the sum of the characters it received is zero, and the prompt was never opened; this is the one refusal that happens after something was written
    - case: all locks satisfied -> the ordered plan is written first and the rendered reviewed content second, both through the sink, both before the prompt, and the returned decision echoes the digest
    - case: any refusal -> the returned decision names exactly which lock refused, the first one that failed, in the fixed order
    - case: the exported lock list -> exactly plan, flag, tty, output, sink, content, prompt, answer, in that order
    - case: the existing flag, pipe, and no-plan cases -> still return the flag, tty, and plan locks they return today
    - case: a fully piped CLI apply with `--yes`, so the input stream and the output stream are both non-terminal -> refuses on the tty lock, which is lock three and therefore fails before lock four, names the existing `terminal interativo` clause in its PT-BR reason, writes nothing to stdout, and never opens the prompt
    - case: a CLI apply with a live input terminal and its output redirected to a file -> refuses on the output lock, calls the recording sink zero times, and never opens the prompt
  </behavior>
  <action>
Extend the five-entry lock list into a content-bound, human-visibility-bound gate in `apply-gate.js`, add the frozen fixture that owns the reviewed text, move the render to a single declared position, and migrate every SAFE-04 case that position changes, all in this task so the suite is green before the next task starts.

Publish the final lock order as the exported `APPLY_LOCKS` list with exactly these eight entries in this order: `plan`, `flag`, `tty`, `output`, `sink`, `content`, `prompt`, `answer`. The five existing entries keep their existing relative order; the three new locks are appended between `tty` and `prompt`. That placement is deliberate and is what keeps the existing behaviour intact: a missing flag still refuses on `flag`, a pipe still refuses on `tty`, and a missing plan still refuses on `plan`, exactly as the shipped suite asserts today. Refusals name exactly one lock, the first that failed, so the operator is told one thing to fix at a time and the order is assertable.

Declare the render position once, here, and let every other instruction in this plan follow from it. The render happens after the `content` lock and before the `prompt` lock, and it is the first act after the content lock: the gate evaluates `plan`, `flag`, `tty`, `output`, `sink`, and `content` in that order writing nothing at all, and only when all six have passed does it call the sink — first with the ordered plan text, then with the rendered reviewed content — and only then does it open the prompt. The render is not itself a lock, so `APPLY_LOCKS` still has exactly eight entries. The consequence the rest of this plan depends on: a refusal on `plan`, `flag`, `tty`, or `output`, and a sink that is missing or not a function, all observe zero sink calls and never open the prompt, and the one refusal that happens after anything was written is the `sink` zero-character refusal below, which is observed with sink calls whose reported character count is zero. Delete the current `write(planText)` call that sits ahead of the flag lock, because that call is what let an operator "review" a plan on a terminal the gate was about to refuse, and never reintroduce a pre-lock write.

Add an output-terminal lock. The gate receives an explicit `outputIsTTY` boolean from the caller and refuses on it when it is not exactly true, before writing anything and before asking anything, as lock number four. The PT-BR reason states that the plan was not shown on a live terminal and that a redirected or piped output cannot be reviewed by a human. This is the fix for the reproduced bypass where a live input terminal with a redirected output still confirmed, and because the render moved below this lock that same case now also writes nothing at all. Name the two reachable terminal families explicitly, because they fail on different locks and no case in this plan may attribute one to the other. A **fully piped** invocation — the child process the suite spawns, whose input stream and output stream are both non-terminal — fails on lock three `tty` and never reaches lock four, so its refusal carries the existing `terminal interativo` clause in its PT-BR reason and its write-observable is an empty stdout. The `output` lock is reachable exactly when the input terminal is live and the output stream is not, which is the case the lock exists for. Never write a case, a reason, or a comment claiming that a fully piped apply refuses on `output`.

Add a real-sink lock. Remove the silent no-op default. A missing or non-function `write` refuses on `sink` at lock five, before any render. The injected `write` must report how many characters it wrote, so the gate can decide after the render whether the plan actually reached a terminal: if the total reported across the two render calls is zero, refuse on `sink` with a PT-BR reason naming the swallowed plan, and do so before opening the prompt. Because `process.stdout.write` returns a boolean rather than a count, task 2 wraps it in an adapter that reports the character count; until then the gate's own tests inject a counting sink. Keep the sink a pure injected function; the gate must not reach for any process stream itself.

Add a reviewed-content lock. The gate now requires an immutable reviewed object plus a canonical digest, and validates that the Release notes and the Milestone completion record are both present and non-blank and that the digest recomputed from the content at prompt time is byte-identical to the digest supplied with the plan; a mismatch refuses with a PT-BR reason stating the reviewed content changed between rendering and confirmation. This lock is evaluated before the render, from the content itself, so the gate knows before it writes anything whether it has anything legitimate to show. The render then shows the operator the actual reviewed content, not only its digest: the plan text first, the Release notes and the Milestone completion record second, both before the prompt. Echo the digest in the returned decision so a later executor can bind its write to the same object.

Own the reviewed text in a new frozen fixture, `tools/release-close/fixtures/reviewed.json`, so no task has to invent operator-facing content. Shape it as a client-shaped snapshot like `reference.json`: the same three tag envelopes taken verbatim from that file, the same `version` and `expectedSha`, the same `target` block and the same canonical `ci` block, the same `release` envelope whose `data` carries the frozen identifiers plus a `notes` string in PT-BR, and the same `milestones` envelope whose single data record carries a `number`, `state` equal to `closed`, zero open issues, and a `completionRecord` string in PT-BR. Those two strings, `release.data.notes` and `milestones.data[0].completionRecord`, are the only sources of the reviewed Release notes and Milestone completion record in the repository. This fixture is client-shaped, not flat classifier evidence: keep both envelopes as envelopes, add no `releases` or `closeMarkers` key, and never reuse it as a classifier input. Export one `canonicalReviewedDigest(reviewed)` function from the gate that serializes a fixed field order — `version`, `expectedSha`, `commitSha`, `releaseNotes`, `milestoneCompletionRecord`, each as a `name` plus `length` plus `value` triple so no separator can be forged — and returns a hex digest; task 2 calls this same exported function from the plan builder, so the rendered digest and the recomputed digest can never diverge.

In `safe04.test.js`, migrate every call site the new render position changes, in this same task, because the suite must be green before the next task starts. The list is complete; work through it exactly as written.

Cases that need no new input, because they refuse at `plan`, `flag`, or `tty` before the sink lock, and that must be strengthened rather than rewritten: `recusa quando nenhum plano foi renderizado para revisão` still returns `plan`; `a CLI recusa sem a flag --yes antes de abrir qualquer prompt` is untouched; `recusa sem a flag --yes e nomeia a fechadura ausente` still returns `flag` and now additionally receives a recording `write` and `outputIsTTY: false`, asserting the sink was called zero times and nothing was asked, which makes it the canonical proof of the render position; `recusa com entrada não interativa mesmo com a flag --yes` still returns `tty` with its existing `perguntou` deep-equal to `[]` and now also receives a recording `write` with `outputIsTTY: false`, asserting the sink was called zero times; `a CLI recusa com pipe mesmo com a flag --yes e não pergunta nada` keeps matching `terminal interativo` on stderr because that clause lives in the tty reason and the tty lock is the one a fully piped child process hits, still returns the `tty` lock, and additionally asserts stdout is empty, since a piped apply now renders nothing before any lock fails. Do not migrate that case to the output lock: the spawned child has a piped input stream, so the output lock is unreachable from it.

Cases that reach the prompt and therefore must be given the three new inputs — `outputIsTTY: true`, a recording `write` that reports the number of characters it received, and reviewed content plus its digest loaded from `fixtures/reviewed.json` through the exported digest function: `recusa quando a confirmação digitada não é sim` still returns `answer` for each wrong word; `confirma somente com flag, terminal interativo e a digitação sim` still returns `confirmed: true` with `lock: null`; `grava o plano antes de qualquer pergunta` now asserts the recorded event order is exactly the plan render, then the reviewed-content render, then the prompt; `verifica flag, depois terminal, depois resposta, nessa ordem` keeps its `flag` and `tty` sub-cases unchanged, migrates only the wrong-answer sub-case so it still returns `answer`, and has its `APPLY_LOCKS` assertion updated to the exact eight-entry list; `avaliações sequenciais do portão não compartilham estado` keeps the `flag` sub-case unchanged and migrates both approved sub-cases, so they still return `confirmed: true` and stay deeply equal to each other.

The one case whose expectation inverts, and this is the direct consequence of the declared render position: `grava o plano antes de perguntar mesmo quando vai recusar` currently asserts that the plan was written even when the run is going to refuse. Under the new position nothing is written before the locks pass, so migrate it to assert that the recorded event list is exactly empty, that the returned lock is `flag`, and that nothing was asked. Do not weaken it into a comment; it is now the assertion that an operator is never shown a plan for an apply that the gate was always going to refuse.

The two captured `confirmApply` results inside the credential-scan test, at lines 482-496, are part of the same migration. The first two refuse at `flag` and `tty` and need no change; the third, which is a live input terminal with a wrong answer, and the fourth, which is a live input terminal with the correct word, both need `outputIsTTY: true`, a recording `write`, and reviewed content from `fixtures/reviewed.json`, so the positive entry actually reaches the confirmation path and actually renders content. Push the rendered plan text and the rendered reviewed content of that positive entry into the scanned output list, so the credential scan keeps covering everything the gate now writes.

The D-15 visibility case, `a CLI mostra o plano completo antes de recusar o apply por pipe`, cannot stay as it is written: a piped apply now renders nothing, which is the whole point of the terminal locks. Split it into two cases that state the real invariant. The first drives `confirmApply` directly with `outputIsTTY: true`, a recording `write`, and reviewed content, using the plan text the CLI itself renders via the existing `runCli(['plan'])` helper, and asserts that all eight ordered step ids are present in what the sink received, that the plan render came before the reviewed-content render, and that both came before the prompt. The second keeps the fully piped CLI invocation and asserts the opposite of the old expectation: a non-zero exit, the `tty`-lock reason naming `terminal interativo` on stderr, an empty stdout, and no prompt. Do not weaken that second case into an output-lock assertion: a piped child process cannot reach lock four, and a case written that way would fail for the wrong reason and hide the real ordering.

Add the six new negative cases from the behavior block in this task as well, so the new locks are proven here rather than deferred to the suite rewrite in task 3.

Do not reorder the existing five locks, and do not weaken any of them. Do not accept a digest supplied by the caller without recomputing it from the content. Do not let the gate decide anything about eligibility or classification; it only decides whether a human reviewed a specific, unchanged content. Do not add any capability that could act on the content, and do not keep the confirmation word path open when the content lock has not passed. Do not add a boolean flag that lets a caller skip the content check, the way an opt-out would defeat the whole point. Do not write anything before the content lock has passed, and do not require a plan to be rendered on a terminal the gate is about to refuse.

Avoid: rendering the reviewed content after the prompt, which would make the digest meaningless. Avoid placing the three new locks before `tty`, which would change the lock an existing pipe refusal reports. Avoid writing the reviewed text as template prose in production source: it must come from the frozen fixture field named above. Avoid requiring both "nothing is written when the output lock refuses" and "the plan is rendered when a refusal happens" — the chosen position satisfies the first and forbids the second, so a case that expects a render on any refusal is the case that has to change.
  </action>
  <verify>
    <automated>node --test tools/release-close/safe04.test.js && node --test tools/release-close/</automated>
    <fails_when>either run exits non-zero, or a live input terminal with a redirected output confirms, or a missing or blank reviewed content confirms, or a changed digest confirms, or a missing or zero-character sink confirms, or the exported APPLY_LOCKS list is not exactly the eight entries in the fixed sequence, or the flag, tty, or no-plan case stops returning its original lock, or a fully piped CLI apply stops refusing on the tty lock, or a live-input apply with a redirected output stops refusing on the output lock, or a pre-render refusal on plan, flag, tty, output, or content, or a missing or non-function sink, ever calls the sink at all, or the zero-character sink case reports zero sink calls instead of a call with zero characters received, or the plan render does not precede the reviewed-content render and both do not precede the prompt once every lock has passed, or the whole tool suite regresses</fails_when>
  </verify>
  <done>Apply refuses when the output is not a live terminal, on a missing or zero-character sink, on absent reviewed content, and on a digest that changed between rendering and confirmation; nothing at all is written and no prompt is opened until every lock through `content` has passed, after which the ordered plan is written first and the reviewed content second and the prompt comes last; a live input terminal with a redirected output refuses on the `output` lock while a fully piped invocation refuses on the earlier `tty` lock, each naming the lock that actually fired; the fixed order is exported as eight locks; the reviewed text has one named source in a client-shaped frozen fixture; and every SAFE-04 case that this render position changes is green in this task.</done>
  <reversibility rating="costly">The reviewed-content object, the shared canonical digest function, and the eight-lock order are what Phase 13 binds the live write to; changing the digest scheme or the lock order after the runbook quotes it would invalidate the operator procedure.</reversibility>
  <acceptance_criteria>Source asserts the gate has no no-op default sink, asserts it recomputes the digest through the single exported canonicalReviewedDigest rather than trusting the supplied one, asserts the exported APPLY_LOCKS list is exactly plan, flag, tty, output, sink, content, prompt, answer, asserts the gate performs its only write calls between the content lock and the prompt lock and nowhere earlier, and asserts fixtures/reviewed.json carries both envelopes, non-blank notes and completionRecord strings, the target block, and the canonical ci block while carrying no releases or closeMarkers key. Behavior asserts each case above, including that a refusal on plan, flag, tty, output, or content and a missing or non-function sink call the sink zero times and never open the prompt; that the zero-character sink refusal is the single case observed with sink calls whose reported character total is zero and with no prompt; that a live input terminal with a redirected output returns the output lock; that a fully piped CLI invocation returns the tty lock with the `terminal interativo` reason and an empty stdout; that the flag, tty, and no-plan cases still return their original locks; and that once every lock passes the recorded order is plan render then reviewed-content render then prompt. Test asserts the six new refusal families are green, the inverted write-before-refusal case now asserts an empty event list, the D-15 visibility case is split into a live-terminal render case and a fully piped no-render case asserting the tty lock, and the credential scan covers the rendered plan and reviewed content of the positive entry.</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Reviewed content in the plan, output-terminal wiring, a prompt that settles on end-of-input, and the SAFE-04 cases those changes invalidate</name>
  <files>tools/release-close/release-close.js, tools/release-close/safe04.test.js</files>
  <read_first>tools/release-close/release-close.js lines 181-228 (the plan builder where the reviewed content and digest belong), 230-252 (the plan text renderer that must show the reviewed content before the prompt), 327-338 (the ask factory that resolves only from the answer callback), and 340-392 (the apply path that passes the input-terminal boolean and the output sink, which this task re-points at the output stream's own isTTY and a character-counting adapter); tools/release-close/apply-gate.js as rewritten by task 1 (the eight exported locks, the three new input names, and the exported canonicalReviewedDigest the plan builder must call); tools/release-close/fixtures/reviewed.json (the one fixture that owns the notes and completionRecord strings the reviewed object is built from); tools/release-close/safe04.test.js lines 398-443 (the plan-text assertions whose rendered output changes in this task); .planning/phases/09-release-close-contract-fixtures/09-REVIEW.md WR-05 (end-of-input after the prompt left the process alive until an external timeout)</read_first>
  <behavior>
    - case: the plan object for a snapshot carrying the reviewed fields -> carries the reviewed Release notes, the reviewed Milestone completion record, and the canonical digest over both, and the digest computed twice in a row is identical
    - case: the plan object for a snapshot with no reviewed fields -> carries no reviewed object and no digest, and the text says so in PT-BR rather than inventing text
    - case: the rendered plan text -> shows the reviewed Release notes and the Milestone completion record and the digest above the ordered steps and above the measured mutation count
    - case: apply with a live input terminal and a redirected output -> refuses on the output lock, writes nothing to the sink, and the prompt is never opened
    - case: apply where the terminal closes immediately after the prompt opens -> the run returns a refusal and does not wait
    - case: apply where the input stream emits an error after the prompt opens -> the run returns a refusal and does not wait
    - case: apply run twice over the same frozen snapshot -> the two plans carry the identical digest and the identical rendered text
  </action>
  <action>
Wire the reviewed content and the two new gate inputs into the CLI, make the prompt settle, and add the SAFE-04 cases this wiring invalidates, all in this task.

In the plan builder, build an immutable reviewed content object from what the decision actually observed. The reviewed Release notes are the `notes` string of the observed Release record and the reviewed Milestone completion record is the `completionRecord` string of the observed Milestone record; both are copied byte for byte from the snapshot payload, whose only source in the repository is the `release.data.notes` and `milestones.data[0].completionRecord` fields of `tools/release-close/fixtures/reviewed.json` that task 1 added. The object also carries the target version, the expected SHA, and the observed peeled commit, all of which the plan already resolves. When the snapshot carries no Release record or no Milestone record, or either string is blank, build no reviewed object and no digest at all: the renderer then states in PT-BR that there is no reviewed content for this target, and the gate content lock refuses. Never assemble the notes or the completion record from template prose, from the ordered step list, or from any other invented source, because a digest over invented text would bind the operator's approval to nothing.

Compute the digest by calling the `canonicalReviewedDigest` function exported by `apply-gate.js` in task 1, not by re-implementing the serialization here. Attach the reviewed object and the digest to the plan, recompute the digest a second time inside the builder, and refuse to emit a plan when the two computations disagree, so a non-deterministic digest can never reach the operator.

In the plan text renderer, show the reviewed Release notes and the Milestone completion record and the digest, above the ordered recovery steps and above the measured mutation count, so the content precedes the prompt in the visible order as well as in the byte order. Keep the eight ordered steps, their PT-BR wording, the refusal reasons, and the exit codes unchanged.

In the apply path, pass the caller's `outputIsTTY` boolean, the real output sink, the reviewed object, and the digest to the gate, using the injected streams from the import-safe entry work in 09-07 rather than reaching for the process globals. Take `outputIsTTY` from the injected output stream's own `isTTY` property, not from the input stream: the whole point of the lock is that a live input terminal plus a redirected output still refuses, so reading stdin twice would re-open the bypass. Because `process.stdout.write` returns a boolean rather than a character count, wrap it in an adapter that performs the write and returns the number of characters it received, so the gate's sink lock can tell a real terminal from a swallowed plan. Let the gate own the render order: the CLI hands it the plan text, the reviewed object, the digest, the output-terminal boolean, and the counting sink, and then does nothing until the gate returns. Do not write the plan to the output stream yourself before calling the gate, because the render belongs between the content lock and the prompt lock and writing it early is exactly the pre-lock write task 1 removed. Keep the blocked-plan branch as it is today: when the plan is not apply-released the CLI prints the plan and refuses without a gate call at all, which is a different path and is not the visibility invariant the gate owns. Keep the existing order of the remaining work: the flag, terminal, output, sink, content, and answer locks are evaluated by the gate in its fixed order, and the prompt still opens only after the content lock has passed.

In the ask factory, settle exactly once. Resolve on the answer, resolve on the interface closing without an answer, and refuse on a stream error, with a single settled flag so a late answer after a close cannot settle a second time or open a second prompt. Translate end-of-input and stream errors into the refusal shape the gate already understands, with PT-BR reasons that state the confirmation was not received, and make sure the interface is always closed on every path so the process cannot stay alive.

In `safe04.test.js`, add in this same task the four cases this wiring creates, so nothing is left red for task 3: a live input terminal with a redirected output refuses on the output lock, calls the recording sink zero times, and never opens the prompt; a plan built from the frozen `fixtures/reviewed.json` snapshot carries the exact notes and completionRecord strings and a digest the exported function recomputes identically; a stream that closes immediately after the prompt opens returns a refusal with a single settled prompt and does not keep the run alive; and two plan runs over the same frozen snapshot produce byte-identical text and an identical digest. Extend the existing plan-text assertions to tolerate the added reviewed-content lines while still asserting the eight ordered steps, their order numbers, and their markers. Keep the CLI apply cases on the reference baseline untouched: with both streams piped they refuse on `tty`, the first terminal-family lock, before the output or content lock is ever reached, which is the ordering task 1 established and the reason a piped apply now prints nothing before anything fails. The `output` lock is exercised by the injected live-input-redirected-output case, not by the piped CLI case, and no migrated case may swap the two.

Avoid: leaving a floating promise that can keep the process alive. Avoid asking twice. Avoid reordering the lock checks. Avoid putting anything clock-derived or environment-derived into the reviewed object, since that would make the digest drift between two identical runs. Avoid a second digest implementation in this module.
  </action>
  <verify>
    <automated>node --test tools/release-close/safe04.test.js && node --test tools/release-close/ && node tools/release-close/release-close.js plan && node tools/release-close/release-close.js plan --json</automated>
    <fails_when>any command exits non-zero, or the rendered plan omits the reviewed notes, the completion record, and the digest for a snapshot that has them, or the digest differs between two identical plan runs, or a live-input apply with a redirected output stops refusing on the output lock or still writes to the sink or opens the prompt, or a fully piped apply reports the output lock instead of the tty lock, or the CLI reads its output-terminal boolean from the input stream, or a closed or errored input stream leaves the run pending, or the whole tool suite regresses</fails_when>
  </verify>
  <done>The plan carries the reviewed Release notes, the Milestone completion record, and a stable canonical digest rendered above the ordered steps when the snapshot has them and says so plainly when it does not; the gate receives the output-terminal boolean taken from the output stream and a character-counting sink; nothing is written and no prompt is opened until every lock through content has passed; end-of-input or a stream error refuses promptly with a single settled prompt; and every SAFE-04 case this wiring invalidates is green in this task.</done>
  <reversibility rating="costly">The reviewed object and digest shape become the operator-visible approval artifact in Phase 13, and the runbook will quote the rendered layout.</reversibility>
  <acceptance_criteria>Source asserts the plan builder carries the reviewed object and digest, asserts the digest comes from the single exported canonicalReviewedDigest rather than a local serialization, asserts no template prose stands in for the notes or the completion record, asserts the apply path reads the output-terminal boolean from the output stream and not the input stream, asserts the injected sink reports a character count rather than a boolean, asserts the CLI performs no output write before the gate returns, and asserts the ask factory has a single-settled guard on answer, close, and error. Behavior asserts each case above, including that a redirected output refuses on the output lock with the sink called zero times and no prompt, that a closed terminal returns a refusal rather than hanging, and that a snapshot with no reviewed fields produces no reviewed object.</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>SAFE-04 suite on the production seam with exact codes, redirection refusal, digest binding, and no-drift proofs</name>
  <files>tools/release-close/safe04.test.js</files>
  <read_first>tools/release-close/safe04.test.js lines 84-130 (the local snapshot and evidence helpers that collapse releases to their first element and take milestones[0]) and lines 175-186 (the all-fixture assertion that checks only that a code is a string); the production evidence builder and plan builder exported by 09-07; the eight-lock order, the reviewed-content lock, and the exported canonicalReviewedDigest from 09-08 task 1; tools/release-close/fixtures/reviewed.json (the scenario that carries the reviewed text); all files under tools/release-close/fixtures/ (every frozen scenario this suite must now drive exactly, including the completed, unrelated, and reviewed ones)</read_first>
  <action>
Rewrite the SAFE-04 proof so it exercises the production seam and asserts exact values, in `safe04.test.js` only — this task changes no production source and no fixture.

Delete the local snapshot and evidence helpers that re-implement normalization, and replace every use with the production evidence builder and the decision seam exported by 09-07. That local copy is precisely why the duplicate and conflicting fixtures could pass while the suite exercised a different state than the one it claimed.

Build an exact expectation table mapping every frozen fixture scenario to its expected classification code, its expected `outcome`, and its expected reviewed-content presence, then assert those exact values. For the completed scenario assert the exact pair `code === 'MISSING'` with `outcome === 'COMPLETE_NOOP'`, the same literal plan 09-05 task 2 fixed, so the two plans cannot drift apart; for every other scenario assert `outcome` is absent. For the reviewed scenario assert the reviewed object carries exactly the `notes` and `completionRecord` strings the fixture holds, byte for byte, and for the reference baseline assert no reviewed object exists at all rather than an empty one. Replace the assertion that only checks a code is a string with the exact expected code for every scenario, and assert that the decision carries the full retained identifier lists rather than a single collapsed record.

Consolidate into this one table every refusal case that tasks 1 and 2 already added, and add the remaining gate cases: a live input terminal with a redirected output refuses on the output lock with nothing written and nothing asked; a fully piped CLI invocation refuses on the tty lock with the `terminal interativo` reason and an empty stdout, kept as a row of its own so it is never conflated with the redirected-output row above; a missing or blank reviewed content refuses on the content lock; a reviewed content whose digest does not match the recomputed one refuses; a missing and non-function sink refuse on the sink lock with zero sink calls, and a zero-character sink refuses on the same lock while being observed with sink calls and no prompt; the ordered plan is written first and the reviewed content second, both before the prompt, in the recorded event order; every refusal on `plan`, `flag`, `tty`, `output`, or `content` leaves the recorded event list empty; and the end-of-input and stream-error cases return a refusal without a second prompt and without the process staying alive, asserted by running the apply path through a controlled stream that closes immediately.

Add the no-drift proofs: rendering the plan twice over the same frozen snapshot produces byte-identical text and an identical digest, and preflighting the confirmation twice changes nothing and produces the identical decision. Record the SAFE-04 boundary in a PT-BR comment: only one interactive confirmation decision may settle, end-of-input and stream errors refuse rather than hang or prompt again, and cross-process arbitration is not claimed here.

Keep the existing secret-hygiene tests, the flag, pipe, and no-plan refusal cases, the ordered-step assertions, and the frozen baseline assertions intact, and extend the captured-output list with the new apply invocations and the reviewed-content rendering so the credential scan keeps covering everything the gate now writes.

Avoid: reintroducing a local evidence helper. Avoid asserting only on substring matches where an exact value exists. Avoid restating the eight-lock order as a copy in the test instead of comparing against the exported list. Avoid adding a runner, an assertion library, or any `dependencies` entry to `tools/release-close/package.json` (D-02, D-06). Avoid spawning anything other than the local Node binary.
  </action>
  <verify>
    <automated>node --test tools/release-close/safe04.test.js && node --test tools/release-close/</automated>
    <fails_when>either run exits non-zero, or any scenario asserts only the presence of a code instead of its exact value, or a redirected-output apply confirms, or a digest mismatch confirms, or a zero-character sink confirms, or an end-of-input apply leaves the run pending, or the whole tool suite regresses</fails_when>
  </verify>
  <done>The SAFE-04 proof runs entirely on the production seam with exact per-scenario codes, the redirected-output and content and digest and sink refusals are all covered, the end-of-input path refuses without hanging or double-prompting, and repeated rendering and preflight leave the digest unchanged.</done>
  <acceptance_criteria>Test asserts every flat evidence and client-shaped frozen fixture scenario with exact codes and outcome, including the completed scenario as the exact pair MISSING plus COMPLETE_NOOP, the reviewed scenario carrying the fixture's own notes and completionRecord strings, and the reference baseline carrying no reviewed object at all, asserts the retained identifier lists are present, asserts every refusal family from tasks 1 and 2 in one table — including the output lock writing nothing, a fully piped CLI row asserting the tty lock rather than the output lock, the plan-then-reviewed render order preceding the prompt, the empty event list on every pre-render refusal, and the zero-character sink refusal kept distinct from those because it is observed with sink calls carrying zero characters and no prompt — plus the end-of-input and stream-error cases with a single settled prompt, asserts the write-before-prompt event order, asserts byte-identical repeat rendering and preflight, and asserts the credential scan still passes over the widened captured outputs. Source asserts safe04.test.js no longer defines a local snapshot or evidence helper and does not restate the lock order as a literal copy.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator terminal to apply gate | Typed confirmation crosses here; redirected, piped, or non-terminal output must never count as review |
| reviewed content to confirmation | The Release notes and Milestone completion record the operator reads must be byte-identical to the ones a later executor would submit |
| input stream to prompt settlement | End-of-input and stream errors cross here and must refuse rather than leave the process pending |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-08-01 | Elevation | output-redirected confirmation | high | mitigate | The gate requires a live output terminal and refuses on that lock as lock four, before the render and before the prompt, so nothing is written and nothing is asked; a test supplies a live input terminal and a redirected output and asserts the output lock with an empty event list. A fully piped invocation is a different case that fails earlier: lock three `tty` is the first to fail and the `output` lock is unreachable from it, so that refusal carries the existing `terminal interativo` reason and an empty stdout, and no case may attribute it to the output lock |
| T-09-08-02 | Tampering | content changed between rendering and confirmation | high | mitigate | One exported canonicalReviewedDigest function is the only serializer, the gate recomputes it at prompt time and must match the rendered digest byte for byte, and the plan builder recomputes it twice and refuses a non-deterministic result |
| T-09-08-03 | Repudiation | confirmation with no reviewed content | high | mitigate | Blank or absent Release notes or Milestone completion record refuse on the content lock, the reviewed content itself is rendered before the prompt rather than only its digest, and both texts come from named fixture fields rather than from template prose |
| T-09-08-04 | Denial of service | prompt left pending on end-of-input | medium | mitigate | The ask factory settles once on answer, close, and stream error, always closes the interface, and a test drives a stream that closes immediately and asserts a prompt refusal without a second prompt |
| T-09-08-05 | Tampering | no-op output sink accepting a swallowed plan | high | mitigate | The silent default sink is removed, a missing or non-function sink refuses at lock five before the render and is observed with zero sink calls, the injected sink must report a character count, and a sink reporting zero written characters refuses on the same lock after the render and before the prompt — the one refusal family whose write-observable is sink calls carrying zero characters rather than zero calls, which the suite asserts as a distinct row so it can never be confused with a pre-render refusal |
| T-09-08-06 | Information disclosure | reviewed content and captured output | low | mitigate | Reviewed content is copied byte for byte from frozen fixture fields, no template prose supplies operator-facing text, and the credential scan covers every newly captured output including the reviewed-content rendering |
| T-09-08-07 | Repudiation | reordering the lock sequence to dodge a refusal | medium | mitigate | The eight-entry order is exported as a list and compared against directly by the suite, the flag, tty, and no-plan cases assert their original lock so a reordered list fails the suite instead of passing quietly, and the render position itself is asserted — nothing written before the content lock, plan before reviewed content, both before the prompt — so a gate that moved the write back ahead of a lock fails too |
| T-09-08-08 | Repudiation | a render position asserted two ways at once | high | mitigate | The plan names exactly one position — render after the content lock, before the prompt — and every migrated case agrees with it: the pre-render refusals assert an empty event list, the passed case asserts plan-then-reviewed-then-prompt, and the fully piped CLI case asserts the tty lock with an empty stdout. No case asserts both that nothing is written on refusal and that a plan is rendered on a refusal, so the two claims can never both be true |
| T-09-08-SC | Tampering | npm installs | high | mitigate | No install step exists in any task of this plan; the zero-dependency manifest and the node:test runner are unchanged |

</threat_model>

<verification>
`node --test tools/release-close/safe04.test.js` green; `node --test tools/release-close/` green with no regression in the eligibility, classifier, canary, or evidence suites; `node tools/release-close/release-close.js plan` and `plan --json` still exit zero on the frozen baseline; a manual probe with a pseudo-terminal for input and a file for output refuses on the output lock, writes nothing, and never opens the prompt; a fully piped manual probe refuses on the tty lock with the `terminal interativo` reason and an empty stdout; a manual probe on a real controlling terminal shows the eight ordered steps and the reviewed content on that terminal before the prompt; a manual probe that closes the input immediately after the prompt refuses promptly rather than hanging; `git status` shows changes confined to `tools/release-close/apply-gate.js`, `tools/release-close/release-close.js`, `tools/release-close/safe04.test.js`, and `tools/release-close/fixtures/reviewed.json`.
</verification>

<success_criteria>
The human-visible reviewed-plan lock is real and its render position is unambiguous: nothing at all is written and no prompt is opened until every lock from `plan` through `content` has passed; a live input terminal with a redirected output refuses on the `output` lock while a fully piped invocation refuses on the earlier `tty` lock, each naming the lock that actually fired and each leaving the terminal untouched; a missing or non-function sink refuses with zero sink calls and a zero-character sink refuses with sink calls carrying zero characters, both on the `sink` lock; absent or changed reviewed content refuses through one canonical digest function; the reviewed Release notes and Milestone completion record have a single named source in a client-shaped frozen fixture and are rendered with the ordered plan — plan first, reviewed content second — before the prompt; the fixed order is exported as eight locks with the original five untouched; end-of-input refuses without hanging or double-prompting; and repeated rendering leaves the digest unchanged.
</success_criteria>

<output>
Create `.planning/phases/09-release-close-contract-fixtures/09-08-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Symbol | File | Consumed by |
|--------|------|-------------|
| Gate exporting the eight-lock order, the real-sink lock, the reviewed-content lock, the single canonicalReviewedDigest function, and one declared render position between the content and prompt locks | tools/release-close/apply-gate.js (extended) | Phase 13 live apply procedure, Phase 11 executor binding |
| Immutable reviewed content built from the frozen fixture fields, canonical digest, output-terminal wiring from the output stream, character-counting sink, single-settled prompt | tools/release-close/release-close.js (extended) | Phase 12 runbook, Phase 13 operator procedure |
| Frozen client-shaped scenario owning the reviewed Release notes and Milestone completion record text, envelopes intact and no flat-list keys | tools/release-close/fixtures/reviewed.json | Phase 11 executor binding, Phase 13 operator procedure |
| Production-seam SAFE-04 suite with exact codes, refusal matrix, and no-drift proofs | tools/release-close/safe04.test.js (rewritten) | 09-VERIFICATION re-run, SAFE-04 traceability |
