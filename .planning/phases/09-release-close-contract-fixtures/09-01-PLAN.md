---
phase: 09-release-close-contract-fixtures
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/release-close/package.json
  - tools/release-close/client.js
  - tools/release-close/fake-client.js
  - tools/release-close/eligibility.js
  - tools/release-close/release-close.js
  - tools/release-close/fixtures/reference.json
  - tools/release-close/eligibility.test.js
autonomous: true
requirements:
  - OPS-01
  - SAFE-02
estimate:
  tokens: 45000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - Operator runs node tools/release-close/release-close.js verify --help and sees verify/plan/apply usage with zero installs
    - verify against the reference fixture reports eligible true only when peeled tag commit equals remote main and the full expected SHA
    - Every verify output carries mutations: 0 and the suite fails if any write call is recorded
    - node --test tools/release-close/ passes on a clean checkout with no network and no database
  artifacts:
    - tools/release-close/package.json
    - tools/release-close/client.js
    - tools/release-close/fake-client.js
    - tools/release-close/eligibility.js
    - tools/release-close/release-close.js
    - tools/release-close/fixtures/reference.json
    - tools/release-close/eligibility.test.js
  key_links:
    - release-close.js imports checkTagEligibility from eligibility.js and serves snapshots from fake-client.js
    - eligibility.js returns writeAction null on every path and throws only TypeError on invalid input shape
  prohibitions:
    - tool must never create, move, delete, or force-update a git ref or tag
    - tool must never read, log, or transmit operator credentials or auth headers
    - verify must never open a network connection or spawn a subprocess in this phase
---

<objective>
Tracer slice (per D-01, D-02, D-04, D-09): thinnest production-quality end-to-end path — package manifest, client seam, minimal fake, pure tag-eligibility function, CLI skeleton serving verify, canonical reference fixture, and one node:test suite proving the loop with mutations: 0.

Purpose: Prove the architecture (pure contract + injected client + frozen fixture) on first tokens before expansion plans build on it.
Output: Runnable tool skeleton plus green scoped suite.
</objective>

<execution_context>
@/home/lucas/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/lucas/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/src/services/votingService.js
@backend/package.json
@start-dev.sh
</context>

<tasks>

<task type="tracer">
  <name>Tracer: package seam plus pure eligibility plus reference fixture</name>
  <files>tools/release-close/package.json, tools/release-close/client.js, tools/release-close/fake-client.js, tools/release-close/eligibility.js, tools/release-close/fixtures/reference.json</files>
  <read_first>backend/package.json (minimal keys precedent, invert type to module per D-02); backend/src/services/votingService.js lines 10-57 (canVote guard chain plus validateVoteInput throw-only-on-invalid-input shape for D-11); .planning/phases/09-release-close-contract-fixtures/09-RESEARCH.md Pattern 1 and Pitfall 3 (two-hop peel, injected client seam)</read_first>
  <action>Create tools/release-close/package.json with name release-close, private true, type module, and no dependencies key and no scripts key per D-02/D-03. Create tools/release-close/client.js documenting the five read-method names getTagRef, getTagObject, getBranchHead, getReleaseByTag, listMilestones, each returning normalized ok/status/data objects, with no write-method names anywhere per D-09. Create tools/release-close/fake-client.js as function makeFakeClient taking a snapshot object, returning an async client implementing the five reads from snapshot data, recording every call as method-plus-args-plus-sequence entries in a calls array, exposing a writes array that stays empty, per D-07 and researcher resolution to record a call log. Create tools/release-close/eligibility.js exporting checkTagEligibility(client, options) with options version plus expectedSha; validate expectedSha as 40 lowercase hex and version as vX.Y.Z shape, throwing TypeError with PT-BR message otherwise per D-11; implement the two-hop peel (tag ref of object type tag, then tag-object lookup, single-hop commit type rejected as lightweight with eligible false), then strict string equality of peeled commit against branch head and expectedSha, every return shaped as eligible/code/reason/writeAction with reason PT-BR, code EN, writeAction null per D-10/D-11/D-12. Create tools/release-close/fixtures/reference.json encoding the canonical live baseline verbatim: tag object sha 0a68d6f0c55e7be07d13a0bbc4ed36d4af772630 of type tag, peeled commit 10c62ac85fd3ab275b8926c89f5f34ba4116e2cf, remote main head identical, expectedSha identical, release absent, milestones empty, frozen timestamp string, canned run identifiers 36095855139 and 36095872529, per D-05/D-08 and CONTEXT integration points.</action>
  <verify>
    <automated>node --test tools/release-close/ 2>&1 | tail -5; node -e "import('./tools/release-close/eligibility.js').then(m => console.log(typeof m.checkTagEligibility))"</automated>
    <fails_when>non-zero exit from the test run, or the second command prints anything other than function</fails_when>
  </verify>
  <done>checkTagEligibility accepts the reference snapshot and rejects a diverged SHA with writeAction null; package has type module and zero dependencies.</done>
  <acceptance_criteria>Source asserts package.json contains type module and no dependencies key; behavior asserts eligibility true on reference snapshot and eligible false with SAFE-02 code family on SHA divergence; CLI asserts nothing yet (covered by task 2).</acceptance_criteria>
</task>

<task type="auto">
  <name>CLI skeleton with verify path and help</name>
  <files>tools/release-close/release-close.js</files>
  <read_first>start-dev.sh lines 1-24 (fail-fast header, hand-rolled flag parser, unknown-flag refusal, PT-BR messages); tools/release-close/eligibility.js and tools/release-close/fake-client.js from task 1 (import surface); 09-RESEARCH.md Pattern 3 (mutations counter) and Code Examples apply-gate section for future-task boundary</read_first>
  <action>Create tools/release-close/release-close.js as zero-dependency ESM with a hand-rolled argv parser over the verbs verify, plan, apply and flags help, json, yes, repo, version, sha, unknown flags printing PT-BR usage to stderr with non-zero exit per D-01. Implement verify to load the reference fixture via import-meta-relative URL, serve it through makeFakeClient, call checkTagEligibility, print human-readable PT-BR text including a mutations line with value 0, and support a json flag printing the decision object plus mutations field per D-13/D-16. Implement plan and apply as explicit not-yet-wired refusals naming their owning later plan (plan in 09-03), exiting non-zero without prompting, so the verbs exist per OPS-01 while their behavior stays out of this slice. Never import child_process, never read process env token variables, never pass verbose-style flags anywhere per RESEARCH Pitfall 7.</action>
  <verify>
    <automated>node tools/release-close/release-close.js verify --help; echo "help_exit=$?"; node tools/release-close/release-close.js verify --json | grep -c mutations</automated>
    <fails_when>help output missing any of verify plan apply, non-zero exit on --help path, or the mutations count line prints 0</fails_when>
  </verify>
  <done>verify prints text by default and JSON with mutations field under --json; plan/apply refuse as not-yet-wired; --help documents all three verbs.</done>
  <acceptance_criteria>Source asserts release-close.js contains no require calls and no subprocess import; behavior asserts default verify output contains the mutations zero line; CLI asserts --help exit zero and --bogus-flag exit non-zero with usage on stderr.</acceptance_criteria>
</task>

<task type="auto">
  <name>Tracer proof suite in node:test with PT-BR names</name>
  <files>tools/release-close/eligibility.test.js</files>
  <read_first>backend/tests/unit.test.js lines 1-28 (describe plus short PT-BR it-name convention, invert runner import to node:test per D-06); tools/release-close/eligibility.js, tools/release-close/fake-client.js, tools/release-close/fixtures/reference.json (system under test); 09-RESEARCH.md Code Examples node:test fixture-loading pattern</read_first>
  <action>Create tools/release-close/eligibility.test.js importing describe/it from node:test and strict assert from node:assert, loading fixtures via file read relative to import.meta.url, covering: accepts annotated tag when peel equals main equals expected SHA with eligible true and writeAction null; rejects lightweight single-hop commit-type ref as ineligible with writeAction null; rejects diverged main head; rejects diverged expected SHA; rejects absent tag ref; throws TypeError on abbreviated SHA and on malformed version; asserts fake writes array length zero and every decision carries mutations-equivalent zero-write proof; asserts read ordering ref before tag-object before branch head via the fake call log. Mirror the repo PT-BR it-name style in all test names per D-12. Flagged assumption A (OPS-01 idempotency probe, unresolved): re-running verify twice on the same fixture is safe because verify performs only in-memory reads and emits mutations 0 — codify with a double-run test asserting identical output and zero writes. Flagged assumption B (OPS-01 concurrency probe, unresolved): Phase 9 concurrency guarantee is single-process only (one fake client per run, no shared state); true cross-process locking is Phase 11 REC-05 scope — codify with a test asserting two independent fake clients hold isolated call logs.</action>
  <verify>
    <automated>node --test tools/release-close/</automated>
    <fails_when>non-zero exit, or the summary line shows 0 passed</fails_when>
  </verify>
  <done>Scoped suite green, proving CLI-to-pure-function loop, peel equality, invalid-input throws, zero writes, and the two flagged OPS-01 assumptions as executable tests.</done>
  <acceptance_criteria>Source asserts test file imports from node:test and never from vitest; behavior asserts all eight cases above pass; test asserts double-run identical output and isolated call logs for the flagged assumptions.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator→CLI argv | Untrusted --repo/--version/--sha values cross here |
| fake-client→pure logic | Test-only data crosses here; Phase 10 replaces with gh subprocess |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Tampering | release-close.js argv handling | high | mitigate | No subprocess exists in this plan, so injection has no sink; add negative test with metachar values asserting they are treated as opaque strings (blocks future execFile wiring from regressing) |
| T-09-02 | Tampering | fixtures/reference.json | medium | mitigate | Canonical SHA values frozen verbatim; full-40-hex validators reject abbreviations; PR review checks values against ROADMAP baseline |
| T-09-03 | Information disclosure | CLI output | medium | mitigate | Never read env token variables, never log headers; output limited to decision fields plus mutations counter |
| T-09-SC | Tampering | npm installs | high | mitigate | Zero-dependency lock: package legitimacy audit is not applicable (no packages); no install step exists in any task |

</threat_model>

<verification>
node --test tools/release-close/ green; node tools/release-close/release-close.js verify --help documents three verbs; backend untouched (git status shows only tools/release-close/ paths).
</verification>

<success_criteria>
Tracer slice committed: CLI skeleton, pure eligibility, client seam, minimal fake, reference fixture, and proof suite all green with mutations: 0 on every verify output.
</success_criteria>

<output>
Create `.planning/phases/09-release-close-contract-fixtures/09-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Symbol | File | Consumed by |
|--------|------|-------------|
| package manifest (type module, zero deps) | tools/release-close/package.json | All later plans (ESM resolution) |
| client interface (5 read methods) | tools/release-close/client.js | 09-02 fake expansion + Phase 10 gh-client |
| makeFakeClient (snapshot reads + call log + writes trap) | tools/release-close/fake-client.js | 09-02 scripted sequences, 09-03 write-trap asserts |
| checkTagEligibility | tools/release-close/eligibility.js | 09-03 plan display, Phase 10 preflight |
| CLI entry (verify live; plan/apply refused) | tools/release-close/release-close.js | 09-03 apply path + output work |
| reference fixture (canonical v0.1.1 baseline) | tools/release-close/fixtures/reference.json | 09-02 state fixtures, Phase 10 live comparison |
