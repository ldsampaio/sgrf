---
phase: 02-rules-decisions-docs-14-close-out
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/03-regras-de-negocio.md
  - docs/14-decisoes-em-aberto.md
autonomous: true
requirements:
  - VOT-03
user_setup: []

estimate:
  tokens: 30000
  raw_tokens: 20000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "A PARCIAL tally outcome moves the request to AGUARDANDO_ARBITRAGEM instead of concluding with an amount (per D-01)"
    - "The arbitrated amount may be any value in (0, requestedAmountCents], not clamped to voted partials (per D-02)"
    - "Arbitration records mandatory justification plus audit with decidedBy = chefe (per D-03)"
    - "docs/03 names the status AGUARDANDO_ARBITRAGEM and states it replaces first-partial-vote-wins in votingService.js:84 (per D-04)"
    - "docs/14 partial-approval row is closed with date 2026-09-23 plus rationale (per D-12)"
    - "No backend/ or frontend/ file is modified by this plan (per D-12 scope discipline)"
  artifacts:
    - docs/03-regras-de-negocio.md
    - docs/14-decisoes-em-aberto.md
  key_links:
    - "RN-009 arbitration rule in docs/03 is the contract Phase 6 plan 06-03 implements"
    - "Closed partial row in docs/14 carries date plus rationale so the rule is never re-guessed in code"
---

<objective>
Close the partial-approval aggregation slice end to end: the prescriptive arbitration contract in docs/03 (new RN-009) AND the closed partial-approval row in docs/14, per locked decisions D-01 through D-04.

Purpose: Phase 6 plan 06-03 consumes this rule directly — without a locked, prescriptive record, the implementer re-guesses the aggregation (first-partial-wins survives).
Output: RN-009 appended to docs/03 plus the partial-approval docs/14 row closed with date and rationale.
</objective>

<execution_context>
@/home/lucas/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/lucas/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-rules-decisions-docs-14-close-out/02-CONTEXT.md
@.planning/phases/02-rules-decisions-docs-14-close-out/02-RESEARCH.md
@.planning/phases/02-rules-decisions-docs-14-close-out/02-PATTERNS.md
@docs/03-regras-de-negocio.md
@docs/14-decisoes-em-aberto.md
</context>

<tasks>

<task type="tracer">
  <name>End-to-end partial-approval arbitration rule — docs/03 RN-009 plus closed docs/14 row</name>
  <files>docs/03-regras-de-negocio.md, docs/14-decisoes-em-aberto.md</files>
  <read_first>docs/03-regras-de-negocio.md (RN-001 through RN-008 voice and numbering — new rule appends at RN-009), docs/14-decisoes-em-aberto.md (terse 2-column table to close in place), .planning/phases/02-rules-decisions-docs-14-close-out/02-CONTEXT.md (D-01 through D-04, authoritative), .planning/phases/02-rules-decisions-docs-14-close-out/02-PATTERNS.md (new-RN shape and traceability checklist)</read_first>
  <action>Append RN-009 to docs/03 in the existing terse prescriptive voice (header format `## RN-009 — Arbitragem de aprovação parcial`, short declarative sentences, fenced block for the status constant), transcribing D-01 through D-04 with concrete values only: PARCIAL tally outcome moves the request to AGUARDANDO_ARBITRAGEM and never concludes with an amount per D-01; arbitrated amount is any value in (0, requestedAmountCents] per D-02; mandatory justification plus audit with decidedBy = CHEFE_DEPARTAMENTO per D-03; status constant AGUARDANDO_ARBITRAGEM in SCREAMING_SNAKE Portuguese per D-04 with an explicit sentence that this replaces first-partial-vote-wins in backend/src/services/votingService.js closeVoting; end the RN with the decision stamp (Decidido em 2026-09-23, D-01...D-04). Do NOT specify any skip/nudge/escalate interaction between AGUARDANDO_ARBITRAGEM and JOB-02 auto-close — that interaction is flagged for Phase 6/7, not decided here. Close the partial-approval docs/14 row in place (keep the 2-column pipe table, lowercase terse cells) with the decision plus date 2026-09-23 plus rationale per D-12. Then run a read-only consistency check of docs/06-permissoes.md and docs/07-fluxos.md against the new RN and record the outcome in the plan SUMMARY (default per D-12 scope discipline: no edit to docs/06 or docs/07; edit only on direct contradiction and justify it). Wave-discipline note: plan 02-02 edits the same two files in Wave 1 — keep every edit append-only and confined to the RN-009 block and the partial row so regions never overlap; on any merge conflict, rebase and keep both regions.</action>
  <verify>
    <automated>grep -n 'AGUARDANDO_ARBITRAGEM' docs/03-regras-de-negocio.md docs/14-decisoes-em-aberto.md</automated>
    <fails_when>exit code nonzero, or zero matching lines in either file</fails_when>
  </verify>
  <acceptance_criteria>docs/03 contains AGUARDANDO_ARBITRAGEM and the string (0, requestedAmountCents]; docs/03 contains the sentence replacing first-partial-vote-wins with a votingService.js:84 citation; docs/14 partial row contains 2026-09-23; git diff --name-only shows no backend/ or frontend/ path</acceptance_criteria>
  <done>RN-009 arbitration contract plus closed partial docs/14 row exist, are greppable, and no src/ file is touched.</done>
  <reversibility rating="reversible">Markdown-only doc edits, revertible with git revert; no runtime effect.</reversibility>
</task>

<task type="auto">
  <name>Traceability and exact-string hardening for the arbitration slice</name>
  <files>docs/03-regras-de-negocio.md, docs/14-decisoes-em-aberto.md</files>
  <read_first>docs/03-regras-de-negocio.md (the RN-009 block just written), docs/14-decisoes-em-aberto.md (the closed partial row), .planning/phases/02-rules-decisions-docs-14-close-out/02-RESEARCH.md (Pitfalls 1-2 and the Key insight verbatim-vocabulary contract)</read_first>
  <action>Re-read RN-009 and the closed partial row and harden exact-string fidelity without changing locked meaning: role string reads CHEFE_DEPARTAMENTO exactly; amounts read as integer cents with the (0, requestedAmountCents] range; no new suspension status string appears anywhere in the slice; the partial example from CONTEXT (votes 8000, 5000, 6000 no longer auto-resolving) is reflected as rationale, not as a new constraint. Confirm every D-01, D-02, D-03, D-04 maps to at least one sentence (reviewer traceability D-to-sentence), and fix any gap by adding the missing sentence. Verify scope with git diff --stat showing only docs/03-regras-de-negocio.md and docs/14-decisoes-em-aberto.md.</action>
  <verify>
    <automated>git diff --stat -- docs/03-regras-de-negocio.md docs/14-decisoes-em-aberto.md && git diff --name-only | grep -v -e '^docs/03-regras-de-negocio.md$' -e '^docs/14-decisoes-em-aberto.md$' -e '^\.planning/' ; test $? -eq 1</automated>
    <fails_when>the trailing grep finds any changed path outside the two doc files (test returns 0), or git diff --stat shows zero changed lines</fails_when>
  </verify>
  <acceptance_criteria>RN-009 contains CHEFE_DEPARTAMENTO verbatim and (0, requestedAmountCents]; no SUSPENSO_* string other than pre-existing ones appears in the new block; D-01 through D-04 each trace to a sentence; diff touches only the two doc files</acceptance_criteria>
  <done>Arbitration slice is traceable D-to-sentence, uses exact code vocabulary, and stays inside the two-file scope.</done>
  <reversibility rating="reversible">Markdown-only refinements, revertible with git revert; no runtime effect.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs/03 → Phase 6 code | Untrusted-by-default transcription: Phase 6 implements whatever the RN says, so ambiguous prose becomes an access-control or money bug |
| docs/14 stale recommendation → implementer | A surviving recommendation (quorum threshold, per-request vista cap) is followed instead of the locked decision |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | RN-009 arbitration text (docs/03) | high | mitigate | Prescriptive RN with verbatim status/role strings (AGUARDANDO_ARBITRAGEM, CHEFE_DEPARTAMENTO), explicit replacement of votingService.js:84 behavior, reviewer traceability D-01..D-04 to sentences (task 2) |
| T-02-02 | Tampering | Partial docs/14 row | medium | mitigate | Row closed in place with date plus rationale; no stale recommendation survives alongside the decision |
| T-02-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs in this phase — `npm install` must not appear; no legitimacy checkpoint needed |
</threat_model>

<verification>
grep -n 'AGUARDANDO_ARBITRAGEM' docs/03-regras-de-negocio.md docs/14-decisoes-em-aberto.md shows matches in both files; git diff --stat shows only the two doc files; no backend/ or frontend/ path in the diff.
</verification>

<success_criteria>
RN-009 states the arbitration rule prescriptively with exact strings and the D-01..D-04 decision stamp; the docs/14 partial row is closed with date plus rationale; the slice is independently reviewable and ready for Phase 6 plan 06-03.
</success_criteria>

## Artifacts this phase produces

- New RN entry: RN-009 (Arbitragem de aprovação parcial) in docs/03-regras-de-negocio.md — PARCIAL → AGUARDANDO_ARBITRAGEM, range (0, requestedAmountCents], mandatory justification plus audit, decidedBy = chefe, decision stamp (Decidido em 2026-09-23, D-01...D-04).
- Closed docs/14 row: partial-approval row closed with decision plus date 2026-09-23 plus rationale.
- Decision stamp: each new sentence traceable to D-01, D-02, D-03, or D-04 (no code symbols produced — code files are read-only evidence).
- Consistency note: docs/06 plus docs/07 read-only check outcome recorded in the plan SUMMARY (default: no edit).

<output>
Create `.planning/phases/02-rules-decisions-docs-14-close-out/02-01-SUMMARY.md` when done
</output>
