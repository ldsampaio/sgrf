---
phase: 02-rules-decisions-docs-14-close-out
plan: "03"
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
files_modified:
  - docs/03-regras-de-negocio.md
  - docs/14-decisoes-em-aberto.md
autonomous: true
requirements:
  - VOT-03
  - VOT-04
user_setup: []

estimate:
  tokens: 35000
  raw_tokens: 22000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "No minimum quorum exists and the tally of cast valid votes decides, with the prior maioria-dos-conselheiros plus sem-quorum recommendation explicitly marked OVERRIDDEN (per D-09)"
    - "Vista stays 1 per conselheiro with stacking deadline extensions, and the doc's maximo-de-um-por-solicitacao is explicitly marked REJECTED (per D-10)"
    - "Conselheiros may request vote interruption, the chefe pautas it in reuniao extraordinaria via SUSPENSO_REUNIAO_ORDINARIA, and only ADMINISTRADOR/CHEFE_DEPARTAMENTO enter the manual deliberation result (per D-11)"
    - "Every closed docs/14 item carries date plus rationale and docs/03 states each rule prescriptively (per D-12)"
    - "The plan and docs/14 record that ROADMAP success criterion 4 (quorum/vista deferred to v2) is STALE and overridden — no defer-quorum text appears anywhere (per D-13)"
    - "The REQUIREMENTS.md v2 trigger line quorum update is recorded as a pending follow-up, not silently dropped and not edited in this phase (per D-13)"
    - "Exactly two files change: docs/03 plus docs/14; no src/ in the diff; the commit lands via PR flow (per D-12 plus Phase 1 learning)"
  artifacts:
    - docs/03-regras-de-negocio.md
    - docs/14-decisoes-em-aberto.md
  key_links:
    - "RN-011/RN-012/RN-013 plus closed quorum/vista rows unblock the council workflow from the stale v2 deferral"
    - "OVERRIDDEN/REJECTED markers stop Phase 6 from implementing the stale docs/14 recommendations"
---

<objective>
Close the remaining two rules (quorum-absence D-09, vista plus interruption D-10/D-11) with RN-011/RN-012/RN-013 plus closed docs/14 rows, then verify the whole four-rule commit: full D-01..D-12 traceability, exactly-two-file diff, CI green via PR flow.

Purpose: The user overruled the ROADMAP v2 deferral — quorum and vista are decided NOW, and without explicit OVERRIDE/REJECT markers Phase 6 would implement the stale recommendations.
Output: Three new RNs, two closed docs/14 rows with deviation markers, a verified single docs commit landed via PR, and a recorded D-13 REQUIREMENTS follow-up.

Spec-less probe fallback (visible skip): no phase SPEC exists and phase_req_ids is prose with no IDs to probe, so no probe-derived predicates were generated this run — locked decisions D-01..D-13 in 02-CONTEXT.md are the authoritative source and are lifted into must_haves.truths instead.
</objective>

<execution_context>
@/home/lucas/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/lucas/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-rules-decisions-docs-14-close-out/02-CONTEXT.md
@.planning/phases/02-rules-decisions-docs-14-close-out/02-RESEARCH.md
@.planning/phases/02-rules-decisions-docs-14-close-out/02-PATTERNS.md
@.planning/phases/02-rules-decisions-docs-14-close-out/02-01-SUMMARY.md
@.planning/phases/02-rules-decisions-docs-14-close-out/02-02-SUMMARY.md
@docs/03-regras-de-negocio.md
@docs/14-decisoes-em-aberto.md
</context>

<tasks>

<task type="tracer">
  <name>End-to-end quorum plus vista slice — RN-011/RN-012/RN-013 plus closed rows with deviation markers</name>
  <files>docs/03-regras-de-negocio.md, docs/14-decisoes-em-aberto.md</files>
  <read_first>docs/03-regras-de-negocio.md (RN-009/RN-010 blocks from Wave 1 — append RN-011 through RN-013 after them), docs/14-decisoes-em-aberto.md (quorum, ausencia-de-quorum, and vista rows with the stale recommendations to override), .planning/phases/02-rules-decisions-docs-14-close-out/02-CONTEXT.md (D-09 through D-12, authoritative), .planning/phases/02-rules-decisions-docs-14-close-out/02-RESEARCH.md (Pitfalls 1, 2, 4 plus the STALE criterion-4 analysis)</read_first>
  <action>Append RN-011 (quorum-absence), RN-012 (vista), RN-013 (extraordinary meeting) to docs/03 in the existing terse prescriptive voice with concrete values only: RN-011 states no minimum quorum, the tally of cast valid votes decides, no sem-quorum status is introduced, decision stamp (Decidido em 2026-09-23, D-09) per D-09; RN-012 states 1 vista per conselheiro per request with stacking deadline extensions enforced by the existing @@unique([requestId, requestedBy]) behavior, decision stamp (Decidido em 2026-09-23, D-10) per D-10; RN-013 states conselheiros may request interrupting the vote, the chefe pautas the request in reuniao extraordinaria reusing the existing SUSPENSO_REUNIAO_ORDINARIA flow with no new status string, only ADMINISTRADOR and CHEFE_DEPARTAMENTO may enter the manual deliberation result via the existing collegiateDecision path, decision stamp (Decidido em 2026-09-23, D-11) per D-11. Close the docs/14 quorum plus ausencia-de-quorum rows in place with the explicit marker that the prior maioria-dos-conselheiros-elegiveis plus sem-quorum-acao-manual recommendation is OVERRIDDEN per D-09 with rationale plus date; close the vista row in place with the explicit marker that maximo-de-um-por-solicitacao is REJECTED per D-10 with rationale plus date, stating 1-per-conselheiro stacking instead. Record in the docs/14 closings that ROADMAP Phase 2 success criterion 4 (quorum/vista deferred to v2) is STALE and overridden by user decision — never write defer-quorum or defer-vista text. Surface as an explicit assumption in the plan SUMMARY (not a decision): the AGUARDANDO_ARBITRAGEM versus JOB-02 auto-close interaction stays undecided (skip/nudge/escalate) for Phase 6/7.</action>
  <verify>
    <automated>grep -n 'OVERRIDDEN\|REJECTED\|REJEITADA\|SUSPENSO_REUNIAO_ORDINARIA' docs/03-regras-de-negocio.md docs/14-decisoes-em-aberto.md</automated>
    <fails_when>exit code nonzero; or the quorum/vista docs/14 rows lack an explicit override-or-reject marker; or any new SUSPENSO_ string other than SUSPENSO_REUNIAO_ORDINARIA appears</fails_when>
  </verify>
  <acceptance_criteria>docs/03 contains RN-011, RN-012, RN-013 with SUSPENSO_REUNIAO_ORDINARIA reused verbatim; docs/14 quorum rows contain an OVERRIDDEN marker with 2026-09-23 and rationale; docs/14 vista row contains a REJECTED marker with 2026-09-23 and rationale; no defer-quorum or defer-vista text anywhere; plan SUMMARY records the AGUARDANDO_ARBITRAGEM/JOB-02 interaction as an undecided assumption for Phase 6/7</acceptance_criteria>
  <done>Quorum plus vista rules are closed with load-bearing deviation markers, and the stale-deferral deviation is recorded.</done>
  <reversibility rating="reversible">Markdown-only doc edits, revertible with git revert; no runtime effect.</reversibility>
</task>

<task type="auto">
  <name>Whole-commit verification — D-01..D-12 traceability, two-file scope, CI green via PR</name>
  <files>docs/03-regras-de-negocio.md, docs/14-decisoes-em-aberto.md</files>
  <read_first>docs/03-regras-de-negocio.md (full file: RN-001 through RN-013), docs/14-decisoes-em-aberto.md (full file: 4 closed rows plus untouched rows), .planning/phases/02-rules-decisions-docs-14-close-out/02-01-SUMMARY.md, .planning/phases/02-rules-decisions-docs-14-close-out/02-02-SUMMARY.md (Wave 1 slice outcomes to integrate)</read_first>
  <action>Perform the reviewer read-through over the full diff: confirm every D-01 through D-12 traces to at least one doc sentence and list the mapping in the plan SUMMARY; confirm D-13 is honored as a recorded pending follow-up — do NOT edit REQUIREMENTS.md in this phase (D-12 limits the commit to docs/03 plus docs/14), instead write into the plan SUMMARY the exact pending update: the REQUIREMENTS.md v2 trigger line Remaining docs/14 decisions: quorum ... is now satisfied for quorum/vista and must be updated by whoever next touches REQUIREMENTS.md, never silently dropped. Confirm untouched docs/14 rows (limite, saldo insuficiente, taxa de dolar, aluno/auxilio, exclusao de usuario, senha temporaria, anexos, e-mails falhos) are unchanged. Confirm the diff touches exactly docs/03-regras-de-negocio.md plus docs/14-decisoes-em-aberto.md with git diff --stat and the name-only gate below. Land via PR flow per the Phase 1 learning (direct pushes to protected main are declined) and confirm the required backend plus frontend CI checks are green on the PR using the proven Phase 01 verify commands (ci.yml content pins plus branch-protection readback).</action>
  <verify>
    <automated>git diff --stat -- docs/03-regras-de-negocio.md docs/14-decisoes-em-aberto.md && git diff --name-only | grep -v -e '^docs/03-regras-de-negocio.md$' -e '^docs/14-decisoes-em-aberto.md$' -e '^\.planning/' ; test $? -eq 1 && grep -c 'checkout@v7' .github/workflows/ci.yml && grep -c 'setup-node@v7' .github/workflows/ci.yml && grep -c 'node-version: 22' .github/workflows/ci.yml && gh api repos/ldsampaio/sgrf/branches/main/protection --jq '.required_status_checks.contexts | join(",")'</automated>
    <fails_when>the trailing scope grep finds any changed path outside the two doc files; or any ci.yml pin count returns 0; or the protection readback omits the backend,frontend contexts</fails_when>
  </verify>
  <acceptance_criteria>D-01 through D-12 each map to a doc sentence listed in the SUMMARY; D-13 pending REQUIREMENTS update recorded in the SUMMARY with the exact trigger-line wording; untouched docs/14 rows byte-identical; diff limited to the two doc files; PR shows backend plus frontend checks green</acceptance_criteria>
  <done>The four-rule docs commit is fully traceable, scope-clean, landed via PR with CI green, and the D-13 follow-up is recorded.</done>
  <reversibility rating="reversible">Docs commit on a PR branch, revertible or closable before merge; no runtime effect.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs/14 stale recommendation → Phase 6 implementer | The quorum/vista rows previously recommended a quorum check and a per-request vista cap — following them now contradicts the locked decisions |
| docs/03 → future V5 validation spec | D-06 (missing justification = 400) and D-02 (amount range) become the Phase 6 validation contract — imprecision becomes a validation bypass |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05 | Tampering | Quorum/vista docs/14 rows | high | mitigate | Explicit OVERRIDDEN (D-09) and REJECTED (D-10) markers with rationale plus date, so Phase 6 cannot follow the stale maioria-dos-conselheiros or maximo-de-um-por-solicitacao text |
| T-02-06 | Tampering | RN-013 deliberation-entry roles (docs/03) | high | mitigate | RN-013 locks ADMINISTRADOR plus CHEFE_DEPARTAMENTO entry via the existing collegiateDecision path with no widened roles and no new status string (task 1) |
| T-02-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs in this phase — `npm install` must not appear; no legitimacy checkpoint needed |
</threat_model>

<verification>
RN-011/RN-012/RN-013 present with exact strings; OVERRIDDEN plus REJECTED markers present in docs/14 with dates; D-01..D-12 mapping listed in SUMMARY; diff limited to two files; PR backend plus frontend checks green.
</verification>

<success_criteria>
All four rules (partial, cancellation, quorum, vista/interruption) are closed with date plus rationale; stale ROADMAP criterion 4 deviation is recorded; the single docs commit lands via PR with CI green; D-13 follow-up is recorded, not dropped.
</success_criteria>

## Artifacts this phase produces

- New RN entries: RN-011 (quorum-absence — no minimum quorum, tally of cast valid votes decides, no sem-quorum status), RN-012 (vista — 1 per conselheiro, stacking extensions, @@unique kept), RN-013 (extraordinary meeting — interruption request, chefe pautas via SUSPENSO_REUNIAO_ORDINARIA, admin/chefe-only collegiateDecision entry), each with decision stamp (Decidido em 2026-09-23, D-09/D-10/D-11).
- Closed docs/14 rows: quorum plus ausencia-de-quorum rows with OVERRIDDEN marker, vista row with REJECTED marker, each with date plus rationale.
- Decision stamps: every new sentence traceable to D-09, D-10, D-11, or D-12; whole-commit D-01..D-12 mapping listed in the plan SUMMARY (no code symbols produced).
- Recorded follow-up: D-13 REQUIREMENTS.md v2 trigger-line update captured verbatim in the SUMMARY for whoever next touches REQUIREMENTS.md.
- Explicit assumption: AGUARDANDO_ARBITRAGEM versus JOB-02 auto-close interaction flagged undecided for Phase 6/7.

<output>
Create `.planning/phases/02-rules-decisions-docs-14-close-out/02-03-SUMMARY.md` when done
</output>
