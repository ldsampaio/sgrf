---
phase: 02-rules-decisions-docs-14-close-out
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/03-regras-de-negocio.md
  - docs/14-decisoes-em-aberto.md
autonomous: true
requirements:
  - VOT-04
user_setup: []

estimate:
  tokens: 30000
  raw_tokens: 20000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "Only the dono in RASCUNHO/EM_VOTACAO, ADMINISTRADOR in any non-terminal status, and CHEFE_DEPARTAMENTO in any status before CONCLUIDO may cancel (per D-05)"
    - "Every cancellation requires justification text, missing means 400, and the text lands in REVERSE metadata plus AuditEvent request_cancelled/provision_reversed (per D-06)"
    - "Cancelling an approved or provisioned request writes an audited compensating REVERSE FinancialTransaction, and CONCLUIDO can never be cancelled (per D-07)"
    - "All non-terminal statuses except CANCELADO are cancellable, INDEFERIDO is cancellable by admin, and CONCLUIDO/CANCELADO are immutable (per D-08)"
    - "docs/14 cancellation row is closed with date 2026-09-23 plus rationale (per D-12)"
    - "The collegiateDecision chefe-only code gap is flagged for Phase 6, not fixed here (per D-11)"
    - "No backend/ or frontend/ file is modified by this plan (per D-12 scope discipline)"
  artifacts:
    - docs/03-regras-de-negocio.md
    - docs/14-decisoes-em-aberto.md
  key_links:
    - "RN-010 cancellation rule in docs/03 is the contract Phase 6 plan 06-04 implements"
    - "Closed cancellation row in docs/14 carries date plus rationale so the matrix is never re-guessed in code"
---

<objective>
Close the cancellation-after-approval slice end to end: the prescriptive status-by-role cancellation contract in docs/03 (new RN-010) AND the closed cancellation row in docs/14, per locked decisions D-05 through D-08.

Purpose: Phase 6 plan 06-04 consumes this rule directly — without a locked matrix, the implementer re-guesses who may cancel what (today cancel does zero checks).
Output: RN-010 appended to docs/03 plus the cancellation docs/14 row closed with date and rationale.
</objective>

<execution_context>
@/home/lucas/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/lucas/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-rules-decisions-docs-14-close-out/02-CONTEXT.md
@.planning/phases/02-rules-decisions-docs-14-close-out/02-RESEARCH.md
@.planning/phases/02-rules-decisions-docs-14-close-out/02-PATTERNS.md
@docs/03-regras-de-negocio.md
@docs/14-decisoes-em-aberto.md
</context>

<tasks>

<task type="tracer">
  <name>End-to-end cancellation rule — docs/03 RN-010 plus closed docs/14 row</name>
  <files>docs/03-regras-de-negocio.md, docs/14-decisoes-em-aberto.md</files>
  <read_first>docs/03-regras-de-negocio.md (RN-001 through RN-008 voice and numbering — new rule appends at RN-010, after the RN-009 arbitration block), docs/14-decisoes-em-aberto.md (cancellation row reading today permitido apenas por admin ou chefe, com justificativa), .planning/phases/02-rules-decisions-docs-14-close-out/02-CONTEXT.md (D-05 through D-08, authoritative), .planning/phases/02-rules-decisions-docs-14-close-out/02-PATTERNS.md (status-by-role matrix checklist and Pitfall 3 edges)</read_first>
  <action>Append RN-010 to docs/03 in the existing terse prescriptive voice (header format `## RN-010 — Cancelamento após aprovação`, short declarative sentences, fenced block for the status-by-role enumeration), transcribing D-05 through D-08 with concrete values only: dono (requester) cancels only in RASCUNHO and EM_VOTACAO; ADMINISTRADOR cancels in any non-terminal status; CHEFE_DEPARTAMENTO cancels in any status before CONCLUIDO per D-05; justification mandatory for every cancellation with missing meaning 400, text recorded in REVERSE transaction metadata plus AuditEvent request_cancelled and provision_reversed per D-06; cancelling an approved or provisioned request writes an audited compensating REVERSE FinancialTransaction reusing the reverseProvision pattern, and CONCLUIDO (spent) is never cancellable per D-07; cancellable set is all non-terminal statuses except CANCELADO itself, INDEFERIDO explicitly cancellable by admin as cleanup, terminal CONCLUIDO and CANCELADO immutable per D-08; role strings use the exact 5-role vocabulary ADMINISTRADOR, CHEFE_DEPARTAMENTO, CONSELHEIRO, PROFESSOR, ALUNO; end the RN with the decision stamp (Decidido em 2026-09-23, D-05...D-08). Close the cancellation docs/14 row in place with the matrix plus date 2026-09-23 plus rationale per D-12. FLAG in the plan SUMMARY (do not fix): the D-11 code-vs-decision gap — backend/src/controllers/votingController.js collegiateDecision currently enforces chefe-only while D-11 locks admin plus chefe entry; the RN states admin plus chefe and Phase 6 reconciles the code. Wave-discipline note: plan 02-01 edits the same two files in Wave 1 — keep every edit append-only and confined to the RN-010 block and the cancellation row so regions never overlap; on any merge conflict, rebase and keep both regions.</action>
  <verify>
    <automated>grep -n 'request_cancelled\|provision_reversed\|REVERSE' docs/03-regras-de-negocio.md docs/14-decisoes-em-aberto.md</automated>
    <fails_when>exit code nonzero, or zero matching lines in either file</fails_when>
  </verify>
  <acceptance_criteria>docs/03 RN-010 contains RASCUNHO, EM_VOTACAO, CONCLUIDO, CANCELADO, INDEFERIDO, REVERSE, request_cancelled, and provision_reversed; docs/14 cancellation row contains 2026-09-23; plan SUMMARY flags the collegiateDecision chefe-only gap for Phase 6; git diff --name-only shows no backend/ or frontend/ path</acceptance_criteria>
  <done>RN-010 cancellation contract plus closed cancellation docs/14 row exist, are greppable, the code gap is flagged, and no src/ file is touched.</done>
  <reversibility rating="reversible">Markdown-only doc edits, revertible with git revert; no runtime effect.</reversibility>
</task>

<task type="auto">
  <name>Traceability and edge completeness for the cancellation slice</name>
  <files>docs/03-regras-de-negocio.md, docs/14-decisoes-em-aberto.md</files>
  <read_first>docs/03-regras-de-negocio.md (the RN-010 block just written), docs/14-decisoes-em-aberto.md (the closed cancellation row), .planning/phases/02-rules-decisions-docs-14-close-out/02-RESEARCH.md (Pitfall 3 INDEFERIDO-cleanup and CONCLUIDO-immutability edges, Pitfall 4 no-new-status rule)</read_first>
  <action>Re-read RN-010 and the closed cancellation row and verify the two edges Pitfall 3 warns about are stated explicitly: admin may cancel INDEFERIDO as cleanup, and CONCLUIDO plus CANCELADO are immutable — add the missing sentence if either edge is absent, without changing locked meaning. Confirm every D-05, D-06, D-07, D-08 maps to at least one sentence (reviewer traceability D-to-sentence). Confirm no new SUSPENSO_* status string was invented anywhere in the slice (D-11 reuses SUSPENSO_REUNIAO_ORDINARIA). Verify scope with git diff --stat showing only docs/03-regras-de-negocio.md and docs/14-decisoes-em-aberto.md.</action>
  <verify>
    <automated>grep -n 'INDEFERIDO' docs/03-regras-de-negocio.md | grep -v '^#' ; grep -c 'CONCLUIDO' docs/03-regras-de-negocio.md</automated>
    <fails_when>either grep returns zero matches (edge sentence missing from the RN)</fails_when>
  </verify>
  <acceptance_criteria>RN-010 states the INDEFERIDO-by-admin cleanup edge and the CONCLUIDO/CANCELADO immutability edge explicitly; D-05 through D-08 each trace to a sentence; no invented SUSPENSO_* string in the slice; diff touches only the two doc files</acceptance_criteria>
  <done>Cancellation slice is edge-complete, traceable D-to-sentence, uses exact code vocabulary, and stays inside the two-file scope.</done>
  <reversibility rating="reversible">Markdown-only refinements, revertible with git revert; no runtime effect.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs/03 → Phase 6 code | Phase 6 implements whatever the RN says — a headline-only matrix ("admin/chefe can cancel") without the status split becomes an authorization hole |
| docs/03 → Phase 4 permission map | D-05/D-11 role names feed the deny-by-default transcription — a typo'd role string propagates |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03 | Spoofing / Elevation | RN-010 status-by-role matrix (docs/03) | high | mitigate | Full matrix enumerated (dono RASCUNHO/EM_VOTACAO, admin any non-terminal incl. INDEFERIDO, chefe before CONCLUIDO, CONCLUIDO/CANCELADO immutable) with exact 5-role vocabulary; reviewer traceability D-05..D-08 to sentences (task 2) |
| T-02-04 | Repudiation | Cancellation audit trail (docs/03) | medium | mitigate | RN-010 mandates justification into REVERSE metadata plus AuditEvent request_cancelled/provision_reversed so Phase 6 cannot ship a silent cancel |
| T-02-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs in this phase — `npm install` must not appear; no legitimacy checkpoint needed |
</threat_model>

<verification>
grep -n 'request_cancelled\|provision_reversed\|REVERSE' docs/03-regras-de-negocio.md docs/14-decisoes-em-aberto.md shows matches in both files; INDEFERIDO and CONCLUIDO edges present in RN-010; git diff --stat shows only the two doc files.
</verification>

<success_criteria>
RN-010 states the full cancellation matrix prescriptively with exact strings and the D-05..D-08 decision stamp; the docs/14 cancellation row is closed with date plus rationale; the collegiateDecision gap is flagged for Phase 6; the slice is independently reviewable.
</success_criteria>

## Artifacts this phase produces

- New RN entry: RN-010 (Cancelamento após aprovação) in docs/03-regras-de-negocio.md — dono RASCUNHO/EM_VOTACAO, ADMINISTRADOR any non-terminal including INDEFERIDO cleanup, CHEFE_DEPARTAMENTO before CONCLUIDO, mandatory justification (missing = 400) into REVERSE metadata plus AuditEvent, compensating REVERSE FinancialTransaction, CONCLUIDO/CANCELADO immutable, decision stamp (Decidido em 2026-09-23, D-05...D-08).
- Closed docs/14 row: cancellation row closed with matrix plus date 2026-09-23 plus rationale.
- Decision stamp: each new sentence traceable to D-05, D-06, D-07, or D-08 (no code symbols produced — code files are read-only evidence).
- Explicit flag: D-11 collegiateDecision chefe-only code gap recorded for Phase 6 reconciliation (flag, not fix).

<output>
Create `.planning/phases/02-rules-decisions-docs-14-close-out/02-02-SUMMARY.md` when done
</output>
