---
phase: 11-guarded-idempotent-reconciliation
plan: "03"
type: execute
wave: 3
depends_on: ["11-02"]
files_modified:
  - tools/release-close/release-close.js
  - tools/release-close/apply-gate.js
autonomous: true
requirements:
  - REC-04
estimate:
  tokens: 40000
  raw_tokens: 30000
  tasks: 1
  confidence: medium
must_haves:
  truths:
    - Duplicate or materially conflicting Release/Milestone objects stop the rehearsal with an actionable conflict and are never overwritten or deleted
    - The conflict detection is deterministic and never silently overwrites
    - Conflicts are reported with the natural GitHub keys and stable IDs
  artifacts:
    - tools/release-close/release-close.js (conflict detection)
    - tools/release-close/apply-gate.js (conflict detection)
  key_links:
    - The conflict detection is deterministic and never silently overwrites
    - Conflicts are reported with the natural GitHub keys and stable IDs
  prohibitions:
    - never silently overwrite a duplicate or conflicting object
    - never delete a Release or Milestone
---

<objective>
Add conflict detection for duplicate or conflicting Release/Milestone objects. The conflict detection is deterministic and never silently overwrites. Conflicts are reported with the natural GitHub keys and stable IDs.

Purpose: Prove the apply path stops on duplicate or conflicting objects with an actionable conflict.
Output: Conflict detection that never silently overwrites or deletes.
</objective>

<execution_context>
@/home/lucas/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/lucas/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-guarded-idempotent-reconciliation/11-01-PLAN.md
@tools/release-close/release-close.js
@tools/release-close/apply-gate.js
</context>