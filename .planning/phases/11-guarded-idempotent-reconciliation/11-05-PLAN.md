---
phase: 11-guarded-idempotent-reconciliation
plan: "05"
type: execute
wave: 5
depends_on: ["11-04"]
files_modified:
  - tools/release-close/apply.test.js
autonomous: true
requirements:
  - REC-01
  - REC-02
  - REC-03
  - REC-04
  - REC-05
estimate:
  tokens: 40000
  raw_tokens: 30000
  tasks: 1
  confidence: medium
must_haves:
  truths:
    - Full test suite for the apply path with reconciliation sequence
    - Tests prove the apply path blocks concurrent invocations
    - Tests prove the apply path retries with re-read after failures
    - Tests prove the apply path stops on duplicate or conflicting objects
    - All tests pass on a clean checkout with no network and no database
  artifacts:
    - tools/release-close/apply.test.js
  key_links:
    - Tests prove the apply path executes the reconciliation sequence
    - Tests prove the apply path blocks concurrent invocations
    - Tests prove the apply path retries with re-read after failures
    - Tests prove the apply path stops on duplicate or conflicting objects
  prohibitions:
    - never claim verification passed without executing the tests
---

<objective>
Add tests for the apply path with reconciliation sequence. Tests prove the apply path executes the reconciliation sequence, blocks concurrent invocations, retries with re-read after failures, and stops on duplicate or conflicting objects.

Purpose: Prove the apply path works correctly with tests.
Output: Full test suite for the apply path.
</objective>

<execution_context>
@/home/lucas/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/lucas/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-guarded-idempotent-reconciliation/11-01-PLAN.md
@tools/release-close/release-close.js
@tools/release-close/apply-gate.js
</context>