---
phase: 11-guarded-idempotent-reconciliation
plan: "01"
type: execute
wave: 1
depends_on: ["10-08"]
files_modified:
  - tools/release-close/release-close.js
  - tools/release-close/apply-gate.js
autonomous: true
requirements:
  - REC-01
  - REC-02
estimate:
  tokens: 50000
  raw_tokens: 35000
  tasks: 2
  confidence: medium
must_haves:
  truths:
    - The apply path executes the reconciliation sequence: Release draft → readback → publish → readback → Milestone open → readback → close → final readback
    - Each step uses a fresh snapshot reconstructed after every transition
    - The reconciliation sequence is ordered deterministically and never skips a step
    - All rehearsal writes remain on fake clients or disposable fixtures rather than ldsampaio/sgrf
  artifacts:
    - tools/release-close/release-close.js (apply path)
    - tools/release-close/apply-gate.js (reconciliation sequence)
  key_links:
    - The apply path now executes the reconciliation sequence on fake clients
    - Each step uses a fresh snapshot reconstructed after every transition
    - The reconciliation sequence is ordered deterministically and never skips a step
  prohibitions:
    - never write to the remote in the apply path; all rehearsal writes remain on fake clients or disposable fixtures
    - never skip a step in the reconciliation sequence
---

<objective>
Implement the apply path with the reconciliation sequence: Release draft → readback → publish → readback → Milestone open → readback → close → final readback. Each step uses a fresh snapshot reconstructed after every transition. The reconciliation sequence is ordered deterministically and never skips a step. All rehearsal writes remain on fake clients or disposable fixtures rather than ldsampaio/sgrf.

Purpose: Prove the apply path executes the reconciliation sequence with a fresh snapshot after every transition.
Output: Apply path executing the reconciliation sequence on fake clients.
</objective>

<execution_context>
@/home/lucas/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/lucas/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-read-only-exact-sha-preflight/10-VERIFICATION.md
@tools/release-close/release-close.js
@tools/release-close/apply-gate.js
</context>