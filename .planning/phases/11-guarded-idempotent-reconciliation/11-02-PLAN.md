---
phase: 11-guarded-idempotent-reconciliation
plan: "02"
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - tools/release-close/release-close.js
  - tools/release-close/apply-gate.js
autonomous: true
requirements:
  - REC-03
estimate:
  tokens: 40000
  raw_tokens: 30000
  tasks: 1
  confidence: medium
must_haves:
  truths:
    - After a timeout, lost response, 409, 422, 429, or server failure, the tool re-reads GitHub before adopting, retrying, or reporting a partial state and never blindly repeats a write
    - The retry logic is bounded and never retries indefinitely
    - Each retry uses a fresh snapshot reconstructed after the failure
  artifacts:
    - tools/release-close/release-close.js (retry logic)
    - tools/release-close/apply-gate.js (retry logic)
  key_links:
    - The retry logic is bounded and never retries indefinitely
    - Each retry uses a fresh snapshot reconstructed after the failure
  prohibitions:
    - never blindly repeat a write after a failure; always re-read GitHub first
    - never retry indefinitely; always bound the retries
---

<objective>
Add retry with re-read after failures (timeout, lost response, 409, 422, 429, server failure). The retry logic is bounded and never retries indefinitely. Each retry uses a fresh snapshot reconstructed after the failure.

Purpose: Prove the apply path re-reads GitHub after failures before retrying.
Output: Retry logic with bounded retries and fresh snapshots after failures.
</objective>

<execution_context>
@/home/lucas/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/lucas/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-guarded-idempotent-reconciliation/11-01-PLAN.md
@tools/release-close/release-close.js
@tools/release-close/apply-gate.js
</context>