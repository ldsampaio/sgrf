---
phase: 11-guarded-idempotent-reconciliation
plan: "04"
type: execute
wave: 4
depends_on: ["11-03"]
files_modified:
  - tools/release-close/release-close.js
  - tools/release-close/apply-gate.js
autonomous: true
requirements:
  - REC-05
estimate:
  tokens: 40000
  raw_tokens: 30000
  tasks: 1
  confidence: medium
must_haves:
  truths:
    - Concurrent invocations for one repository/version on the same workstation are blocked by a local lock
    - Historical resume rejects a target without recorded prior partial-state evidence
    - All rehearsal writes remain on fake clients or disposable fixtures rather than ldsampaio/sgrf
  artifacts:
    - tools/release-close/release-close.js (local lock)
    - tools/release-close/apply-gate.js (local lock)
  key_links:
    - Concurrent invocations are blocked by a local lock
    - Historical resume rejects a target without recorded prior partial-state evidence
    - All rehearsal writes remain on fake clients or disposable fixtures
  prohibitions:
    - never write to the remote in the apply path; all rehearsal writes remain on fake clients or disposable fixtures
    - never resume a target without recorded prior partial-state evidence
---

<objective>
Add local lock for concurrent invocations. Concurrent invocations for one repository/version on the same workstation are blocked by a local lock. Historical resume rejects a target without recorded prior partial-state evidence. All rehearsal writes remain on fake clients or disposable fixtures rather than ldsampaio/sgrf.

Purpose: Prove the apply path blocks concurrent invocations with a local lock.
Output: Local lock for concurrent invocations and resume rejection.
</objective>

<execution_context>
@/home/lucas/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/lucas/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-guarded-idempotent-reconciliation/11-01-PLAN.md
@tools/release-close/release-close.js
@tools/release-close/apply-gate.js
</context>