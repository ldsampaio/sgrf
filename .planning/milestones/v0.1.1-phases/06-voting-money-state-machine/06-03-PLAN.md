---
phase: 06-voting-money-state-machine
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
  - 06-02
files_modified:
  - backend/tests/voting.test.js
  - backend/src/services/votingService.js
  - backend/src/controllers/votingController.js
  - backend/src/app.js
  - backend/src/jobs/votingCloser.js
autonomous: true
requirements:
  - VOT-03
user_setup: []

estimate:
  tokens: 42000
  raw_tokens: 21000
  tasks: 4
  confidence: high

must_haves:
  truths:
    - "Any DEFERIR_PARCIALMENTE vote moves request to AGUARDANDO_ARBITRAGEM (logical state using APROVADO_PARCIALMENTE + arbitration metadata)"
    - "Chefe explicitly sets final approved amount via arbitration endpoint with mandatory justification"
    - "Arbitration amount must be in (0, requestedAmountCents] range"
    - "Audit trail records decidedBy=CHEFE_DEPARTAMENTO, action=partial_arbitration"
    - "All three closeVoting callers handle arbitration return: castVote, closeManual, votingCloser"
  artifacts:
    - backend/tests/voting.test.js (extended with VOT-03 characterization tests)
    - backend/src/services/votingService.js (closeVoting modified for arbitration flow, returns arbitration indicator)
    - backend/src/controllers/votingController.js (new partialArbitration handler + castVote/closeManual updated)
    - backend/src/app.js (new PATCH /api/requests/:id/partial-arbitration route)
    - backend/src/jobs/votingCloser.js (closeExpired updated to handle arbitration return)
  key_links:
    - "closeVoting → AGUARDANDO_ARBITRAGEM (logical) → partialArbitration endpoint → conditional FundBalance provision → APROVADO_PARCIALMENTE"
    - "castVote/closeManual/votingCloser → closeVoting return value handling"

---

<objective>
Implement VOT-03 partial-approval aggregation rule per RN-009 (Phase 2 decision): replace "first partial vote wins" with chefe arbitration. When any DEFERIR_PARCIALMENTE vote exists, closeVoting moves the request to AGUARDANDO_ARBITRAGEM (logical state using APROVADO_PARCIALMENTE status + decisionReason/collegiateMinutes metadata) instead of auto-concluding with the first partial vote's amount. The chefe then explicitly sets the final approved amount via a new arbitration endpoint with mandatory justification. All three callers of closeVoting (castVote, closeManual, votingCloser) are updated to handle the arbitration return value.

Purpose: Implement the decided aggregation rule deliberately with tests encoding the correct behavior, removing the arbitrary first-vote-wins bias. Fix the return-value breaking change across all callers per Plan Checker BLOCKER B-1. Define AGUARDANDO_ARBITRAGEM persistence strategy per Plan Checker BLOCKER B-2 (Option B: existing status + metadata).

Output: Modified closeVoting with arbitration indicator return, new arbitration endpoint, updated callers, characterization tests.
</objective>

<execution_context>
@/home/lucas/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/lucas/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-voting-money-state-machine/06-RESEARCH.md
@.planning/phases/06-voting-money-state-machine/06-CONTEXT.md
@backend/src/services/votingService.js
@backend/src/controllers/votingController.js
@backend/src/app.js
@backend/src/jobs/votingCloser.js
@backend/tests/voting.test.js
@docs/03-regras-de-negocio.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: VOT-03 Characterization tests — partial arbitration flow</name>
  <files>
    backend/tests/voting.test.js
  </files>
  <behavior>
    - Test 1: closeVoting with any DEFERIR_PARCIALMENTE vote → returns { needsArbitration: true, status: 'APROVADO_PARCIALMENTE', arbitration: true } (logical AGUARDANDO_ARBITRAGEM), NOT APROVADO_PARCIALMENTE with amount
    - Test 2: partialArbitration endpoint — chefe sets final amount in (0, requestedAmountCents] with justification → status becomes APROVADO_PARCIALMENTE, FundBalance provisioned, arbitration metadata cleared
    - Test 3: partialArbitration rejects amount <= 0 or > requestedAmountCents → 400
    - Test 4: partialArbitration rejects missing justification → 400
    - Test 5: partialArbitration rejects non-chefe users → 403
    - Test 6: AuditEvent created with action=partial_arbitration, decidedBy=CHEFE_DEPARTAMENTO, justification in afterData
    - Test 7: castVote in tie-break (AGUARDANDO_DESEMPATE) calling closeVoting handles arbitration return correctly
    - Test 8: closeManual calling closeVoting handles arbitration return correctly
    - Test 9: votingCloser.closeExpired calling closeVoting handles arbitration return (logs, doesn't crash)
  </behavior>
  <action>
Write characterization-then-flip tests for VOT-03 in backend/tests/voting.test.js BEFORE implementing changes. Tests encode the CORRECT behavior per RN-009, CONTEXT.md D-03, and RESEARCH.md Assumption A1:

1. "closeVoting moves to arbitration on any partial vote" — setup votes with at least one DEFERIR_PARCIALMENTE, call closeVoting, verify return value indicates arbitration needed (not auto-concluded with amount). Return object: { needsArbitration: true, status: 'APROVADO_PARCIALMENTE', arbitration: true, requestId } — status is APROVADO_PARCIALMENTE but arbitration flag signals awaiting chefe decision.

2. "Chefe arbitrates final amount with justification" — call new PATCH /api/requests/:id/partial-arbitration with valid amount + justification, verify APROVADO_PARCIALMENTE + provision + arbitration metadata cleared.

3. "Arbitration validates amount range" — amount <= 0 or > requestedAmountCents returns 400.

4. "Arbitration requires justification" — missing justification returns 400.

5. "Arbitration requires chefe role" — conselheiro/admin gets 403.

6. "Audit trail recorded" — AuditEvent with partial_arbitration action and justification.

7. "castVote handles arbitration return" — when tie-break vote triggers closeVoting and arbitration is needed, castVote returns the arbitration indicator in response (not the closed request with amount).

8. "closeManual handles arbitration return" — admin/chefe manual close returns arbitration indicator for frontend to show arbitration UI.

9. "votingCloser handles arbitration return" — cron job logs arbitration needed and continues (doesn't crash on unexpected return shape).

These tests WILL FAIL initially because: (a) closeVoting uses first-partial-wins logic (lines 81-88), (b) no arbitration endpoint exists, (c) callers expect closed request object, not arbitration indicator.
  </action>
  <verify>
    <automated>cd backend && npx vitest run voting.test.js -t "VOT-03"</automated>
    <fails_when>Any test in the "VOT-03: Partial approval arbitration" describe block fails OR the test file has no tests matching "VOT-03"</fails_when>
  </verify>
  <done>
VOT-03 tests written and failing (characterization phase complete — red)
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement arbitration flow in closeVoting + new endpoint + update all callers</name>
  <files>
    backend/src/services/votingService.js
    backend/src/controllers/votingController.js
    backend/src/app.js
    backend/src/jobs/votingCloser.js
  </files>
  <action>
Implement the arbitration flow per D-03 in CONTEXT.md, RN-009, and RESEARCH.md Assumption A1 (AGUARDANDO_ARBITRAGEM is logical state using APROVADO_PARCIALMENTE + metadata). Fix BLOCKER B-1 (caller return value handling) and BLOCKER B-2 (persistence strategy).

**votingService.js closeVoting (lines 59-111):**
- REMOVE: `const p = validVotes.find((v) => v.voteType === 'DEFERIR_PARCIALMENTE'); approvedCents = p ? p.approvedAmountCents : 0;` (lines 83-85) — the first-partial-wins logic.
- ADD: Check `validVotes.some(v => v.voteType === 'DEFERIR_PARCIALMENTE')` — if true:
  - Set status = 'APROVADO_PARCIALMENTE' (existing enum value, per Assumption A1)
  - Set approvedCents = 0 (no provision yet)
  - Set decisionReason = 'AGUARDANDO_ARBITRAGEM' (metadata flag)
  - Set collegiateMinutes = 'Aguardando arbitragem do chefe — voto parcial detectado' (metadata flag)
  - Do NOT provision FundBalance
  - Return arbitration indicator: `{ needsArbitration: true, status: 'APROVADO_PARCIALMENTE', arbitration: true, requestId: r.id }`
- ELSE: proceed with existing logic (DEFERIDO, INDEFERIDO, EMPATE, SEM_VOTOS)
- For APROVADO/APROVADO_PARCIALMENTE (non-arbitration): provision FundBalance as before, return updated request object (existing behavior)
- The return type is now a discriminated union: either the updated request (when no arbitration) or the arbitration indicator object. Callers MUST check `needsArbitration` before accessing request properties.

**votingController.js — new partialArbitration handler:**
- Create `async function partialArbitration(req, res, next)`
- Validate: user.role === 'CHEFE_DEPARTAMENTO' (403 otherwise)
- Validate: request exists, request.status === 'APROVADO_PARCIALMENTE' AND request.decisionReason === 'AGUARDANDO_ARBITRAGEM' (400 otherwise — not in arbitration state)
- Validate: body.approvedAmountCents in (0, requestedAmountCents] (400 otherwise)
- Validate: body.justification non-empty string (400 otherwise)
- Inside $transaction:
  - Conditional FundBalance updateMany (GA-VOT-05 pattern — Wave 4 will harden; for now use existing read-then-write pattern inside transaction, Wave 4 will refactor to conditional updateMany)
  - Create FinancialTransaction type PROVISION with metadata: { decidedBy: 'CHEFE_DEPARTAMENTO', action: 'partial_arbitration', justification: body.justification }
  - Create AuditEvent action: 'partial_arbitration', afterData: { status: 'APROVADO_PARCIALMENTE', approvedAmountCents: body.approvedAmountCents, justification: body.justification }
  - Update request: status = 'APROVADO_PARCIALMENTE', approvedAmountCents = body.approvedAmountCents, decidedAt = new Date(), decidedBy = req.user.id, decisionReason = null (clear arbitration flag), collegiateMinutes = body.justification (store justification)
  - Return updated request

**votingController.js — update castVote (line 62-64):**
- After `const closed = await closeVoting(r.id, req.user.id);`
- ADD: Check `if (closed?.needsArbitration) { return res.status(201).json({ vote: ..., arbitration: closed }); }`
- This returns the arbitration indicator to frontend so it can show arbitration UI

**votingController.js — update closeManual (line 136):**
- After `const closed = await closeVoting(r.id, req.user.id);`
- ADD: Check `if (closed?.needsArbitration) { await audit({ ... action: 'voting_closed_arbitration', afterData: closed, req }); return res.json({ arbitration: closed }); }`
- Audit action distinguishes arbitration closure from normal closure

**app.js:** Add route `PATCH /api/requests/:id/partial-arbitration` → votingController.partialArbitration with auth middleware (requirePermission('requests:arbitrate') or inline role check)

**votingCloser.js closeExpired (line 14):**
- After `await closeVoting(r.id, 'system-cron');`
- ADD: Check `if (result?.needsArbitration) { logger.info({ requestId: r.id }, 'voting auto-closed → arbitration needed'); continue; }`
- Don't throw on arbitration return; log and continue to next expired request
  </action>
  <verify>
    <automated>cd backend && npx vitest run voting.test.js -t "VOT-03"</automated>
    <fails_when>Any VOT-03 test fails after implementation</fails_when>
  </verify>
  <done>
VOT-03 tests pass: closeVoting routes to arbitration on any partial vote, chefe sets final amount with justification, all three callers handle arbitration return correctly, audit trail recorded, FundBalance provisioned after arbitration
  </done>
</task>

<task type="auto">
  <name>Task 3: Frontend arbitration UI — chefe sets amount + justification</name>
  <files>
    frontend/src/views/Requests.vue
  </files>
  <action>
Implement the arbitration UI in frontend/src/views/Requests.vue per Advisory I-2 and CONTEXT.md D-03 (chefe explicitly sets final value approved):

- In the request detail/view where status is displayed, add conditional logic:
  - If request.status === 'APROVADO_PARCIALMENTE' AND request.decisionReason === 'AGUARDANDO_ARBITRAGEM' AND current user is CHEFE_DEPARTAMENTO:
    - Show arbitration panel with:
      - Label: "Arbitragem necessária — voto parcial detectado"
      - MoneyInput for final approved amount (min: 1, max: requestedAmountCents, required)
      - Textarea for justification (required, rows=3, placeholder "Justificativa obrigatória para arbitragem")
      - Button "Confirmar Arbitragem" → calls PATCH /api/requests/:id/partial-arbitration with { approvedAmountCents, justification }
      - Loading state during request
      - On success: refresh request data, show success toast, arbitration panel disappears
      - On error: show error alert with server message
  - Reuse existing patterns: MoneyInput, StatusBadge, alert/error display, api.js with withCredentials
  - Ensure validation messages match backend 400/403 responses

- No new components — extend existing Requests.vue with conditional arbitration section.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
    <fails_when>Frontend build fails OR grep for 'partial-arbitration' in Requests.vue returns 0 matches</fails_when>
  </verify>
  <done>
Frontend build passes; Requests.vue shows arbitration UI for chefe when request is in AGUARDANDO_ARBITRAGEM logical state; arbitration API call works end-to-end
  </done>
</task>

<task type="auto">
  <name>Task 4: Regression guard — full voting test suite</name>
  <files>
    backend/tests/voting.test.js
  </files>
  <action>
Run the full voting.test.js suite to ensure VOT-03 changes don't break VOT-01, VOT-02, or pre-existing tests. The arbitration flow replaces first-partial-wins, so any test expecting the old behavior must be updated to match the new correct behavior. Verify tie-break (VOT-01) and annual cap (VOT-02) still work correctly with the new arbitration path.

Additionally verify:
- All three callers (castVote, closeManual, votingCloser) handle the arbitration return without crashing
- The arbitration indicator return shape is consistent: { needsArbitration: true, status: 'APROVADO_PARCIALMENTE', arbitration: true, requestId }
- Frontend build passes
  </action>
  <verify>
    <automated>cd backend && npx vitest run voting.test.js</automated>
    <fails_when>Any test in voting.test.js fails (including VOT-01, VOT-02, VOT-03, pre-existing)</fails_when>
  </verify>
  <verify>
    <automated>cd frontend && npm run build</automated>
    <fails_when>Frontend build fails</fails_when>
  </verify>
  <done>
Full voting.test.js suite passes — VOT-01, VOT-02, VOT-03, and all pre-existing tests green; all three closeVoting callers handle arbitration return; frontend build passes
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → API (arbitration endpoint) | Untrusted chefe input; amount range + justification validated server-side |
| Browser → API (vote endpoints) | Untrusted user input; arbitration return shape handled by frontend |
| API → Database (FundBalance provision) | Trusted internal; conditional update prevents overspend (GA-VOT-05 later) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-07 | Elevation of Privilege | partialArbitration endpoint | high | mitigate | Explicit role check: only CHEFE_DEPARTAMENTO; amount range validated; justification required |
| T-06-08 | Tampering | closeVoting arbitration logic | medium | mitigate | Centralized in votingService; no client-controlled arbitration trigger |
| T-06-09 | Repudiation | Arbitration audit trail | low | mitigate | AuditEvent with partial_arbitration action, justification, decidedBy recorded |
| T-06-10 | Information Disclosure | Arbitration amount exposure | low | accept | Amount visible to requester/council per business rules; no secret data |
| T-06-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages |

</threat_model>

<verification>
Wave 2 verification: `cd backend && npx vitest run voting.test.js -t "VOT-03"` must pass. Full suite `cd backend && npx vitest run voting.test.js` must pass. Frontend `npm run build` must pass. All three callers handle arbitration return verified by Tests 7-9.
</verification>

<success_criteria>
- closeVoting detects any DEFERIR_PARCIALMENTE vote and returns arbitration indicator (no auto-conclude with first partial amount)
- AGUARDANDO_ARBITRAGEM logical state uses APROVADO_PARCIALMENTE status + decisionReason='AGUARDANDO_ARBITRAGEM' + collegiateMinutes metadata (per Assumption A1)
- New PATCH /api/requests/:id/partial-arbitration endpoint exists and works
- Chefe can set final amount in (0, requestedAmountCents] with mandatory justification
- Non-chefe users get 403; invalid amounts get 400; missing justification gets 400
- FundBalance provisioned conditionally after arbitration; FinancialTransaction + AuditEvent created with correct metadata
- castVote, closeManual, and votingCloser all handle arbitration return correctly (no crashes, proper responses)
- Frontend arbitration UI works for chefe
- All VOT-01, VOT-02, VOT-03 tests pass; no regression in pre-existing tests
</success_criteria>

<output>
Create `.planning/phases/06-voting-money-state-machine/06-03-SUMMARY.md` when done
</output>