---
phase: 06-voting-money-state-machine
plan: 05
type: execute
wave: 4
depends_on:
  - 06-01
  - 06-02
  - 06-03
  - 06-04
files_modified:
  - backend/tests/voting.test.js
  - backend/src/controllers/requestController.js
  - backend/src/services/votingService.js
  - backend/src/controllers/votingController.js
  - backend/src/controllers/financeController.js
  - backend/src/controllers/settingsController.js
autonomous: true
requirements:
  - GA-VOT-05
user_setup: []

estimate:
  tokens: 48000
  raw_tokens: 24000
  tasks: 4
  confidence: high

must_haves:
  truths:
    - "All 6 balance-write sites use conditional updateMany with availableCents/provisionedCents >= amount check"
    - "No balance reads occur outside of transactions"
    - "Concurrent submissions at any write site cannot overspend — only floor(availableCents/amount) succeed"
    - "FundBalance availableCents never goes negative"
    - "FinancialTransaction sum matches FundBalance deltas"
    - "patchBalance (settings balance adjustment) wrapped in single transaction with both operations"
  artifacts:
    - backend/tests/voting.test.js (extended with GA-VOT-05 concurrency tests)
    - backend/src/controllers/requestController.js (submit, markSpent fixed)
    - backend/src/services/votingService.js (closeVoting fixed — rework from 06-03 acknowledged)
    - backend/src/controllers/votingController.js (collegiateDecision, partialArbitration fixed)
    - backend/src/controllers/financeController.js (markSpent, reverseProvision fixed)
    - backend/src/controllers/settingsController.js (patchBalance fixed — 6th write site confirmed)
  key_links:
    - "6 write sites → conditional updateMany pattern → FundBalance atomic decrement → TOCTOU eliminated"
    - "06-03/06-04 balance updates refactored again in 06-05 (progressive hardening per W-2)"

---

<objective>
Implement GA-VOT-05 money-path TOCTOU race guard: refactor all 6 balance-mutation write sites to use Prisma conditional `updateMany` with `where: { availableCents: { gte: amount } }` (or `provisionedCents: { gte: amount }` for spend/reverse) inside `$transaction`, eliminating read-then-write race conditions. Add concurrency test simulating simultaneous submissions to prove the fix.

**Scope Note (W-3 confirmed):** ROADMAP.md mentions `patchBalance` as potential 6th write site. Codebase search confirms `settingsController.patchBalance` (lines 37-55) performs `fundBalance.upsert` + `financialTransaction.create` — two separate operations outside a transaction. This is the 6th write site and MUST be included in GA-VOT-05 hardening.

**Rework Acknowledgement (W-2):** Plans 06-03 and 06-04 implement balance updates using the current read-then-write pattern (Wave 2/3). Plan 06-05 (Wave 4) refactors ALL write sites including those just modified in 06-03 (closeVoting, partialArbitration) and 06-04 (cancellation reversal) to the conditional updateMany pattern. This progressive hardening is intentional — early waves establish correct behavior with tests; Wave 4 hardens the concurrency safety. The concurrency tests in Task 1 cover ALL 6 sites including the arbitration and cancellation paths.

Purpose: Make it impossible for the department to overspend under concurrency — the core "funds cannot leak" invariant.
Output: 6 write sites hardened with atomic conditional updates + concurrency test.
</objective>

<execution_context>
@/home/lucas/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/lucas/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-voting-money-state-machine/06-RESEARCH.md
@.planning/phases/06-voting-money-state-machine/06-CONTEXT.md
@backend/src/controllers/requestController.js
@backend/src/services/votingService.js
@backend/src/controllers/votingController.js
@backend/src/controllers/financeController.js
@backend/src/controllers/settingsController.js
@backend/tests/voting.test.js
@.planning/codebase/CONCERNS.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: GA-VOT-05 Characterization test — concurrency stress test</name>
  <files>
    backend/tests/voting.test.js
  </files>
  <behavior>
    - Test 1: N concurrent submissions (submit endpoint) with total requested > availableCents → only floor(available/amount) succeed, rest get 400 "Insufficient balance"
    - Test 2: N concurrent closeVoting calls on different requests → only floor(available/sum) provisions succeed
    - Test 3: N concurrent collegiateDecision calls → same guarantee
    - Test 4: N concurrent markSpent calls → same guarantee (spending provisioned amount)
    - Test 5: N concurrent reverseProvision/cancel calls → same guarantee (reversing provisioned amount)
    - Test 6: N concurrent partialArbitration calls → same guarantee (provisioning after arbitration)
    - Test 7: N concurrent patchBalance adjustments → same guarantee (balance adjustments atomic)
    - Test 8: After all concurrent tests, FundBalance.availableCents >= 0 AND sum(FinancialTransaction) matches FundBalance deltas
  </behavior>
  <action>
Write characterization-then-flip concurrency tests for GA-VOT-05 in backend/tests/voting.test.js BEFORE refactoring write sites. Tests encode the CORRECT behavior per RESEARCH.md Pattern 2 and CONTEXT.md GA-VOT-05:

Use real PostgreSQL (test DB) with supertest to fire concurrent requests:
- Setup: FundBalance with known availableCents (e.g., 10000 cents)
- Spawn N=5 parallel requests each requesting 3000 cents (total 15000 > 10000)
- Assert: exactly 3 succeed (floor(10000/3000)), 2 fail with 400
- Verify: FundBalance.availableCents === 1000 (10000 - 3*3000)
- Verify: FinancialTransaction count matches successes

Repeat pattern for each of the 6 write sites (adapting request shape):
1. **requestController.submit** (auto-approval provision) — POST /api/requests
2. **votingService.closeVoting** (vote closure provision) — includes arbitration path from 06-03
3. **votingController.collegiateDecision** (chefe decision from suspended) — POST /api/requests/:id/collegiate-decision
4. **financeController.markSpent** (spend provisioned) — POST /api/requests/:id/mark-spent
5. **financeController.reverseProvision / requestController.cancel** (reverse) — POST /api/requests/:id/cancel (approved statuses)
6. **settingsController.patchBalance** (admin balance adjustment) — PATCH /api/settings/balance

For Sites 4, 5, 6 the conditional check differs:
- markSpent: `where: { referenceYear, provisionedCents: { gte: amount } }` (check provisioned, not available)
- reverseProvision/cancel: `where: { referenceYear, provisionedCents: { gte: amount } }` (check provisioned)
- patchBalance: `where: { referenceYear }` with version check (optimistic lock via version increment) — different pattern since it sets absolute values

**Critical:** Move ALL balance reads INSIDE the $transaction. No findUnique before the transaction. The conditional updateMany IS the check (except patchBalance which uses version-based optimistic locking).

This test WILL FAIL initially because current code uses read-then-write pattern vulnerable to TOCTOU at all 6 sites.
  </action>
  <verify>
    <automated>cd backend && npx vitest run voting.test.js -t "GA-VOT-05"</automated>
    <fails_when>Any test in the "GA-VOT-05: TOCTOU race guard" describe block fails OR the test file has no tests matching "GA-VOT-05"</fails_when>
  </verify>
  <done>
GA-VOT-05 concurrency tests written and failing (characterization phase complete — red)
  </done>
</task>

<task type="auto">
  <name>Task 2: Refactor all 6 write sites to conditional updateMany pattern</name>
  <files>
    backend/src/controllers/requestController.js
    backend/src/services/votingService.js
    backend/src/controllers/votingController.js
    backend/src/controllers/financeController.js
    backend/src/controllers/settingsController.js
  </files>
  <action>
Refactor each of the 6 write sites per RESEARCH.md Pattern 2 and CONTEXT.md GA-VOT-05 decision. The pattern:

```javascript
// OLD (race prone):
const bal = await tx.fundBalance.findUnique({ where: { referenceYear } });
if (!bal || bal.availableCents < amount) throw new Error('Insufficient');
await tx.fundBalance.update({ where: { referenceYear }, data: { availableCents: { decrement: amount }, provisionedCents: { increment: amount }, version: { increment: 1 } } });

// NEW (atomic) — for provisioning (Sites 1, 2, 3, 6-partialArbitration):
const result = await tx.fundBalance.updateMany({
  where: { referenceYear, availableCents: { gte: amount } },
  data: { availableCents: { decrement: amount }, provisionedCents: { increment: amount }, version: { increment: 1 } }
});
if (result.count === 0) throw new Error('Insufficient balance');
```

**Site 1: requestController.submit (auto-approval provision)** — lines where FundBalance updated for new auto-approved request. Wrap in $transaction, use conditional updateMany.

**Site 2: votingService.closeVoting (vote closure provision)** — lines 95-106 where FundBalance updated after tally. **Note: This was modified in 06-03 for arbitration flow; refactor again here to conditional updateMany (progressive hardening per W-2).** Only provision when NOT in arbitration state (arbitration path provisions in partialArbitration handler).

**Site 3: votingController.collegiateDecision (chefe decision provision)** — lines 207-216 where FundBalance updated for chefe decision from suspended. Wrap in $transaction, use conditional updateMany.

**Site 4: financeController.markSpent (spend provisioned)** — lines where provisionedCents→spentCents (different: decrement provisioned, increment spent). Conditional check: `where: { referenceYear, provisionedCents: { gte: amount } }`.

**Site 5: financeController.reverseProvision / requestController.cancel (reverse)** — lines where provisionedCents→availableCents (decrement provisioned, increment available). Conditional check: `where: { referenceYear, provisionedCents: { gte: amount } }`. **Note: 06-04 implements inline reversal in requestController.cancel; refactor that inline code to conditional updateMany here.**

**Site 6: settingsController.patchBalance (admin balance adjustment)** — lines 37-55. Current pattern uses `upsert` + separate `financialTransaction.create` outside transaction. Refactor to:
- Wrap both operations in single $transaction
- Use optimistic locking: `where: { referenceYear, version: currentVersion }` on update (not updateMany since setting absolute values)
- If version mismatch (count === 0), throw 409 "Balance modified concurrently — retry"
- This is different from the availableCents gte pattern because patchBalance sets absolute values, not decrements

**Site 6b: votingController.partialArbitration (arbitration provision)** — new handler from 06-03. **Note: This was added in 06-03 with read-then-write; refactor to conditional updateMany here (progressive hardening per W-2).** Use same pattern as Site 2.

**Critical for all sites:** Move ALL balance reads INSIDE the $transaction. No findUnique before the transaction. The conditional updateMany IS the check (except patchBalance which uses version check).

Keep existing FinancialTransaction + AuditEvent creation after the conditional update (still inside transaction).
  </action>
  <verify>
    <automated>cd backend && npx vitest run voting.test.js -t "GA-VOT-05"</automated>
    <fails_when>Any GA-VOT-05 concurrency test fails after refactoring</fails_when>
  </verify>
  <done>
All 6 write sites use conditional updateMany (or version check for patchBalance); GA-VOT-05 concurrency tests pass; FundBalance never negative under concurrency
  </done>
</task>

<task type="auto">
  <name>Task 3: Full regression suite + integration verification + grep audit</name>
  <files>
    backend/tests/voting.test.js
  </files>
  <action>
Run the complete backend test suite to ensure GA-VOT-05 refactoring doesn't break VOT-01 through VOT-04 or any pre-existing functionality. The conditional updateMany pattern must work correctly for all normal (non-concurrent) flows too.

Additionally, verify:
- `cd backend && npx vitest run` (full suite)
- No `findUnique` on fundBalance OUTSIDE of $transaction in any of the 6 modified files (grep check) — **extended per Advisory I-1 to cover new partialArbitration handler in votingController.js**
- All FinancialTransaction + AuditEvent pairs still created correctly
- Frontend build still passes (no backend API contract changes)
- `grep -rn "fundBalance.findUnique" backend/src/controllers/ backend/src/services/ | grep -v "test" | wc -l` returns 0 for modified files
- `grep -rn "fundBalance.updateMany" backend/src/controllers/ backend/src/services/ | grep -v "test" | wc -l` returns >= 6 (all sites refactored)
  </action>
  <verify>
    <automated>cd backend && npx vitest run</automated>
    <fails_when>Any test in backend test suite fails</fails_when>
  </verify>
  <verify>
    <automated>cd backend && grep -rn "fundBalance.findUnique" src/controllers/ src/services/ | grep -v "test" | wc -l</automated>
    <fails_when>Count > 0 (indicates balance reads outside transaction remain in modified files)</fails_when>
  </verify>
  <verify>
    <automated>cd backend && grep -rn "fundBalance.updateMany" src/controllers/ src/services/ | grep -v "test" | wc -l</automated>
    <fails_when>Count < 6 (not all 6 write sites refactored)</fails_when>
  </verify>
  <verify>
    <automated>cd frontend && npm run build</automated>
    <fails_when>Frontend build fails</fails_when>
  </verify>
  <done>
Full backend test suite passes; no fundBalance.findUnique outside transactions in modified files; all 6 sites use updateMany/version check; frontend build passes; Phase 6 complete
  </done>
</task>

<task type="auto">
  <name>Task 4: Documentation update — GA-VOT-05 implementation notes</name>
  <files>
    docs/03-regras-de-negocio.md
  </files>
  <action>
Update docs/03-regras-de-negocio.md to document the GA-VOT-05 TOCTOU fix for future maintainers. Add a section or note explaining:
- The 6 balance-write sites and their conditional update patterns
- Why conditional updateMany is used instead of read-then-write
- The version-based optimistic locking for patchBalance (absolute value setting)
- That concurrency tests exist in voting.test.js proving the fix

This is a documentation-only task — no code changes. Follow existing documentation style in the file.
  </action>
  <verify>
    <automated>grep -q "GA-VOT-05\|TOCTOU\|conditional updateMany" docs/03-regras-de-negocio.md</automated>
    <fails_when>Pattern not found in docs/03-regras-de-negocio.md</fails_when>
  </verify>
  <done>
docs/03-regras-de-negocio.md updated with GA-VOT-05 implementation notes
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → API (all 6 write endpoints) | Untrusted concurrent requests; atomic DB-level guard prevents overspend |
| API → Database (FundBalance conditional update) | Trusted internal; PostgreSQL row-level lock + conditional WHERE ensures atomicity |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-15 | Tampering | All 6 write sites (TOCTOU) | critical | mitigate | Conditional updateMany with availableCents/provisionedCents gte check — atomic at DB level |
| T-06-16 | Elevation of Privilege | Concurrent request flooding | high | mitigate | DB-level guard limits successes to available balance; excess get 400 |
| T-06-17 | Repudiation | FinancialTransaction audit under concurrency | medium | mitigate | Each successful conditional update creates FinancialTransaction + AuditEvent in same transaction |
| T-06-18 | Denial of Service | Legitimate requests fail under load | low | accept | 400 "Insufficient balance" is correct behavior when funds exhausted; retry with backoff |
| T-06-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages |

</threat_model>

<verification>
Wave 4 verification: `cd backend && npx vitest run voting.test.js -t "GA-VOT-05"` must pass. Full backend suite `cd backend && npx vitest run` must pass. Grep for `fundBalance.findUnique` outside transactions must return 0. Grep for `fundBalance.updateMany` must return >= 6. Frontend `npm run build` must pass. Docs updated with implementation notes.
</verification>

<success_criteria>
- All 6 write sites refactored to conditional updateMany pattern (or version check for patchBalance)
- No fundBalance reads outside $transaction in modified files
- Concurrency test: N parallel requests at each site, only floor(balance/amount) succeed
- FundBalance.availableCents never negative under any concurrency scenario
- FinancialTransaction sum matches FundBalance deltas
- All VOT-01 through VOT-04 and GA-VOT-05 tests pass
- Frontend build passes
- docs/03-regras-de-negocio.md updated with GA-VOT-05 notes
- Phase 6 all requirements verified
</success_criteria>

<output>
Create `.planning/phases/06-voting-money-state-machine/06-05-SUMMARY.md` when done
</output>