---
phase: 06-voting-money-state-machine
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/tests/voting.test.js
  - backend/src/controllers/votingController.js
  - backend/src/services/votingService.js
autonomous: true
requirements:
  - VOT-01
user_setup: []

estimate:
  tokens: 24000
  raw_tokens: 12000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "Chefe can change their vote during AGUARDANDO_DESEMPATE to break a tie"
    - "AGUARDANDO_DESEMPATE always converges to APROVADO or INDEFERIDO (never stuck)"
    - "Chefe cannot vote twice (once in normal phase, once in tie-break)"
  artifacts:
    - backend/tests/voting.test.js (extended with VOT-01 characterization tests)
    - backend/src/controllers/votingController.js (changeMyVote guard extended)
    - backend/src/services/votingService.js (canVote eligibility for tie-break)
  key_links:
    - "changeMyVote status guard → canVote eligibility → closeVoting convergence"

---

<objective>
Implement VOT-01 tie-break convergence: allow chefe to change vote during AGUARDANDO_DESEMPATE so tied requests always reach a terminal status (APROVADO or INDEFERIDO), preventing the dead-end where AGUARDANDO_DESEMPATE gets stuck when chefe already voted in the normal phase. This is the tracer slice — one end-to-end path through vote casting → tie detection → tie-break vote change → closeVoting → terminal status.

Purpose: Fix the critical state machine dead-end bug and establish the test infrastructure pattern (characterization-then-flip) used by all subsequent plans.
Output: Extended voting.test.js with VOT-01 tests + minimal fixes to votingController.changeMyVote and votingService.canVote.
</objective>

<execution_context>
@/home/lucas/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/lucas/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-voting-money-state-machine/06-RESEARCH.md
@.planning/phases/06-voting-money-state-machine/06-CONTEXT.md
@backend/src/controllers/votingController.js
@backend/src/services/votingService.js
@backend/tests/voting.test.js
</context>

<tasks>

<task type="tracer">
  <name>Task 1: VOT-01 Tracer — End-to-end tie-break convergence test + fix</name>
  <files>
    backend/tests/voting.test.js
    backend/src/controllers/votingController.js
    backend/src/services/votingService.js
  </files>
  <action>
Write characterization-then-flip tests for VOT-01 in backend/tests/voting.test.js BEFORE implementing fixes (per RESEARCH.md Pattern 1). Tests must encode the CORRECT desired behavior:

1. "Chefe can change vote during AGUARDANDO_DESEMPATE" — currently fails because changeMyVote guard only allows EM_VOTACAO
2. "AGUARDANDO_DESEMPATE converges to terminal status after chefe tie-break vote" — currently fails because chefe cannot change vote, closeVoting re-tallies to EMPATE again

Test setup: Create request → chefe votes DEFERIR in normal phase → conselheiro1 votes DEFERIR, conselheiro2 votes INDEFERIR → tie triggers AGUARDANDO_DESEMPATE → chefe calls changeMyVote to switch to INDEFERIR → closeVoting returns INDEFERIDO.

Then implement minimal fixes to make tests pass (per D-01, D-02 in CONTEXT.md):
- votingController.changeMyVote (line 82): extend status guard from `r.status !== 'EM_VOTACAO'` to `r.status !== 'EM_VOTACAO' && r.status !== 'AGUARDANDO_DESEMPATE'` AND add eligibility check: only CHEFE_DEPARTAMENTO may change vote in AGUARDANDO_DESEMPATE
- votingService.canVote (lines 22-24): verify existing check `request.status === 'AGUARDANDO_DESEMPATE' && user.role !== 'CHEFE_DEPARTAMENTO'` is correct and covers the tie-break eligibility

No other files modified. This is the thinnest vertical slice touching all layers for VOT-01.
  </action>
  <verify>
    <automated>cd backend && npx vitest run voting.test.js -t "VOT-01"</automated>
    <fails_when>Any test in the "VOT-01: Tie-break convergence" describe block fails OR the test file has no tests matching "VOT-01"</fails_when>
  </verify>
  <done>
All VOT-01 tests pass: chefe can change vote in AGUARDANDO_DESEMPATE, request converges to APROVADO or INDEFERIDO, chefe cannot double-vote (normal + tie-break)
  </done>
</task>

<task type="auto">
  <name>Task 2: Grep audit — verify no other status === guards miss AGUARDANDO_DESEMPATE</name>
  <files>
    backend/src/controllers/votingController.js
    backend/src/services/votingService.js
    backend/src/jobs/votingCloser.js
  </files>
  <action>
Run grep across backend/src for all `status ===` and `status !==` guards related to voting states to ensure the tie-break fix is complete and no other guard inadvertently blocks the tie-break path. Specifically check:
- votingController.castVote (line 35-40) — should allow voting in EM_VOTACAO only (not tie-break)
- votingController.closeManual (line 95) — should work for AGUARDANDO_DESEMPATE
- votingController.suspend/unsuspend (lines 112, 135) — should not allow suspend in AGUARDANDO_DESEMPATE
- votingCloser.closeExpired — should handle AGUARDANDO_DESEMPATE (though Phase 7 owns this)
- votingService.closeVoting — should already handle AGUARDANDO_DESEMPATE via tally()

Document any additional guards found that need adjustment in a comment in votingController.js near changeMyVote. Do NOT modify other guards in this plan — only document. Future plans (Phase 7) will handle votingCloser.
  </action>
  <verify>
    <automated>cd backend && grep -rn "status ==" src/ | grep -E "(EM_VOTACAO|AGUARDANDO_DESEMPATE|SUSPENSO)" | wc -l</automated>
    <fails_when>Command returns 0 (no guards found) — indicates grep pattern mismatch</fails_when>
  </verify>
  <done>
Grep audit complete; any additional guards needing adjustment documented in code comments for future plans
  </done>
</task>

<task type="auto">
  <name>Task 3: Regression guard — ensure existing tally tests still pass</name>
  <files>
    backend/tests/voting.test.js
  </files>
  <action>
Run the full voting.test.js suite to ensure the VOT-01 changes don't break the existing 17 tests (tally, calcAmount, helpers). The characterization-then-flip approach means new tests are added, old tests must remain green. If any existing test fails, fix the implementation (not the test) unless the test encodes buggy behavior — in which case update the test to match correct behavior and note in commit.
  </action>
  <verify>
    <automated>cd backend && npx vitest run voting.test.js</automated>
    <fails_when>Any test in voting.test.js fails (including pre-existing tests)</fails_when>
  </verify>
  <done>
Full voting.test.js suite passes — 17 existing tests + new VOT-01 tests all green
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → API (vote endpoints) | Untrusted user input crosses here; role-based authorization enforced |
| API → Database (Prisma) | Trusted internal boundary; conditional updates prevent TOCTOU |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Elevation of Privilege | votingController.changeMyVote | high | mitigate | Explicit role check: only CHEFE_DEPARTAMENTO can change vote in AGUARDANDO_DESEMPATE; conselheiros get 403 |
| T-06-02 | Tampering | votingService.canVote | medium | mitigate | Centralized eligibility logic; status guard prevents voting in invalid states |
| T-06-03 | Repudiation | Audit trail for tie-break vote change | low | accept | Existing audit on vote create/update covers this; no new audit action needed |
| T-06-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages; package-legitimacy gate passed in RESEARCH.md |

</threat_model>

<verification>
Wave 0 verification: `cd backend && npx vitest run voting.test.js -t "VOT-01"` must pass. Full suite `cd backend && npx vitest run voting.test.js` must pass. Grep audit output reviewed for completeness.
</verification>

<success_criteria>
- VOT-01 characterization tests written and passing
- changeMyVote allows chefe vote change in AGUARDANDO_DESEMPATE only
- canVote enforces chefe-only eligibility in AGUARDANDO_DESEMPATE
- No request can remain stuck in AGUARDANDO_DESEMPATE
- All 17 pre-existing tests still pass
</success_criteria>

<output>
Create `.planning/phases/06-voting-money-state-machine/06-01-SUMMARY.md` when done
</output>