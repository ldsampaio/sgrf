---
phase: 08-reports-audit-auth-polish
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/src/routes/reports.routes.js
autonomous: true
requirements:
  - REP-01
user_setup: []

estimate:
  tokens: 18000
  raw_tokens: 9000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "Every report export (CSV/JSON/PDF via reports, financial, voting, accountability, dashboard) writes a report_exported AuditEvent before any bytes stream"
    - "Audit is fail-open (catch and warn, still streams) so audit DB outage does not DoS reports"
    - "Forced password reset audit uses distinct action forced_password_reset (not report_exported) if that route is touched"
  artifacts:
    - backend/src/routes/reports.routes.js (audit before any format branch in all 6 handlers, fail-open)
  key_links:
    - "scopeFilter → audit (report_exported, entityId <report>-<format>) → format branch (csv/pdf/json) → stream"
---

<objective>
Make every report export auditable: move/add AuditEvent report_exported to top of each handler in reports.routes.js, before any if (format==='csv') / if (format==='pdf') / res.json branch, with fail-open semantics (catch and logger.warn but still stream). Today only two PDF branches audit and they do it after pipe — violates "before any bytes stream".

Purpose: Every CSV/JSON export is audited per REP-01, like PDF already (but PDF also moved before stream), and audit failure never blocks the export.
Output: Reports routes with audit-before-stream in all handlers, fail-open.
</objective>

<execution_context>
@/home/lucas/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/lucas/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-reports-audit-auth-polish/08-CONTEXT.md
@.planning/phases/08-reports-audit-auth-polish/08-RESEARCH.md
@backend/src/routes/reports.routes.js
@backend/src/services/auditService.js
</context>

<tasks>

<task type="tracer">
  <name>Task 1: REP-01 Audit-before-stream in all report handlers (fail-open)</name>
  <files>
    backend/src/routes/reports.routes.js
  </files>
  <action>
At the top of each report handler in backend/src/routes/reports.routes.js, BEFORE any format branching or DB query that precedes streaming, add:

```javascript
await audit({ actorId: req.user.id, action: 'report_exported', entityType: 'report', entityId: `${reportName}-${req.query.format || 'json'}`, req }).catch(e => logger.warn({err:e.message}, 'report audit failed'));
```

Do this for all handlers:
- GET /requests (already has CSV no-audit + PDF audit after pipe — add before, keep PDF's existing but move before pipe)
- GET /financial (CSV no-audit, JSON no-audit — add before)
- GET /voting (currently JSON no audit — add before)
- GET /accountability (PDF audit after pipe — move before)
- GET /integration/provisioned (stub — add before)
- GET /dashboard (currently JSON no audit — add before)
- POST /dashboard-pdf (already audits after end — move before pipe, keep warn)

Use consistent entityId pattern: `${routeBase}-${format}` e.g. requests-csv, requests-pdf, requests-json, financial-csv, financial-json, voting-json, accountability-pdf, dashboard-json, dashboard-pdf.

Ensure:
- Audit is BEFORE res.header / doc.pipe / res.send / res.json
- Fail-open: .catch(logger.warn) so DB failure doesn't block stream (auditService itself is fail-open but add explicit catch for parity)
- Keep existing requirePermission + scopeWhere checks before audit or after — but audit must be after auth (authJwt + requirePermission already guarantee actorId) and before streaming
- Do NOT add audit for forced password reset here — that is user routes, not reports; 08-01 focuses on reports only per REP-01 (distinct forced_password_reset action belongs to user admin flow, out of scope unless touching that file)

Verify with grep that no handler has format branch before audit.
  </action>
  <verify>
    <automated>cd backend && grep -n "report_exported" src/routes/reports.routes.js | wc -l</automated>
    <fails_when>Count < 6 (must have audit in at least 6 report handlers)</fails_when>
  </verify>
  <verify>
    <automated>cd backend && npx vitest run -t "REP-01"</automated>
    <fails_when>Any test in "REP-01: Audit before stream" describe block fails OR file has no tests matching "REP-01"</fails_when>
  </verify>
  <done>
Every report export handler calls audit report_exported before any format branch, fail-open, with entityId <report>-<format>
  </done>
</task>

<task type="auto">
  <name>Task 2: Regression guard — existing report + voting tests still pass</name>
  <files>
    backend/tests/voting.test.js
  </files>
  <action>
Run full backend suite to ensure audit-before-stream (fail-open) doesn't break report scoping, voting tally, or auth. Audit failure must not throw.

If any existing test fails due to added audit (e.g., mock missing prisma.auditEvent), fix by making audit mock no-op in test setup (auditService already swallows errors, but test DB must have AuditEvent table).
  </action>
  <verify>
    <automated>cd backend && npx vitest run</automated>
    <fails_when>Any test in backend test suite fails</fails_when>
  </verify>
  <done>
Full backend suite passes — audit-before-stream is fail-open and does not break existing flows
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → API (reports) | Authenticated via authJwt + requirePermission; actorId trusted |
| API → DB (AuditEvent) | Trusted internal; fail-open prevents DoS on audit outage |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Repudiation | reports.routes.js audit missing for CSV/JSON | high | mitigate | Audit before any branch, entityId includes format, fail-open catch |
| T-08-02 | Denial of Service | Audit DB outage blocks reports | medium | mitigate | Fail-open: .catch(logger.warn) then still stream |
| T-08-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages in this plan |

</threat_model>

<verification>
Wave 0 verification: `cd backend && grep -n "report_exported" src/routes/reports.routes.js | wc -l` must be >=6. `cd backend && npx vitest run -t "REP-01"` must pass. Full suite `cd backend && npx vitest run` must pass.
</verification>

<success_criteria>
- Every report export handler audits report_exported before any format branch
- CSV/JSON now audited like PDF (previously only PDF)
- Audit is fail-open (audit failure does not block stream)
- Grep count >=6, REP-01 tests pass, full suite green
</success_criteria>

<output>
Create `.planning/phases/08-reports-audit-auth-polish/08-01-SUMMARY.md` when done
</output>
