---
phase: 07-background-jobs
plan: 02
type: execute
wave: 1
depends_on:
  - 07-01
files_modified:
  - backend/src/jobs/scheduler.js
  - backend/src/jobs/votingCloser.js
autonomous: true
requirements:
  - JOB-02
user_setup: []

estimate:
  tokens: 18000
  raw_tokens: 9000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "Expired EM_VOTACAO requests (votingDeadlineAt < now) conclude automatically via closeVoting without manual action"
    - "closeExpired is idempotent per run and handles {needsArbitration} without wedging (logs and continues)"
    - "Scheduler runs closeExpired every 5 minutes with re-entrancy guard (only one concurrent run)"
  artifacts:
    - backend/src/jobs/scheduler.js (voting interval wired to closeExpired with guard)
    - backend/src/jobs/votingCloser.js (verified idempotent, take 50 bound, arbitration handling)
  key_links:
    - "scheduler tickVoting → closeExpired → closeVoting (existing) → tally/provision/notify → arbitration handled"
---

<objective>
Wire the voting auto-close job: make scheduler call closeExpired() every 5 minutes with re-entrancy guard, verify idempotency and arbitration handling. closeExpired must query EM_VOTACAO where votingDeadlineAt < now, take 50, and for each id call existing closeVoting(id, 'system-cron') — never re-implement tally or provision. Tied requests (needsArbitration) must log and continue, not wedge.

Purpose: Expired votings conclude automatically (tally → provision → notify) with no manual action, and Phase 6 tie-breaks cannot wedge the job.
Output: Scheduler voting interval active + closeExpired proven idempotent and arbitration-safe.
</objective>

<execution_context>
@/home/lucas/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/lucas/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-background-jobs/07-CONTEXT.md
@.planning/phases/07-background-jobs/07-RESEARCH.md
@backend/src/jobs/scheduler.js
@backend/src/jobs/votingCloser.js
@backend/src/services/votingService.js
</context>

<tasks>

<task type="tdd">
  <name>Task 1: JOB-02 Characterization — expired votings auto-close via closeExpired</name>
  <files>
    backend/tests/voting.test.js
  </files>
  <behavior>
    - Test 1: Seed one EM_VOTACAO request with votingDeadlineAt = now - 1h, two votes (DEFERIR, DEFERIR) → call closeExpired() → status becomes APROVADO, provisioned
    - Test 2: Seed one EM_VOTACAO with tie (DEFERIR vs INDEFERIR + chefe already voted edge from Phase 6) → closeExpired → needsArbitration logged, request not wedged, job continues to next expired row
    - Test 3: Call closeExpired twice on same expired set → second run is idempotent (no double provision, no error)
    - Test 4: Verify take 50 bound — closeExpired selects at most 50 per run (existing behavior)
  </behavior>
  <action>
Write characterization-then-flip tests for JOB-02 in backend/tests/voting.test.js BEFORE modifying scheduler (or extend with new describe('JOB-02: Voting auto-close')).

Tests encode CORRECT behavior per CONTEXT.md Fixed Decisions:

1. "closeExpired closes expired EM_VOTACAO" — create request EM_VOTACAO with deadline in past, add votes that yield DEFERIDO, call closeExpired() (import directly from jobs/votingCloser.js), verify status transitions to APROVADO and FundBalance provisioned.
2. "closeExpired handles needsArbitration without wedging" — create tied scenario where validVotes triggers Phase 6 arbitration path (any DEFERIR_PARCIALMENTE), call closeExpired, verify it logs "arbitration needed" and continues (does not throw), request ends in APROVADO_PARCIALMENTE + AGUARDANDO_ARBITRAGEM.
3. "closeExpired is idempotent" — call closeExpired twice; second call should find zero expired (since first promoted them from EM_VOTACAO) and return 0 or not re-provision.
4. "Take 50 bound" — if exists, verify closeExpired uses take:50 (or just assert expired.length <=50).

These tests use direct import of closeExpired (not scheduler) to isolate job logic from timer — scheduler wiring is verified in Task 2.

These tests WILL PASS if Phase 6 closeVoting is correct, but Task 1 still writes them first to lock the contract before scheduler wiring.
  </action>
  <verify>
    <automated>cd backend && npx vitest run voting.test.js -t "JOB-02"</automated>
    <fails_when>Any test in the "JOB-02: Voting auto-close" describe block fails OR the test file has no tests matching "JOB-02"</fails_when>
  </verify>
  <done>
JOB-02 tests written and passing — expired votings auto-close, arbitration handled, idempotent
  </done>
</task>

<task type="auto">
  <name>Task 2: Scheduler voting interval (5 min) with guard</name>
  <files>
    backend/src/jobs/scheduler.js
  </files>
  <action>
Ensure backend/src/jobs/scheduler.js tickVoting is wired per Pattern 2 and CONTEXT.md JOB-01-02:

Already created in 07-01, but now verify and harden:

- tickVoting must be: async function tickVoting() { if (votingRunning) return; votingRunning = true; try { await closeExpired(); } catch(e){ logger.error(e); } finally { votingRunning = false; } }
- startScheduler must create votingTimer = setInterval(tickVoting, 5*60*1000) (300s, not 60s)
- stopScheduler must clearInterval(votingTimer) and null it
- Ensure closeExpired import is correct (../config/db fix already done in 07-01)
- Do NOT re-implement closeVoting — tickVoting must call closeExpired() which itself calls closeVoting; no duplicated tally logic

After wiring, run full suite to ensure no timer leak and no regression.
  </action>
  <verify>
    <automated>cd backend && npx vitest run</automated>
    <fails_when>Any test in backend test suite fails</fails_when>
  </verify>
  <verify>
    <automated>cd backend && grep -rn "closeExpired|tickVoting" src/jobs/scheduler.js | wc -l</automated>
    <fails_when>Count == 0 (scheduler must reference closeExpired/tickVoting)</fails_when>
  </verify>
  <done>
Scheduler voting interval active every 5 min with guard; full suite passes; closeExpired handles arbitration and idempotency
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB → Job (ResourceRequest poll) | Trusted internal; status filter + take 50 bounds work |
| Job → closeVoting (tally/provision) | Trusted internal; closeVoting enforces state machine + ledger invariants |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-05 | Tampering | closeExpired take bound | low | mitigate | take:50 prevents unbounded work per tick; DB-as-arbiter is row status |
| T-07-06 | Denial of Service | Re-entrancy double-close | medium | mitigate | votingRunning boolean guard prevents overlapping ticks; closeVoting idempotent |
| T-07-07 | Tampering | Arbitration wedge | high | mitigate | closeExpired checks result?.needsArbitration and continues; Phase 6 closeVoting returns this shape |
| T-07-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages in this plan |

</threat_model>

<verification>
Wave 1 verification: `cd backend && npx vitest run voting.test.js -t "JOB-02"` must pass. Full suite `cd backend && npx vitest run` must pass. Grep for tickVoting in scheduler.js must be >=1.
</verification>

<success_criteria>
- Expired EM_VOTACAO (deadline < now) auto-closes via closeExpired → APROVADO/INDEFERIDO/ARBITRATION as per tally
- needsArbitration does not wedge job — logs and continues to next expired row
- closeExpired is idempotent per run (second call on same set is no-op)
- Scheduler runs tickVoting every 5 min with votingRunning guard, cleared on SIGTERM via stopScheduler
- No timer leak into tests; full suite green
</success_criteria>

<output>
Create `.planning/phases/07-background-jobs/07-02-SUMMARY.md` when done
</output>
