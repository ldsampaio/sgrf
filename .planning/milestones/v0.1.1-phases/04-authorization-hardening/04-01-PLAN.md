---
phase: 04-authorization-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/middlewares/permissions.js
  - backend/src/middlewares/visibility.js
  - backend/src/routes/requests.routes.js
  - backend/src/routes/messages.routes.js
  - backend/src/routes/settings.routes.js
  - backend/src/routes/reports.routes.js
  - backend/src/routes/users.routes.js
  - backend/src/controllers/requestController.js
  - backend/tests/authz/helpers.js
  - backend/tests/authz/route-coverage.test.js
  - backend/tests/authz/matrix.test.js
  - docs/06-permissoes.md
autonomous: true
requirements: [SEC-01]

estimate:
  tokens: 55000
  raw_tokens: 28000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "GET /api/requests/:id enforces role gate then row visibility: wrong-role caller gets 403, out-of-scope resource reads as 404"
    - "Every route in requests/messages/settings/reports route files declares a permission action; an undeclared action fails closed with 403"
    - "docs/06 ALUNO row for request visualization reads Sim (D-03 override applied) and the reports-scope decision is recorded"
    - "cd backend && npx vitest run tests/authz passes (Wave 0 scaffolding green)"
  artifacts:
    - backend/src/middlewares/permissions.js
    - backend/src/middlewares/visibility.js
    - backend/tests/authz/helpers.js
    - backend/tests/authz/route-coverage.test.js
    - backend/tests/authz/matrix.test.js
  key_links:
    - "requirePermission mounted after authJwt on every touched route (req.user present before role check)"
    - "canViewRequest shared by requestController.getOne and reused later by reports.scopeFilter and listVotes"
    - "fn._permissionAction tag present on every requirePermission frame for the coverage test"
---

<objective>
Build the deny-by-default authorization foundation and prove it end-to-end on one
route slice before expansion: a frozen PERMISSIONS map + requirePermission(action)
(per D-01..D-11, transcribed from docs/06), a shared visibility helper
(canViewRequest/scopeWhere encoding D-03/D-04), route wiring with fail-closed
undeclared actions, Wave 0 test scaffolding, and the docs/06 updates
(ALUNO override per D-03 plus the recorded reports-scope decision).

Purpose: This is the tracer slice — it proves the middleware chain, the
visibility rule, and the test harness work together before Phase 4 expansion
touches every controller.
Output: permissions.js, visibility.js, wired routes, green tests/authz skeleton,
updated docs/06.
</objective>

<execution_context>
@/home/lucas/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/lucas/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-authorization-hardening/04-CONTEXT.md
@.planning/phases/04-authorization-hardening/04-RESEARCH.md
@.planning/phases/04-authorization-hardening/04-PATTERNS.md
@backend/src/middlewares/auth.js
@backend/src/routes/users.routes.js
@backend/src/controllers/requestController.js
@docs/06-permissoes.md
</context>

<tasks>

<task type="tracer">
  <name>Tracer: permission map + visibility helper + one wired route + Wave 0 scaffolding green</name>
  <files>backend/src/middlewares/permissions.js, backend/src/middlewares/visibility.js, backend/src/routes/requests.routes.js, backend/src/controllers/requestController.js, backend/tests/authz/helpers.js, backend/tests/authz/route-coverage.test.js, backend/tests/authz/matrix.test.js</files>
  <action>Create backend/src/middlewares/permissions.js with a frozen PERMISSIONS map transcribed from the docs/06 action table per the research guard table (requests:list/get, votes:list, messages:remove, settings:transactions:view, reports:voting open to all 5 roles with controller row-guards; votes:cast restricted to CHEFE_DEPARTAMENTO plus CONSELHEIRO with eligibility staying in the canVote service; settings:financial:edit and users:force-reset restricted to ADMINISTRADOR plus CHEFE_DEPARTAMENTO), mirroring the requireRole closure shape in middlewares/auth.js lines 18-24, returning fail-closed 403 for undeclared actions and 401 when req.user is absent, tagging each returned middleware frame for coverage detection, per D-08. Create backend/src/middlewares/visibility.js with canViewRequest (RASCUNHO visible only to owner plus ADMINISTRADOR/CHEFE_DEPARTAMENTO per D-04; every non-draft visible to any authenticated role per D-03) and scopeWhere (PROFESSOR/ALUNO branch as own-requests OR non-draft, generalizing the reports scopeFilter shape), using String() coercion on UUID comparison, per D-05 no amount redaction anywhere. Wire GET /api/requests/:id (and its /:id/history alias reusing the same action, no audit-read per out-of-scope rule) with requirePermission after the existing router.use(authJwt), and add the canViewRequest 404 check in requestController.getOne before responding, keeping the files/transactions include. Create Wave 0 scaffolding: backend/tests/authz/helpers.js mocking only the src/config/db.js singleton with hand-rolled per-model functions plus a transaction callback passthrough, signing real access tokens per role via src/utils/tokens.js with test secrets (never stub authJwt), with 5-role user fixtures and draft/non-draft request fixtures; backend/tests/authz/route-coverage.test.js walking the Express router stack asserting each route chain carries the permission tag; backend/tests/authz/matrix.test.js skeleton with at least the getOne allow/deny cases (owner sees own draft, cross-user draft reads 404, PROFESSOR/ALUNO see non-draft, wrong-role on a visible route reads 403) using only Node 22 compatible APIs. Check order throughout is authenticate, then map gate, then fetch, then visibility 404, then ownership 403.</action>
  <verify>
    <automated>cd backend && npx vitest run tests/authz</automated>
    <fails_when>non-zero exit, or the summary shows zero passed tests, or route-coverage reports an untagged route</fails_when>
  </verify>
  <done>Single happy path works end to end and is committed: an authenticated PROFESSOR reads a non-draft request (200 with amounts, D-05), cross-user draft reads 404, undeclared action reads 403, and the authz suite is green.</done>
</task>

<task type="auto">
  <name>Wire requirePermission across remaining routes per guard table</name>
  <files>backend/src/routes/requests.routes.js, backend/src/routes/messages.routes.js, backend/src/routes/settings.routes.js, backend/src/routes/reports.routes.js, backend/src/routes/users.routes.js</files>
  <action>Wire requirePermission with the matching map action on every route in the requests, messages, settings, and reports route files, keeping router.use(authJwt) as the first layer, mirroring the users.routes.js per-route gate precedent; declare broad requirePermission actions on PATCH /api/users/:id and PATCH /api/users/:id/role with the existing canManageUsers handler checks retained as the target-dependent gate; map voting and finance mutation endpoints declared in the requests/finance route files to map actions mirroring their current inline roles with zero behavior change (eligibility logic stays in services); keep the force-password-reset route gate as-is per D-11; never alter the leaders-only mutation arrays in settingsController patchFinancial/patchBalance (D-10 verify-and-lock — view task must not touch mutation gates).</action>
  <verify>
    <automated>cd backend && npx vitest run tests/authz/route-coverage.test.js</automated>
    <fails_when>non-zero exit, or any route reported without a declared permission action</fails_when>
  </verify>
  <done>Every route in the five touched route files declares a permission action; coverage test green; no mutation role array modified.</done>
</task>

<task type="auto">
  <name>Update docs/06 matrix rows (D-03 override + recorded reports decision)</name>
  <files>docs/06-permissoes.md</files>
  <action>Doc-only task, no code changes. In docs/06-permissoes.md change the ALUNO cell of the Visualizar pedidos em votacao row from Nao to Sim per D-03, noting the override next to the row; apply the recorded reports-scope decision (recommendation: unify widen — PROFESSOR/ALUNO report scope follows the same non-draft visibility as list, updating the Gerar relatorios row accordingly) and record the chosen option with rationale in a note under the table; record both edits in the plan SUMMARY so downstream plans cite the final rows.</action>
  <verify>
    <automated>grep -v '^#' docs/06-permissoes.md | grep -c 'Sim | Sim | Sim | Sim | Sim' && grep -c 'D-03' docs/06-permissoes.md</automated>
    <fails_when>non-zero exit, or either count prints 0 (override row or decision note missing)</fails_when>
  </verify>
  <done>docs/06 shows Sim for ALUNO on the voting-visualization row with the D-03 override noted, and the Gerar relatorios row matches the recorded widen decision with rationale.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API (cookie) | Untrusted caller presents access_token cookie; authJwt establishes identity before any permission check |
| route→controller | requirePermission is the role gate; controllers add row-level checks with untrusted IDs from URL params |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Elevation of privilege | middlewares/permissions.js (all wired routes) | high | mitigate | Deny-by-default PERMISSIONS map: undeclared action returns fail-closed 403; route-coverage test walks the router stack and fails the suite on any untagged route |
| T-04-02 | Information disclosure | requestController.getOne + visibility.js | high | mitigate | canViewRequest returns 404 (not 403) for out-of-scope resources per D-08, hiding existence; amounts follow view scope with no separate redaction path per D-05 |
| T-04-03 | Elevation of privilege | PATCH /users/:id role escalation | high | mitigate | Broad requirePermission declaration plus retained canManageUsers target check in handler (chefe cannot touch admins) |
| T-04-SC | Tampering | npm installs | high | mitigate | No new packages installed by this plan (supertest/vitest pre-existing); package-legitimacy gate passes with nothing to audit |
</threat_model>

<verification>
Tracer slice proven: authenticated reads enforce role-then-visibility ordering,
undeclared actions fail closed, docs/06 matches implemented behavior, and the
authz suite runs green under the existing CI invocation with no workflow change.
</verification>

<success_criteria>
- requirePermission + canViewRequest enforced end to end on GET /api/requests/:id
- All five route files declare map actions; coverage test green
- docs/06 ALUNO override + recorded reports decision present
- `cd backend && npx vitest run tests/authz` green
</success_criteria>

<output>
Create `.planning/phases/04-authorization-hardening/04-01-SUMMARY.md` when done
</output>