---
phase: 04-authorization-hardening
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - backend/src/controllers/requestController.js
  - backend/src/controllers/deliberationController.js
  - backend/src/controllers/votingController.js
  - backend/src/controllers/userController.js
  - backend/src/controllers/settingsController.js
  - backend/src/routes/reports.routes.js
  - backend/src/routes/messages.routes.js
autonomous: true
requirements: [SEC-01]

estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "Cancelling a request enforces the RN-010 status-by-role matrix with mandatory justification and audit, and approved-path cancellations carry the Phase 6 REVERSE seam marker"
    - "Message removal allows only author plus ADMINISTRADOR/CHEFE_DEPARTAMENTO with suspension precedence kept; listVotes shows full vote detail to anyone in view scope"
    - "Cross-user drafts are invisible (404) while PROFESSOR/ALUNO see all non-drafts; chefe cannot force-reset an admin; settings transactions stay view-open with mutations leaders-only"
  artifacts:
    - backend/src/controllers/requestController.js
    - backend/src/controllers/deliberationController.js
    - backend/src/controllers/votingController.js
    - backend/src/controllers/userController.js
  key_links:
    - "Check order authJwt, requirePermission 403, fetch, canViewRequest 404, ownership 403 (suspension 423 keeps precedence)"
    - "reports.scopeFilter replaced by (or delegates to) shared scopeWhere — no divergent draft rule"
    - "Phase 6 seam comment marks the approved-cancel branch; no FinancialTransaction writes beyond audit"
---

<objective>
Apply row-level ownership and scoping fixes in controllers on top of the 04-01
foundation: RN-010 cancel matrix, message remove, shared visibility reuse in
getOne/list/votes/reports, listVotes view-scope, settings verify-and-lock, and
the force-password-reset target guard.

Purpose: Turns the route-level role gates into true row-level authorization so
every SEC-01 endpoint denies correctly per role AND per row.
Output: Hardened controllers with audited guards, ready for the 04-03 matrix.
</objective>

<execution_context>
@/home/lucas/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/lucas/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-authorization-hardening/04-CONTEXT.md
@.planning/phases/04-authorization-hardening/04-RESEARCH.md
@.planning/phases/04-authorization-hardening/04-PATTERNS.md
@.planning/phases/04-authorization-hardening/04-01-SUMMARY.md
@backend/src/controllers/requestController.js
@backend/src/controllers/deliberationController.js
@backend/src/controllers/votingController.js
@docs/14-decisoes-em-aberto.md
</context>

<tasks>

<task type="auto">
  <name>Cancel RN-010 matrix + getOne/list visibility in requestController</name>
  <files>backend/src/controllers/requestController.js</files>
  <action>Replace the zero-check cancel stub with the RN-010 matrix transcribed verbatim from docs/14-decisoes-em-aberto.md line 16 (owner only when status is RASCUNHO or EM_VOTACAO; ADMINISTRADOR any non-terminal including INDEFERIDO as cleanup; CHEFE_DEPARTAMENTO only before CONCLUIDO; mandatory justification returning 400 when absent; CONCLUIDO and CANCELADO immutable returning 400), per D-06 with audited justification on the request_cancelled audit event. Order inside cancel: fetch, then canViewRequest 404 hiding existence per D-08, then immutability 400, then justification 400, then the approved-status branch (ADMINISTRADOR/CHEFE_DEPARTAMENTO only, delegating the reversal to a marked Phase 6 (VOT-04/06-04) compensating REVERSE seam comment — no FinancialTransaction writes in this phase) versus the ordinary branch (owner or leaders else 403). Replace the ALUNO owner-only list branch with scopeWhere per D-03 and add the canViewRequest 404 check in getOne before responding, keeping the files/transactions include per D-05. Keep try/catch next(e) delegation and existing audit conventions.</action>
  <verify>
    <automated>cd backend && npx vitest run tests/authz</automated>
    <fails_when>non-zero exit, or the run reports a failing cancel or getOne/list case</fails_when>
  </verify>
  <done>Owner cancels own RASCUNHO/EM_VOTACAO with justification (200 + audited), cross-user cancel denied, justification-less cancel rejected 400, immutable statuses rejected 400, approved path carries the Phase 6 seam marker with identical audit on both paths.</done>
</task>

<task type="auto">
  <name>Message remove guard + listVotes view-scope + reports scopeFilter reuse</name>
  <files>backend/src/controllers/deliberationController.js, backend/src/controllers/votingController.js, backend/src/routes/reports.routes.js, backend/src/routes/messages.routes.js</files>
  <action>In deliberationController.remove insert the author-or-leader check (author plus ADMINISTRADOR/CHEFE_DEPARTAMENTO per D-07, no time window) between the exists check and the suspension guard so suspension 423 keeps precedence, keeping the message_deleted audit; add a parent canViewRequest check to deliberation list (404 when the parent request is invisible) and move the post ALUNO exclusion to the map with no handler change beyond removing the inline branch. In votingController.listVotes add parent request lookup plus canViewRequest (404 when invisible) and return full enriched vote detail when visible per D-01/D-02 with no tally-only redaction layer, reusing the existing enrichVotes join shape; leave eligibility logic in the voting service untouched. Replace reports scopeFilter with (or delegate it to) the shared scopeWhere from 04-01 so PROFESSOR/ALUNO report scope matches list visibility per the recorded 04-01 decision, and filter the voting report to requests passing canViewRequest (drafts excluded unless owner/leaders); treat the /:id/history alias as requests:get with no new audit-read surface per D-09.</action>
  <verify>
    <automated>cd backend && npx vitest run tests/authz</automated>
    <fails_when>non-zero exit, or the run reports a failing remove, listVotes, or reports-scope case</fails_when>
  </verify>
  <done>Non-author non-leader removal denied 403, suspended removal still 423, listVotes mirrors getOne visibility with full detail, reports match list scope with drafts excluded for outsiders.</done>
</task>

<task type="auto">
  <name>Settings verify-and-lock + force-password-reset target guard</name>
  <files>backend/src/controllers/settingsController.js, backend/src/routes/settings.routes.js, backend/src/controllers/userController.js</files>
  <action>Settings is verify-and-lock per D-10: confirm the transactions view has no role gate on route or controller and declare the settings:transactions:view action (all roles) at route level without touching any controller logic; declare settings:financial:edit on the two PATCH mutation routes while keeping both inline leaders-only arrays exactly as-is (never modify patchFinancial/patchBalance role arrays in this task — any diff there fails review). In userController.resendInvite add the target lookup with 404 plus the existing canManageUsers actor/target check returning 403 (chefe blocked from targeting admins per D-11, matching the matrix Gerenciar usuarios Parcial cell), keeping the temp-password enqueue and hash flow unchanged, and emit a distinct password_reset_forced audit action on the force-password-reset path versus invite_resent (route wrapper or path check at the executor's discretion).</action>
  <verify>
    <automated>cd backend && npx vitest run tests/authz</automated>
    <fails_when>non-zero exit, or the run reports a failing settings or force-reset case</fails_when>
  </verify>
  <done>Transactions view open to all 5 roles and locked by test, PATCH mutations still leaders-only (git diff shows no changed mutation role arrays in patchFinancial/patchBalance), chefe-to-admin force-reset denied 403 with distinct audit action.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route→controller | Map gate already passed; row identity (owner/author/target) comes from Postgres rows, never client claims |
| controller→audit | Justification text is untrusted input stored as opaque audit metadata, never executed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-02 | Information disclosure | getOne/list/listVotes/reports (IDOR) | high | mitigate | canViewRequest/scopeWhere shared helper returns 404 outside scope per D-08; matrix tests assert 404 (not 403) for cross-user drafts |
| T-04-03 | Elevation of privilege | cancel / message remove (missing function-level auth) | high | mitigate | RN-010 status-by-role routing + author-or-leader check in handlers; 403 for wrong role on visible scope |
| T-04-04 | Elevation of privilege | force-password-reset targeting admin (forced browsing) | high | mitigate | canManageUsers actor/target check in resendInvite per D-11, not just route roles; distinct audit action |
| T-04-05 | Tampering | cancel justification / audit metadata | low | accept | Justification stored as opaque string via fail-open audit; no CSV/export path touched in this phase, so no injection sink exists here |
</threat_model>

<verification>
Row-level guards deny correctly per role and per row with the D-08 403/404
split, suspension 423 precedence preserved, and the approved-cancel branch
marked for Phase 6 with no money writes beyond audit.
</verification>

<success_criteria>
- RN-010 cancel matrix enforced with justification 400 + audit + Phase 6 seam
- remove/listVotes/reports follow view scope; deliberation post rule in map
- Settings view open locked, mutations untouched; force-reset target-guarded
- `cd backend && npx vitest run tests/authz` green
</success_criteria>

<output>
Create `.planning/phases/04-authorization-hardening/04-02-SUMMARY.md` when done
</output>
