---
phase: 04-authorization-hardening
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified:
  - backend/tests/authz/helpers.js
  - backend/tests/authz/matrix.test.js
  - backend/tests/authz/route-coverage.test.js
autonomous: true
requirements: [SEC-01]

estimate:
  tokens: 45000
  raw_tokens: 22000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "A supertest allow+deny matrix covering the fixed routes times all 5 roles passes, proving both ALLOW paths intact and deny paths correct"
    - "The router-stack coverage test proves undeclared actions fail closed so a future auth-only route fails the suite"
    - "The full backend suite (all 17 pre-existing tests plus the authz matrix) is green with no CI workflow change"
  artifacts:
    - backend/tests/authz/matrix.test.js
    - backend/tests/authz/route-coverage.test.js
  key_links:
    - "Matrix mocks only the db.js singleton; auth chain (cookie to user to permission) stays real via signed tokens"
    - "Full suite runs under the existing CI invocation with zero workflow/service changes"
---

<objective>
Complete the authorization regression gate: the full supertest allow+deny matrix
(fixed routes x all 5 roles, mock-Prisma story with zero CI change) plus the
router-stack coverage test proving undeclared fails closed, ending with the full
suite green.

Purpose: Success criteria 3 and 4 of the phase — legitimate flows keep working
and no future route can silently default to auth-only.
Output: Completed matrix + coverage tests, full `npx vitest run` green.
</objective>

<execution_context>
@/home/lucas/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/lucas/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-authorization-hardening/04-CONTEXT.md
@.planning/phases/04-authorization-hardening/04-RESEARCH.md
@.planning/phases/04-authorization-hardening/04-PATTERNS.md
@.planning/phases/04-authorization-hardening/04-01-SUMMARY.md
@.planning/phases/04-authorization-hardening/04-02-SUMMARY.md
@backend/tests/authz/helpers.js
@backend/tests/authz/matrix.test.js
@backend/tests/authz/route-coverage.test.js
</context>

<tasks>

<task type="auto">
  <name>Complete allow+deny matrix for fixed routes x 5 roles</name>
  <files>backend/tests/authz/helpers.js, backend/tests/authz/matrix.test.js</files>
  <action>Extend the 04-01 matrix skeleton to cover every fixed endpoint times all 5 roles using the mock-Prisma story decided at plan time (mock only the src/config/db.js singleton with per-test findUnique/findMany/update programming; real JWTs signed via src/utils/tokens.js with test secrets driving request.agent against the real app factory — never stub authJwt, no CI service or workflow change, Node 22 compatible APIs only). Deny cases assert the D-08 split (403 wrong role on visible route/scope, 404 resource outside caller scope hiding existence), covering cancel (owner RASCUNHO/EM_VOTACAO allow, cross-user deny, justification 400, immutable 400), message remove (non-author non-leader 403), getOne/list draft invisibility vs non-draft PROFESSOR/ALUNO visibility, listVotes view-scope with full detail, transactions view open to all 5 roles with PATCH mutations leaders-only, force-reset chefe-to-admin 403, and reports voting scoping. Allow cases prove no legitimate flow broke (conselheiro opens a request under vote, owner sees own request, admin force-reset works). Extend helpers.js fixtures only as needed for the new cases.</action>
  <verify>
    <automated>cd backend && npx vitest run tests/authz/matrix.test.js</automated>
    <fails_when>non-zero exit, or the summary shows any failing or skipped matrix case, or any allow-path case fails</fails_when>
  </verify>
  <done>Matrix covers all fixed routes x 5 roles with allow paths intact and deny paths asserting the correct 403/404 split; suite green.</done>
</task>

<task type="auto">
  <name>Finalize router coverage test + full suite green</name>
  <files>backend/tests/authz/route-coverage.test.js</files>
  <action>Harden the 04-01 route-coverage skeleton into the executable form of success criterion 4: walk the app router stack, collect every declared route across all mounted routers, and assert each handler chain contains a frame carrying the permission tag from 04-01, including a negative control proving an untagged (auth-only) route fails the assertion; keep the suite runnable under bare npx vitest run with no config file and no new dependencies. Finish with the full backend suite to confirm no regressions in the 17 pre-existing tests.</action>
  <verify>
    <automated>cd backend && npx vitest run</automated>
    <fails_when>non-zero exit, or the summary shows any failed suite, or the pre-existing test count drops below 17 passing</fails_when>
  </verify>
  <done>Coverage test proves every route declares a permission and the negative control fails closed; full `npx vitest run` green including all pre-existing tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test→app | Matrix drives the real middleware chain over HTTP; only the Prisma singleton is mocked, so permission decisions are tested, not query correctness |
| suite→CI | Tests must pass under the existing CI backend job (dummy DATABASE_URL, no Postgres service) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Elevation of privilege | Undeclared/future routes defaulting to auth-only | high | mitigate | Router-stack coverage test with negative control fails the suite on any untagged route — regression gate for success criterion 4 |
| T-04-02 | Information disclosure | IDOR regressions on scoped reads | high | mitigate | Matrix deny cases pin the 404-outside-scope behavior per D-08 across getOne/list/votes/reports |
| T-04-06 | Denial of service (legitimate use) | Over-denying gates breaking ALLOW flows | medium | mitigate | Explicit allow cases (conselheiro under-vote read, owner self-read, admin force-reset) fail the suite if the deny-by-default pass overreaches |
</threat_model>

<verification>
The phase regression gate stands alone: fixed routes x 5 roles pinned at HTTP
level, undeclared actions provably fail closed, ALLOW flows intact, and the
whole backend suite green without touching the CI workflow.
</verification>

<success_criteria>
- Allow+deny matrix complete and green for all fixed routes x 5 roles
- Router coverage test green with working negative control
- Full `cd backend && npx vitest run` green, no CI change
</success_criteria>

<output>
Create `.planning/phases/04-authorization-hardening/04-03-SUMMARY.md` when done
</output>
