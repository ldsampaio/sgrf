---
phase: 05-session-refresh
plan: 04
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - frontend/src/services/api.js
  - frontend/src/stores/auth.js
  - frontend/src/views/Requests.vue
autonomous: true
requirements:
  - SES-01
user_setup: []

estimate:
  tokens: 25000
  raw_tokens: 16000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - Failed login shows the credential error alert in place — no spurious POST /auth/refresh, no self-bounce wiping the form
    - Bounce from the login page itself never nests a stale /login redirect (post-login lands on / or the true origin)
    - Draft restore never injects an arbitrary type string into the enum select (unknown values fall back to the current default)
    - 401-strict gate and 403 passthrough intact; build green
  artifacts:
    - frontend/src/services/api.js
    - frontend/src/views/Requests.vue
  key_links:
    - api.js interceptor bypass → auth endpoints that legitimately 401 outside a session reject straight to the caller
    - api.js doBounce() same-page guard → safeOrigin falls back to / on /login
    - Requests.vue restoreDraft() → type allowlist-checked against the four valid options
---

<objective>
Close the three VERIFICATION.md gaps against 05-01 (WR-01 blocking, WR-02/WR-03 warnings) using the fix snippets in 05-REVIEW.md: widen the api.js interceptor bypass so login 401s reject straight to Login.vue, add the doBounce() same-page guard, and allowlist-check the restored draft type in Requests.vue. No backend changes, no new files, no new dependencies.

Purpose: The 05-02 human protocol (the phase DoD) cannot run truthfully until these paths are fixed — WR-01/WR-02 pollute exactly the bounce paths the protocol observes. This plan makes the code match the 05-01 must-haves so 05-02 can execute after it.
Output: Two edited source files plus build-green and structural-grep evidence; 05-02 runs next.
</objective>

<execution_context>
@/home/lucas/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/lucas/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-session-refresh/05-CONTEXT.md
@.planning/phases/05-session-refresh/05-RESEARCH.md
@.planning/phases/05-session-refresh/05-UI-SPEC.md
@.planning/phases/05-session-refresh/05-VALIDATION.md
@.planning/phases/05-session-refresh/05-VERIFICATION.md
@.planning/phases/05-session-refresh/05-REVIEW.md
@.planning/phases/05-session-refresh/05-01-SUMMARY.md
@.planning/phases/05-session-refresh/05-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>WR-01 + WR-02: widen interceptor bypass, add same-page bounce guard</name>
  <files>frontend/src/services/api.js, frontend/src/stores/auth.js</files>
  <action>Apply the 05-REVIEW.md fixes verbatim in spirit to frontend/src/services/api.js (currently 48 lines: doBounce at 16-21, bypass at line 31). WR-01: widen the refresh-bypass gate so auth endpoints that legitimately 401 outside a session reject straight to the caller — either match /auth/(login|refresh|register) (e.g. `if (typeof config.url === 'string' && /\/auth\/(login|refresh|register)/.test(config.url)) throw error;`) or add a per-request _skipRefresh flag on the login call in stores/auth.js (authorized touch: flag only, no other store changes) and gate on it. Either approach is acceptable; keep the 401-strict gate (response?.status !== 401), the _retry guard, the ??= single-flight shape, and the no-router-import rule untouched. WR-02: add the same-page guard in doBounce() — when window.location.pathname is /login the origin falls back to / (e.g. `const safeOrigin = window.location.pathname === '/login' ? '/' : origin;`) before the assign() call; keep isRedirecting set before assign(). Do not touch Login.vue or Requests.vue in this task; do not add dependencies or backend edits.</action>
  <verify>
    <automated>cd frontend && npm run build</automated>
    <fails_when>non-zero exit from the build (Vite/Vue compile error in the edited service module)</fails_when>
  </verify>
  <done>Acceptance criteria: wrong-password POST /auth/login 401 rejects straight to the Login.vue err alert with no POST /auth/refresh fired and no self-bounce; a 401 raised while on /login bounces with redirect=/ instead of a nested /login redirect; 403/429/5xx/network paths still reject through untouched.</done>
</task>

<task type="auto">
  <name>WR-03: allowlist-check restored draft type, close out with structural proof</name>
  <files>frontend/src/views/Requests.vue</files>
  <action>Apply the 05-REVIEW.md WR-03 fix in frontend/src/views/Requests.vue restoreDraft() (line 71 currently restores form.value.type from any string): define the four valid options (EQUIPAMENTO, PUBLICACAO, VIAGEM, AUXILIO_ESTUDANTIL — matching the select at line 9) and restore type only when the stored value is in the allowlist, otherwise keep the current form default (e.g. `form.value.type = TYPES.includes(d.type) ? d.type : form.value.type;`). Keep title/justification/spec/valueCents shape checks, the unconditional clear-on-mount, the try/catch lifecycle, and the suppressPersist flag exactly as they are. After the edit, run the build gate plus the structural greps below and record observed outputs; then confirm git status shows no backend diff and no manifest diff (zero new dependencies, frontend-only).</action>
  <verify>
    <automated>cd frontend && npm run build</automated>
    <fails_when>non-zero exit from the build (Vite/Vue compile error in the edited view)</fails_when>
  </verify>
  <done>Acceptance criteria: a tampered slot such as {type:FOO} restores every other field but leaves type at the current default with the select showing a valid option; build green; no backend diff; no manifest diff.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→API (`/auth/login`, `/auth/refresh`, replayed requests) | Which 401s enter refresh vs reject straight through decides bounce reachability |
| bounce URL → Login (`?redirect=`) | Same-page nesting previously smuggled a stale login URL past the validator |
| sessionStorage draft slot | Tampered `type` string crosses into the enum select and the next submit |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Denial of service | api.js shared refresh promise | high | mitigate | Unchanged by this plan — ??= single-flight shape preserved; verified by re-run of 05-03 greps |
| T-05-02 | Denial of service | api.js retry/bounce guards | high | mitigate | Bypass widened narrowly to auth endpoints that legitimately 401 outside a session (WR-01); _retry guard, refresh bypass, and redirect-once flag ordering untouched; same-page guard added (WR-02) |
| T-05-03 | Spoofing | Login.vue ?redirect= handling | high | mitigate | Unchanged validator now receives a clean origin (no nested /login) thanks to the same-page guard |
| T-05-05 | Tampering | Requests.vue snapshot restore | low | mitigate | Upgraded from accept to mitigate: type restore allowlist-checked against the four valid options (WR-03); malformed slot still opens empty without blocking (D-11) |
| T-05-06 | Spoofing | 403 forced-password signal (Phase 8 SES-02) | medium | mitigate | Strict status === 401 gate preserved; re-confirmed by grep in close-out |
</threat_model>

<verification>
Build gate per 05-VALIDATION.md: `cd frontend && npm run build` green after each task commit. Structural proof of all three fixes (record observed outputs in the SUMMARY):
`grep -n "login|refresh\|_skipRefresh" frontend/src/services/api.js` (WR-01 bypass present),
`grep -n "pathname === '/login'" frontend/src/services/api.js` (WR-02 guard present),
`grep -n "TYPES" frontend/src/views/Requests.vue` (WR-03 allowlist present),
`grep -n "response?.status !== 401" frontend/src/services/api.js` (401-strict gate intact),
plus the 05-03 regression greps (single-instance hook, no per-view 401 handling, no router import in service).
Behavioral proof (SC1–SC3, notice, return-to-origin, draft round-trip) is owned by the 05-02 manual protocol — this plan explicitly does NOT re-run it; 05-02 must execute AFTER these gaps land, then the phase re-verifies.
</verification>

<success_criteria>
WR-01/WR-02/WR-03 fixed in api.js + Requests.vue with 05-REVIEW.md snippets applied; build green; all five structural greps resolve to the fixed lines; zero backend diff; zero manifest diff; 05-02 unblocked and next in line.
</success_criteria>

<output>
Create `.planning/phases/05-session-refresh/05-04-SUMMARY.md` when done
</output>
