---
phase: 05-session-refresh
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified: []
autonomous: false
requirements:
  - SES-01
user_setup: []

estimate:
  tokens: 25000
  raw_tokens: 16000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - During the single-flight refresh nothing new renders — no spinner, skeleton, banner, or route change, and form state is preserved; existing view loading refs (Entrando…/Salvando…) behave exactly as before
    - Refresh rejected → exactly one bounce to /login?reason=session-expired&redirect=<origem> guarded by a redirect-once flag; network errors, timeouts, and 5xx reject through with no refresh and no redirect
    - A plain /login visit (no reason param) renders no .alert.warn notice block
    - The .alert.warn notice never displays credential errors, and credential errors never use .alert.warn — a failed login after redirect shows div.alert.error[role=alert] alongside the notice
    - Login mounts with empty email/password, existing placeholders and the Entrar CTA intact, .alert.warn[role=status] above the fields showing the locked copy, and manual login fully usable in the same render
    - A 403 never enters the refresh path (status === 401 gate only), so no spinner or wait state is introduced for it — the view's own loading ref settles normally
    - A 403 rejects through untouched to the view's existing per-view err alert, so forced-change routing (SES-02, Phase 8) stays reachable
    - The module-level redirecting flag is set before assign() is called, so N concurrent rejected retries issue no further navigation and no stacked loading state ever renders
    - A dead refresh cookie leads to exactly one navigation to the session-expired login URL; the redirect-once flag prevents any /login dashboard loop and any stacked navigations
  artifacts:
    - frontend/src/services/api.js
    - frontend/src/views/Login.vue
    - frontend/src/views/Requests.vue
  key_links:
    - TTL-drop expiry → exactly one POST /auth/refresh per expiry with the view staying put
    - N parallel 401s → exactly one refresh in the network tab
    - Dead refresh cookie → exactly one bounce showing the locked notice → login returns to origin
---

<objective>
Execute the manual verification protocol that is the Definition of Done for SES-01 (per D-01..D-14,
05-UI-SPEC Interaction Contract rule 7, and 05-VALIDATION.md): prove silent single-flight refresh,
retry-once, bounce-once with the locked notice, validated return-to-origin, and best-effort draft
restore — and record the evidence. This plan modifies no source files; any temporary TTL change is
local-only and reverted before sign-off.

Purpose: The frontend has no test runner by design, so human-observed browser behavior plus a green
build is the gate (ROADMAP success criteria 1–3).
Output: Protocol evidence recorded in the plan SUMMARY (per-check PASS/FAIL with observed network-tab
facts); failures route back to 05-01 fixes, never to scope reduction.
</objective>

<execution_context>
@/home/lucas/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/lucas/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-session-refresh/05-CONTEXT.md
@.planning/phases/05-session-refresh/05-RESEARCH.md
@.planning/phases/05-session-refresh/05-UI-SPEC.md
@.planning/phases/05-session-refresh/05-VALIDATION.md
@.planning/phases/05-session-refresh/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Protocol part A: silent refresh and single-flight under concurrency</name>
  <files></files>
  <action>Prepare the rig per 05-VALIDATION.md: confirm `cd frontend && npm run build` is green on the 05-01 output, then start backend plus frontend locally (never commit any temporary change). Drop the access-token TTL to ~10s via a LOCAL-ONLY backend setting (revert unconditionally afterwards — git status must show no backend diff at sign-off), log in, and work continuously: the network tab must show exactly one POST /auth/refresh per expiry while the current view stays put with no spinner, banner, navigation, or lost form state (per D-14 single-flight is per-tab; observe one tab). Then force N parallel 401s (e.g. trigger several authenticated calls at once right after expiry) and confirm exactly one refresh in the network tab with all original requests succeeding (per D-02 the replay preserves config including credentials).</action>
  <verify>
    <automated>cd frontend && npm run build</automated>
    <fails_when>non-zero exit from the build (05-01 output broken before the protocol even starts)</fails_when>
    <human-check>Network tab shows exactly one POST /auth/refresh per expiry; the view never navigates, flickers, or loses form state; N parallel 401s produce exactly one refresh and all original requests succeed</human-check>
  </verify>
  <done>Acceptance criteria: one refresh per expiry observed; N parallel 401s collapse to one refresh; temporary TTL change fully reverted with clean git status for backend paths; observations written into the SUMMARY with PASS/FAIL per check.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Protocol part B: dead-cookie bounce, notice, return-to-origin, draft restore</name>
  <files></files>
  <action>With the 05-01 implementation running locally: first type a recognizable draft in Requests (title, justification, value) so the proactive persist writes the single-slot snapshot (per D-09..D-10). Then delete the refresh cookie (DevTools) and trigger any authenticated call: confirm exactly one navigation to /login?reason=session-expired&redirect=<origem> showing Sua sessão expirou. Entre novamente para continuar. in .alert.warn with role=status (per D-08, D-02). Confirm typing preserves the notice (D-06), a wrong-password attempt shows the credential error alert stacked below/alongside without touching the warn (D-07), and refresh-after-notice cleans the query from the URL (D-05). Log in and confirm landing back on the origin view with the draft restored (D-01, D-11). Then repeat with a crafted off-origin redirect value and confirm fallback to '/' with no off-origin navigation (D-04); corrupt the snapshot slot manually and confirm the form opens empty without blocking login or redirect (D-11); confirm a plain /login visit shows no notice (D-03).</action>
  <verify>
    <automated>cd frontend && npm run build</automated>
    <fails_when>non-zero exit from the build</fails_when>
    <human-check>Exactly one bounce navigation; locked notice verbatim with role=status; stacked warn/error roles never mixed; return-to-origin after login; off-origin redirect falls back to /; malformed snapshot opens an empty form without blocking; plain /login shows no notice</human-check>
  </verify>
  <done>Acceptance criteria: every check above recorded PASS with the observed URL/copy/facts in the SUMMARY, or FAIL routed as a named defect against 05-01 (file + expected vs observed); no FAIL is closed by reinterpreting the locked decisions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local rig → production contract | Temporary TTL shortening must never leak into committed code or deploy config |
| observer → evidence record | Human-observed network-tab facts are the gate; misrecorded evidence is a false PASS |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-02-P1 | Tampering | Temporary TTL change | medium | mitigate | Local-only edit, unconditional revert, clean `git status` on backend paths verified before sign-off |
| T-05-02-P2 | Repudiation | Protocol evidence | low | mitigate | Each check recorded with observed facts (request counts, URLs, copy) in 05-02-SUMMARY, not bare PASS marks |
| T-05-03 | Spoofing | ?redirect= round-trip under test | high | mitigate | Off-origin and double-slash probe values exercised live; fallback to '/' observed, not assumed |
</threat_model>

<verification>
Sampling per 05-VALIDATION.md: build green before and after the protocol; manual protocol evidence
recorded before `/gsd-verify-work`. This plan is the phase DoD — ROADMAP criteria 1–3 are proven
here, not by screenshots but by observed network-tab and navigation facts.
</verification>

<success_criteria>
All Part A and Part B checks recorded PASS with evidence; or named defects filed against 05-01 with
expected-vs-observed; temporary rig changes fully reverted; build green.
</success_criteria>

<output>
Create `.planning/phases/05-session-refresh/05-02-SUMMARY.md` when done
</output>
