---
phase: 05-session-refresh
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
files_modified: []
autonomous: true
requirements:
  - SES-01
user_setup: []

estimate:
  tokens: 20000
  raw_tokens: 13000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - During the single-flight refresh nothing new renders — no spinner, skeleton, banner, or route change, and form state is preserved
    - A 403 never enters the refresh path (status === 401 gate only); 403 rejects through untouched to the view's existing per-view err alert
    - The module-level redirecting flag is set before assign() is called, so N concurrent rejected retries issue no further navigation
    - The refresh is invisible — no user-observable change appears on any view other than the one new Login notice, and the build stays green
    - The 401 response hook is installed only on the shared axios instance; no per-view 401 handling exists anywhere; the service module imports no navigation module
  artifacts:
    - frontend/src/services/api.js
    - frontend/src/views/Login.vue
    - frontend/src/views/Requests.vue
  key_links:
    - Structural proof that the hook registration exists in exactly one module and nowhere per-view
    - Build-green close of the phase with zero new dependencies and zero backend diff
---

<objective>
Run the structural regression pass for SES-01 (per D-14, UI-SPEC Interaction Contract rules 1/5/6,
and 05-VALIDATION.md): prove the interceptor lives ONLY on the shared axios instance, no per-view
401 handling appeared, the service module is navigation-module-free, the status gate stayed
401-strict with 403 passthrough intact, and the build is green with zero new dependencies and zero
backend diff. This plan modifies no source files — it reads and reports.

Purpose: ROADMAP success criterion 4 (`npm run build` passes, no navigation-module import in the
shared service, no per-view 401 handling) must hold after the 05-01 tracer lands.
Output: Regression evidence in the plan SUMMARY; any violation is a named defect against 05-01.
</objective>

<execution_context>
@/home/lucas/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/lucas/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-session-refresh/05-CONTEXT.md
@.planning/phases/05-session-refresh/05-RESEARCH.md
@.planning/phases/05-session-refresh/05-UI-SPEC.md
@.planning/phases/05-session-refresh/05-VALIDATION.md
@.planning/phases/05-session-refresh/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Regression greps: single-instance hook, no per-view handling, strict status gate</name>
  <files></files>
  <action>Run read-only structural checks over frontend/src after the 05-01 tracer (no edits): confirm the response-hook registration string resolves to exactly one file — the shared HTTP service module — and appears in no view, store, or guard module; confirm the retry-guard flag likewise appears only in that same service module; confirm the service module contains no import of the SPA navigation module (the documented cycle is service ⇄ navigation ⇄ store, so full-reload navigation must remain the only mechanism there) while the Login view keeps its legitimate pre-existing navigation usage; confirm the error-status gate in the service module admits only the session-expired status with everything else (forbidden, rate-limited, server-error, network failure) rejecting through untouched — in particular the Phase 8 forced-password signal class must still reach views unaltered; confirm no view module added its own status-based session handling. Record each command with its observed output in the SUMMARY; any hit outside the allowed site is a named defect against 05-01, never a reason to widen the touch boundary.</action>
  <verify>
    <automated>cd frontend && grep -rln "interceptors" src && echo --- && grep -rln "_retry" src</automated>
    <fails_when>the hook registration or the retry-guard flag resolves to any file other than src/services/api.js, or to more than one file</fails_when>
  </verify>
  <done>Acceptance criteria: hook registration and retry-guard flag each resolve to exactly src/services/api.js and nowhere else; no per-view session handling exists; status gate admits only the session-expired status with forbidden/rate-limited/server-error/network paths passing through untouched.</done>
</task>

<task type="auto">
  <name>Close-out: navigation-free service, green build, zero-scope-drift</name>
  <files></files>
  <action>Run the remaining close-out checks read-only: confirm the shared service module carries no navigation-module import and performs its bounce exclusively through the full-reload call (per the no-cycle rule); confirm the locked notice copy and role=status marker exist in the Login view with the warn block stacked above the existing error alert; confirm the single-slot draft key with the pinned snapshot fields is referenced only by the Requests view (write on input/change, read-and-clear on mount, clear on successful submit); then run the frontend build gate and confirm no dependency manifest changed and no backend path shows a diff, per the zero-new-deps and frontend-only constraints.</action>
  <verify>
    <automated>cd frontend && npm run build</automated>
    <fails_when>non-zero exit from the build</fails_when>
  </verify>
  <done>Acceptance criteria: service module navigation-free with full-reload-only bounce; locked notice present verbatim with correct role and stacking; draft key referenced only by Requests view; build green; git status shows no backend diff and no manifest diff (zero new dependencies).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service module → navigation module | Any new import here reintroduces the documented init cycle; the boundary must stay import-free |
| view layer → session handling | Any per-view status handling fragments the single-instance contract and hides forced-password signals |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-03-P1 | Tampering | Service-module import graph | high | mitigate | Negative import check on the service module; violation blocks phase sign-off until removed |
| T-05-03-P2 | Tampering | Per-view session handling | medium | mitigate | Registry check that hook and retry-guard strings resolve to exactly one module; violations filed as 05-01 defects |
| T-05-06 | Spoofing | 403 forced-password signal (Phase 8 SES-02) | medium | mitigate | Status-gate strictness re-confirmed here so Phase 8 inherits an untouched 403 path |
</threat_model>

<verification>
Sampling per 05-VALIDATION.md: build green at wave close; structural greps recorded with observed
output in 05-03-SUMMARY. Combined with the 05-02 protocol evidence, this closes ROADMAP criterion 4
and the phase gate (build green + manual protocol evidence before `/gsd-verify-work`).
</verification>

<success_criteria>
All structural checks resolve to the single allowed site with observed outputs recorded; build
green; no backend diff; no manifest diff; any violation filed as a named 05-01 defect.
</success_criteria>

<output>
Create `.planning/phases/05-session-refresh/05-03-SUMMARY.md` when done
</output>
