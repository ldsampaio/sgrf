---
phase: 03-deploy-environment-contract
plan: 03
type: execute
wave: 3
depends_on: [03-01, 03-02]
files_modified:
  - docs/16-contrato-deploy.md
  - backend/.env.example
autonomous: false
requirements: [SEC-02, SEC-04]
estimate:
  tokens: 45000
  raw_tokens: 22500
  tasks: 3
  confidence: low
must_haves:
  truths:
    - A dedicated deploy doc exists as a validatable checklist with three steps (refuse insecure boot, public-URL login, dev intact), each carrying its command plus expected result (per D-12, D-13)
    - backend/.env.example documents COOKIE_SECURE and the production FRONTEND_URL shape with refusal-rule notes
    - Each checklist step renders as a markdown checklist item; renderer-owned wrapping applies with no raw HTML and tables of at most six columns (UI-SPEC zero-one-many and overflow rows)
    - '[Flagged assumption — unverified] Browsers exempt only localhost, not LAN IPs, from the Secure-cookie requirement (RESEARCH A4); the doc expected-failure note wording rests on this'
    - '[Flagged assumption — unverified] Long Secure-cookie failure messages wrap inside the verbatim login alert with no truncation directive (UI-SPEC unresolved overflow row); treated as assumption, not a styled fix'
  artifacts:
    - docs/16-contrato-deploy.md
    - backend/.env.example
  key_links:
    - Checklist step 1 exercises the plan 03-01 gate with placeholder values, never real secrets
    - Checklist step 2 exercises the plan 03-02 cookie wiring through the real Tunnel hostname
    - Checklist step 3 proves ./start-dev.sh still boots against localhost defaults
  prohibitions:
    - The contract doc never presents plain-HTTP LAN-IP login as a supported path; it is recorded as an expected failure by Secure-cookie design
    - The contract doc never contains real secret values, only variable names and placeholder shapes
---

<objective>
Document the environment contract (SEC-02 + SEC-04) as a validatable checklist plus production-noted .env.example, then human-verify the two manual-only behaviors (public-URL login round-trip, dev flow intact) on the real hosts.

Purpose: Undocumented env contracts rot into tribal knowledge and misconfigured deploys; a checklist with commands plus expected results turns every fresh deploy into a self-validating procedure.
Output: docs/16-contrato-deploy.md, updated .env.example, human-verified login and dev flow.
</objective>

<execution_context>
@/home/lucas/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/lucas/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/11-seguranca-e-auditoria.md
@backend/.env.example
</context>

<tasks>

<task type="auto">
  <name>Write deploy contract checklist doc</name>
  <files>docs/16-contrato-deploy.md</files>
  <read_first>docs/11-seguranca-e-auditoria.md (numbered-docs voice and CSRF residual-risk acceptance to reference for the lax decision), backend/.env.example (placeholder shapes the checklist step 1 reuses as refusal fixtures)</read_first>
  <acceptance_criteria>New doc holds three checklist steps each stating a run command and the exact expected result; topology path names Cloudflare edge to cloudflared to server entry port to app port with Tunnel as the only TLS termination; an explicit note records plain-HTTP LAN-IP login as an expected failure rather than a bug; a note records trust-proxy and rate-limit work as Phase 8 owned with nothing to configure here; CORS note mandates the public https host with no trailing slash</acceptance_criteria>
  <action>Per D-09 through D-13 create the contract at the next free numbered-docs slot, locking slot sixteen per the agent's discretion on doc placement. Structure it as a checklist, not prose: step one boots with NODE_ENV production plus placeholder-shaped values and records the refusal naming offenders; step two logs in through the public https hostname and records Secure cookie issuance plus logout deletion; step three boots the dev script and records localhost login intact. Record the edge-to-app topology path, the Cloudflare-only termination stance, the dropped LAN-IP check as expected failure, the exact-match CORS origin rule, and the Phase 8 deferral of proxy and rate-limit work. Keep markdown renderer-safe: no raw HTML, tables capped at six columns.</action>
  <verify>
    <automated>grep -n 'NODE_ENV=production' docs/16-contrato-deploy.md && grep -n 'COOKIE_SECURE=true' docs/16-contrato-deploy.md && grep -n 'start-dev' docs/16-contrato-deploy.md</automated>
    <fails_when>any grep exits non-zero (checklist step missing its command)</fails_when>
  </verify>
  <done>Fresh deploy validates step-by-step from the doc alone; no Caddy path documented or tested.</done>
</task>

<task type="auto">
  <name>Production notes in backend .env.example</name>
  <files>backend/.env.example</files>
  <read_first>backend/.env.example (placeholder plus Deploy-comment voice to copy; JWT lines 7-8 and FRONTEND_URL line 20 are the anchors)</read_first>
  <acceptance_criteria>File gains a COOKIE_SECURE entry with a production-true comment, JWT and admin lines carry comments stating values with the blocked substring or prefix refuse to boot under production, FRONTEND_URL carries a comment mandating the public https host with no trailing slash in production; dev defaults are byte-identical; suite and frontend build stay green</acceptance_criteria>
  <action>Per D-12 append the COOKIE_SECURE line plus production comments in the existing Deploy-comment voice: refusal rule for placeholder-shaped and dev-prefixed values on the secret lines, public-https-host shape for the frontend URL line, and explicit-true guidance for the cookie line. Change no default value in the file.</action>
  <verify>
    <automated>grep -v '^#' backend/.env.example | grep -c COOKIE_SECURE && cd backend && npx vitest run && cd ../frontend && npm run build</automated>
    <fails_when>grep count is not 1, or either build command exits non-zero</fails_when>
  </verify>
  <done>Example env teaches the production contract without altering any dev default; both CI jobs worth of checks pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Human verify: public-URL login and dev flow intact</name>
  <files>docs/16-contrato-deploy.md</files>
  <read_first>docs/16-contrato-deploy.md (checklist steps 2 and 3 being executed)</read_first>
  <acceptance_criteria>Operator confirms login through the public https hostname sets the Secure session cookie and logout deletes it, and confirms the dev script boots with localhost login working; both confirmations recorded in the plan SUMMARY</acceptance_criteria>
  <action>Walk the operator through checklist steps 2 (public https login round-trip plus logout deletion observed in browser devtools) and 3 (dev script boot plus localhost login) on the real hosts, since edge TLS plus real secrets cannot be automated in CI. Record pass or fail per step in the SUMMARY; on failure, file the exact observed behavior as a gap rather than editing code inline.</action>
  <verify>
    <human-check>Operator ran steps 2 and 3 on the real hosts and confirmed both pass</human-check>
  </verify>
  <done>Both manual-only behaviors are human-confirmed; SUMMARY records the confirmations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contract doc → operator | Doc contents drive production secret handling; wrong guidance becomes a live misconfiguration |
| example env → operator copy | Placeholder shapes get copied into real deploys; refusal rule must catch them |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-08 | Spoofing | docs/16-contrato-deploy.md operator guidance | medium | mitigate | Checklist asserts boot-refusal and live login round-trip, not just file presence; each step has command plus expected result |
| T-03-09 | Information disclosure | backend/.env.example / doc secret handling | medium | mitigate | Only var names and placeholder shapes appear; step 1 uses placeholder fixtures and real secrets never enter the doc or CI logs |
| T-03-10 | Tampering | plain-HTTP LAN-IP downgrade confusion | low | mitigate | Doc records LAN-IP login as expected failure by Secure-cookie design, so no operator files it as a bug or disables Secure to work around it |
</threat_model>

<verification>
Doc checklist greps pass; example env holds exactly one active COOKIE_SECURE line; full backend suite plus frontend build green; human checkpoint confirms both manual behaviors; git status shows only the two listed files changed.
</verification>

<success_criteria>
Contract documented as executable checklist, example env teaches production shape, public-URL login and dev flow human-verified, no code behavior changed by this plan.
</success_criteria>

<output>
Create `.planning/phases/03-deploy-environment-contract/03-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

Plan 03 produces (operator-facing, no runtime code):
- `docs/16-contrato-deploy.md` validatable checklist (boot-refuses-insecure-secret, public-URL login works, dev intact — each with command plus expected result, Tunnel-only topology, LAN-IP expected-failure note, Phase 8 proxy deferral note)
- `COOKIE_SECURE` entry plus production refusal-rule and https-host comments in `backend/.env.example`
- Human confirmations of the two manual-only behaviors recorded in `03-03-SUMMARY.md`
