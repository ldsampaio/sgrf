---
phase: 03-deploy-environment-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/config/env.js
  - backend/tests/env-gate.test.js
  - .github/workflows/ci.yml
autonomous: true
requirements: [SEC-02]
estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - Booting with NODE_ENV=production and any of JWT_ACCESS_SECRET, JWT_REFRESH_SECRET, INITIAL_ADMIN_EMAIL, INITIAL_ADMIN_TEMPORARY_PASSWORD empty, containing change-me, or starting with dev- throws at env.js load naming the offending vars (per D-01..D-04)
    - Booting with NODE_ENV unset, development, or test keeps the existing dev fallbacks with no throw, and ./start-dev.sh works unchanged
    - COOKIE_SECURE explicit true/false wins; absent follows NODE_ENV with production mapping to true (per D-05)
    - CI backend job exports dummy JWT and admin values so the production gate can never trip CI regardless of implicit NODE_ENV
    - '[Flagged assumption — unverified] SEC-02 edge probe returned unclassified — review manually; gate semantics are treated as fully captured by D-01..D-04 with no hidden sub-behavior'
  artifacts:
    - backend/src/config/env.js
    - backend/tests/env-gate.test.js
    - .github/workflows/ci.yml
  key_links:
    - env.js gate runs before module.exports is built so no insecure value ever escapes into the app
    - env.cookieSecure export is the single source plan 03-02 consumes in tokens.js
    - CI backend job env dummies decouple the gate from any implicit NODE_ENV=production in runners
  prohibitions:
    - The gate error names only variable names plus fix hints; actual secret values never appear in the thrown message or logs
    - The gate never demands real secrets outside NODE_ENV=production; dev and test fallbacks keep working unchanged
---

<objective>
End-to-end production fail-fast on insecure secret values (SEC-02): one tracer slice proving config load refuses to boot under NODE_ENV=production, expanded to the full four-var matrix plus the COOKIE_SECURE parse helper, with CI belt-and-braces so the gate can never trip the Phase 1 regression gate.

Purpose: Placeholder secrets reaching production mean admin takeover; presence-only compose guards miss set-but-placeholder values. This plan is the inner value-check layer.
Output: Gated env.js, env-gate.test.js, CI dummy env — all green.
</objective>

<execution_context>
@/home/lucas/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/lucas/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/src/config/env.js
@backend/tests/unit.test.js
@.github/workflows/ci.yml
</context>

<tasks>

<task type="tracer">
  <name>End-to-end production boot refuses insecure secrets — one path only</name>
  <files>backend/src/config/env.js, backend/tests/env-gate.test.js</files>
  <read_first>backend/src/config/env.js (gate lands before module.exports; current dev fallbacks on lines 9-10, 20-21), backend/tests/unit.test.js (ESM vitest idiom with Portuguese labels to copy)</read_first>
  <acceptance_criteria>Loading env.js with NODE_ENV=production and JWT_ACCESS_SECRET set to a dev- placeholder throws an Error naming JWT_ACCESS_SECRET; loading it with NODE_ENV unset keeps dev fallbacks with no throw; both behaviors are asserted by the new test file</acceptance_criteria>
  <action>Per D-01 and D-04, add an isInsecureSecret value predicate (empty or whitespace-only, contains the blocked placeholder substring, starts with the blocked dev prefix) plus a strict NODE_ENV equals production guard placed immediately after the dotenv load line and before module.exports is built, initially covering JWT_ACCESS_SECRET and JWT_REFRESH_SECRET read from resolved process.env values, collecting offenders and throwing one Error that enumerates each offending name with a one-line fix hint. Create backend/tests/env-gate.test.js following the unit.test.js ESM describe/it idiom; because env.js is CommonJS, load it fresh per case with createRequire from module after vi.resetModules, saving and restoring process.env around each case. Cover exactly two cases in this slice: production plus insecure JWT value refuses to load while identifying the var, and unset NODE_ENV loads dev fallbacks without refusal.</action>
  <verify>
    <automated>cd backend && npx vitest run tests/env-gate.test.js</automated>
    <fails_when>non-zero exit, or output contains No test files found, or output contains 0 passed</fails_when>
  </verify>
  <done>The single happy path (insecure prod value refused) and the dev path (fallbacks intact) both pass end-to-end through real module load and are committed.</done>
</task>

<task type="auto">
  <name>Expand gate to all four vars plus admin email and cookieSecure parse</name>
  <files>backend/src/config/env.js, backend/tests/env-gate.test.js</files>
  <read_first>backend/src/config/env.js (tracer gate from previous task), backend/src/utils/helpers.js (institutional suffix one-liner to inline — env.js stays dependency-free per pattern map, so duplicate the check rather than importing)</read_first>
  <acceptance_criteria>INITIAL_ADMIN_EMAIL without the institutional suffix is refused under production; empty or placeholder INITIAL_ADMIN_TEMPORARY_PASSWORD is refused under production; COOKIE_SECURE explicit true yields true and explicit false yields false regardless of NODE_ENV while absent tracks production equality; full suite stays green</acceptance_criteria>
  <action>Per D-02 and D-03 extend the offender collection to INITIAL_ADMIN_EMAIL (blocked-value predicate plus institutional suffix requirement on the lowercased resolved value) and INITIAL_ADMIN_TEMPORARY_PASSWORD (blocked-value predicate), keeping the single-throw shape that enumerates every offender. Per D-05 add the cookieSecure export to the same module using the triple-equals-true string comparison convention already used three times in this file: explicit COOKIE_SECURE true yields true, explicit false yields false, otherwise strict NODE_ENV production equality decides. Extend the test file with one case per new behavior reusing the save-restore plus fresh-load technique, asserting refusal identifies var names while never echoing secret contents.</action>
  <verify>
    <automated>cd backend && npx vitest run</automated>
    <fails_when>non-zero exit or any suite reports failed</fails_when>
  </verify>
  <done>All four vars gate under production, admin email enforces institutional suffix, cookieSecure parse matrix passes, full backend suite green.</done>
</task>

<task type="auto">
  <name>CI belt-and-braces dummy secrets in backend job env</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>.github/workflows/ci.yml (backend job env block holding only DATABASE_URL today)</read_first>
  <acceptance_criteria>Backend job env block holds non-placeholder JWT_ACCESS_SECRET, JWT_REFRESH_SECRET, INITIAL_ADMIN_EMAIL with institutional suffix, and INITIAL_ADMIN_TEMPORARY_PASSWORD; workflow Node version, working directories, cache paths, and prisma generate step are byte-identical; full suite still green locally</acceptance_criteria>
  <action>Per D-04 add dummy JWT_ACCESS_SECRET, JWT_REFRESH_SECRET, INITIAL_ADMIN_EMAIL (institutional domain, non-empty), and INITIAL_ADMIN_TEMPORARY_PASSWORD entries under the backend job env mapping, choosing obviously fake values that avoid both the blocked placeholder substring and the blocked dev prefix so they satisfy the value predicate under any NODE_ENV. Leave every other workflow line untouched.</action>
  <verify>
    <automated>grep -n 'JWT_ACCESS_SECRET' .github/workflows/ci.yml && grep -n 'INITIAL_ADMIN_EMAIL' .github/workflows/ci.yml && cd backend && npx vitest run</automated>
    <fails_when>either grep exits non-zero (name absent) or the vitest run exits non-zero</fails_when>
  </verify>
  <done>CI backend job carries dummy secrets, gate can never trip CI, workflow diff touches only the env block.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator env → env.js load | Untrusted-or-placeholder secret values cross into boot-time config |
| CI runner env → vitest | Implicit NODE_ENV in runners could arm the production gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Spoofing | backend/src/config/env.js gate | high | mitigate | Value-blocklist throw on all four vars strictly under NODE_ENV=production; proven by env-gate.test.js throw/no-throw matrix |
| T-03-02 | Tampering | backend/.env.example placeholders copied to prod | medium | mitigate | Blocked-substring rule catches change-me placeholders even when set; production notes land in plan 03-03 |
| T-03-03 | Denial of service | CI tripped by over-broad gate | low | mitigate | Strict NODE_ENV equality plus dummy JWT/admin env in backend job; full suite must stay green |
</threat_model>

<verification>
Full backend suite green via cd backend && npx vitest run; exactly three files changed per git status; no frontend file touched; no trust-proxy or rate-limit configuration added anywhere.
</verification>

<success_criteria>
NODE_ENV=production with insecure values refuses to boot naming offenders; non-production boots unchanged; CI carries dummy secrets; suite green.
</success_criteria>

<output>
Create `.planning/phases/03-deploy-environment-contract/03-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

Plan 01 produces (consumed by later plans and CI):
- `isInsecureSecret` value predicate + production-only throw gate in `backend/src/config/env.js` (plan 03-02 consumes nothing from the gate, but consumes `cookieSecure` below)
- `cookieSecure` export in `backend/src/config/env.js` (single source; consumed by `tokens.js` in plan 03-02)
- `backend/tests/env-gate.test.js` throw/no-throw matrix (extended by plan 03-02 with cookieOpts assertions)
- Dummy `JWT_ACCESS_SECRET` / `JWT_REFRESH_SECRET` / `INITIAL_ADMIN_*` entries in `.github/workflows/ci.yml` backend job env
