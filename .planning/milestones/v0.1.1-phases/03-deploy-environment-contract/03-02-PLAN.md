---
phase: 03-deploy-environment-contract
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - backend/src/utils/tokens.js
  - backend/src/controllers/authController.js
  - backend/tests/env-gate.test.js
  - compose.yaml
autonomous: true
requirements: [SEC-04]
estimate:
  tokens: 55000
  raw_tokens: 27500
  tasks: 3
  confidence: low
must_haves:
  truths:
    - cookieOpts.secure follows env.cookieSecure (explicit COOKIE_SECURE wins, absent tracks NODE_ENV) instead of reading raw process.env, while sameSite stays lax (per D-05, D-08)
    - Logout deletes both session cookies with attributes matching how they were set, so Secure cookies are reliably removed (Pitfall 3 fix)
    - Compose wires COOKIE_SECURE with a production-true default while FRONTEND_URL stays operator-overridable
    - Valid credentials submitted through the public https URL land on / with the Secure session cookie set (UI-SPEC populated row)
    - Cookie or prod-URL misconfiguration surfaces through the existing login error alert with the server message or Falha no login, never as a silent no-login (UI-SPEC error row)
    - The login submit button still disables and reads Entrando while auth is in flight, resetting afterwards (UI-SPEC loading row, locked unchanged)
    - '[Flagged assumption — unverified] SEC-04 edge probe returned unclassified — review manually; cookie semantics are treated as fully captured by D-05..D-08 with no hidden sub-behavior'
    - '[Flagged assumption] Secure is a browser-side attribute so plain-HTTP cloudflared-to-app hops do not affect it (RESEARCH A2); the public-URL login check proves it empirically'
    - '[Flagged assumption] clearCookie must match secure/sameSite to delete Secure cookies (RESEARCH A3); the spread fix is harmless even if browsers are lenient'
  artifacts:
    - backend/src/utils/tokens.js
    - backend/src/controllers/authController.js
    - compose.yaml
  key_links:
    - tokens.js consumes env.cookieSecure from plan 03-01; no second source of truth for the Secure decision remains
    - authController set-cookie and clear-cookie paths share one cookieOpts object so attributes cannot drift
    - compose COOKIE_SECURE=true sits next to FRONTEND_URL=https public host so the Secure decision and the CORS origin agree
  prohibitions:
    - Production session cookies are never issued without the Secure attribute on a non-localhost plain-HTTP URL
    - Phase 3 configures no trust proxy or rate-limit behavior; that work stays atomic in Phase 8 per D-11
---

<objective>
Resolve the cookie/HTTPS story behind Cloudflare Tunnel (SEC-04): Secure flag follows COOKIE_SECURE, logout reliably deletes Secure cookies, compose wires the production defaults — with the login flow observably unchanged except working through the public https URL.

Purpose: Login silently fails off localhost when the Secure flag is hardcoded or misdetected; logout that cannot delete Secure cookies leaves sessions alive. One shared cookieOpts object plus explicit env wiring removes both.
Output: tokens.js, authController.js, compose.yaml updated; suite green; no proxy-related configuration added.
</objective>

<execution_context>
@/home/lucas/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/lucas/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/src/utils/tokens.js
@backend/src/controllers/authController.js
@compose.yaml
</context>

<tasks>

<task type="auto">
  <name>Secure flag follows env.cookieSecure in tokens.js</name>
  <files>backend/src/utils/tokens.js, backend/tests/env-gate.test.js</files>
  <read_first>backend/src/utils/tokens.js (cookieOpts block lines 20-25; module already imports env on line 2), backend/src/config/env.js (cookieSecure export from plan 03-01)</read_first>
  <acceptance_criteria>cookieOpts.secure equals true when COOKIE_SECURE is true under any NODE_ENV, equals false when COOKIE_SECURE is false under production, and tracks NODE_ENV production equality when COOKIE_SECURE is absent; sameSite remains lax; no raw process.env NODE_ENV read remains in tokens.js; suite green</acceptance_criteria>
  <action>Per D-05 and D-08 point the secure field of the shared cookieOpts object at the typed-config export from plan 03-01 and leave httpOnly, sameSite, and path exactly as they are. Remove the direct raw-environment read so the typed config module is the only source of truth for the Secure decision. Add fresh-module-load cases to the gate test file asserting the three-way matrix (explicit true, explicit false under production, absent tracking NODE_ENV) by inspecting the exported cookieOpts object.</action>
  <verify>
    <automated>cd backend && npx vitest run tests/env-gate.test.js</automated>
    <fails_when>non-zero exit or any case reports failed</fails_when>
  </verify>
  <done>Secure decision flows from one typed-config export through every Set-Cookie call site unchanged.</done>
</task>

<task type="auto">
  <name>Logout clearCookie matches set attributes</name>
  <files>backend/src/controllers/authController.js</files>
  <read_first>backend/src/controllers/authController.js (logout function lines 66-70 clearing with path-only options; set-cookie call sites lines 39-40 and 59 spreading cookieOpts)</read_first>
  <acceptance_criteria>Both clearCookie calls carry the same secure, sameSite, httpOnly, and path attributes the cookies were set with, minus maxAge; login, refresh, and me handlers are byte-identical; suite green</acceptance_criteria>
  <action>Per RESEARCH Pitfall 3, spread the shared cookieOpts object into both clearCookie invocations in the logout handler while omitting the lifetime field, so deletion attributes always track whatever the Secure decision resolves to. Do not alter the login, refresh, or any other handler in this file.</action>
  <verify>
    <automated>cd backend && npx vitest run</automated>
    <fails_when>non-zero exit or any suite reports failed</fails_when>
  </verify>
  <done>Logout reliably removes both session cookies including Secure variants; no other auth behavior changed.</done>
</task>

<task type="auto">
  <name>Compose wires COOKIE_SECURE with production default</name>
  <files>compose.yaml</files>
  <read_first>compose.yaml lines 29-45 (FRONTEND_URL overridable line 34, presence guards 35-38, optional-with-default SMTP lines 39-45 whose substitution shape to copy)</read_first>
  <acceptance_criteria>App service environment contains a COOKIE_SECURE entry defaulting to true; FRONTEND_URL line, all presence guards, and every SMTP line are byte-identical; no trust-proxy or rate-limit setting appears anywhere in the file</acceptance_criteria>
  <action>Per D-06 and D-07 add the COOKIE_SECURE wiring to the app service environment block copying the optional-with-default substitution shape, defaulting to the production-safe value so the Secure decision and the public-https CORS origin agree out of the box while operators can still override per deploy. Leave FRONTEND_URL overridable with its existing default untouched, keep every presence guard as-is, and per D-11 add nothing proxy- or rate-limit-related.</action>
  <verify>
    <automated>grep -n 'COOKIE_SECURE' compose.yaml</automated>
    <fails_when>non-zero exit (name absent) or output contains trust or rate</fails_when>
  </verify>
  <done>Compose declares the Secure default for production deploys; topology ownership stays with Cloudflare Tunnel per D-09.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cloudflare edge → browser | Secure cookies accepted only over edge HTTPS; internal plain-HTTP hops never touch cookie enforcement |
| operator env → compose → app | COOKIE_SECURE and FRONTEND_URL values flow into cookie flags and CORS origin |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04 | Information disclosure | backend/src/utils/tokens.js cookieOpts | high | mitigate | Secure follows explicit COOKIE_SECURE with production-true default; sameSite stays lax; proven by cookieOpts matrix cases |
| T-03-05 | Spoofing | compose.yaml FRONTEND_URL / CORS origin | medium | mitigate | Single exact origin must equal the public https host; compose keeps it operator-overridable, contract doc (plan 03-03) mandates the shape |
| T-03-06 | Tampering | backend/src/controllers/authController.js logout | medium | mitigate | clearCookie spreads shared cookieOpts so deletion attributes match issuance; session cannot linger after logout |
| T-03-07 | Denial of service | rate-limit bucketing all Tunnel users as one IP | medium | accept | Acknowledged necessary but atomic in Phase 8 SEC-03 per D-11; Phase 3 configures nothing proxy-related so no half-fix ships |
</threat_model>

<verification>
Full backend suite green via cd backend && npx vitest run; tokens.js holds no raw NODE_ENV read; compose diff touches only the COOKIE_SECURE line; git status shows only the three listed files plus the extended test file.
</verification>

<success_criteria>
Secure flag is operator-controlled per deploy, logout deletes Secure cookies, compose defaults to production-safe, suite green, zero proxy configuration added.
</success_criteria>

<output>
Create `.planning/phases/03-deploy-environment-contract/03-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

Plan 02 produces (consumed by the contract doc and operators):
- `secure` field of `cookieOpts` in `backend/src/utils/tokens.js` sourced from `env.cookieSecure` (documented in plan 03-03 checklist step 2)
- Attribute-matched `clearCookie` calls in `logout` in `backend/src/controllers/authController.js` (verified by the plan 03-03 public-URL logout check)
- `COOKIE_SECURE` wiring with production-true default in `compose.yaml` (referenced by plan 03-03 and `.env.example` notes)
- `cookieOpts` Secure-decision matrix cases appended to `backend/tests/env-gate.test.js`
